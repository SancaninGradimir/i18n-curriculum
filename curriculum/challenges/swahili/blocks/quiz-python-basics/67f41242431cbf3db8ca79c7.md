---
id: 67f41242431cbf3db8ca79c7
title: Kviz o osnovama Pythona
challengeType: 8
dashedName: quiz-python-basics
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koja od sledećih funkcija se koristi za proveru da li varijabla odgovara određenom tipu podataka?
#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--
Koja od sledećih nije oblik konkatenacije niza karaktera?
#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--
Koja od sledećih funkcija se koristi za vraćanje broja karaktera u stringu?
#### --distractors--

`counting()`

---

`length()`

---

`iscount()`

#### --answer--

`len()`

### --question--

#### --text--
Šta će `result` biti u ovom primeru?

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--
Šta se dešava kada dodate float i integer?
#### --distractors--
Rezultat će biti poruka o grešci.

---

The result will be `None`.

---

Rezultat će biti ceo broj.
#### --answer--
Rezultat će biti float.
### --question--

#### --text--
Koji od sledećih je ispravan način za definisanje funkcije?
#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2 
} 
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2 
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--
Šta će se ispisati u konzolu?

```py
def greet():
    pass
    
print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--
Koja od sledećih tvrdnji je netačna prilikom imenovanja varijabli?
#### --distractors--
Ime varijable može početi samo sa slovom ili donjom crtom (\_), a ne sa brojem.

---

Varijable imena smeju da sadrže samo alfanumeričke karaktere (a-z, A-Z, 0-9) i donje crte (_).

---

Nazivi varijabli ne smeju biti jedna od rezervisane ključne reči Pythona, kao što su `if`, `class`, ili `def`.
#### --answer--
Imena promenljivih moraju imati maksimalnu dužinu od 10 karaktera, inače će Python baciti grešku.
### --question--

#### --text--
Koji od sledećih nije podržan tip podataka u Pythonu?
#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--
Koja od sledećih će vratiti novi string sa svim karakterima konvertovanim u VELIKA SLOVA?
#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--
Koja od sledećih funkcija se koristi za dobijanje unosa od korisnika?
#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--
Šta će biti rezultat za sledeći kod?

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--
Šta radi metod ``split()``?
#### --distractors--
Ovaj metod se koristi za razdvajanje tupa u listu podstringova.

---

Ovaj metod se koristi za razdvajanje float-a u listu podstringova.

---

Ovaj metod se koristi za razdvajanje rečnika u listu podstringova.
#### --answer--
Ovaj metod se koristi za razdvajanje niza u listu podniza.
### --question--

#### --text--
Šta će sledeći kod ispisati u konzolu?

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--
Koju od sledećih funkcija se koristi za kreiranje tabele mapiranja karaktera jedan na jedan za prevođenje?
#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--
Koji će biti rezultat za sledeći kod?

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--
8

---

2

---

4
#### --answer--
16
### --question--

#### --text--
Koja vrednost će biti vraćena iz metode ``find()`` ako se u stringu ne pronađe nijedno pojavljivanje podstringa?
#### --distractors--
-2

---

1

---

0
#### --answer--
-1
### --question--

#### --text--
Koja od sledećih funkcija se koristi za brojanje koliko puta se podstring pojavljuje u nekom stringu?
#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--
Šta radi celobrojno deljenje u Pythonu?
#### --distractors--
Ovaj operator se koristi za množenje dva broja i za zaokruživanje rezultata na najbliži ceo broj.

---

Ovaj operator se koristi za konverziju plutajućeg broja u ceo broj.

---

Ovaj operator se koristi za podizanje broja na određeni stepen.
#### --answer--
Ovaj operator se koristi za deljenje dva broja i za zaokruživanje rezultata na najbliži ceo broj.
### --question--

#### --text--
Koja od sledećih funkcija se koristi za zaokruživanje broja na najbliži ceo broj?
#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`

