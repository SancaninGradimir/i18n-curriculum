---
id: 67358ac128957c865dcf3ddf
title: Kviz o JavaScript klasama
challengeType: 8
dashedName: quiz-javascript-classes
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koja je svrha klase u JavaScript-u?
#### --distractors--
Definisanje samo svojstava, a ne metoda.

---

Za definisanje samo statičkih promenljivih.

---

Za organizovanje samo primitivnih podataka.
#### --answer--
Kreiranje ponovo upotrebljivih šablona za objekte.
### --question--

#### --text--
Kako definišete klasu u JavaScript-u?
#### --distractors--
Korišćenje ključne reči `className`.

---

Korišćenje ključne reči `object`.

---

Koristeći samo ključnu reč `constructor`.
#### --answer--
Korišćenje ključne reči `class`.
### --question--

#### --text--
Koji metod se koristi u klasi za inicijalizaciju svojstava?
#### --distractors--

`static`

---

`init`

---

`define`

#### --answer--

`constructor`

### --question--

#### --text--
Šta predstavlja ``this`` unutar konstruktora klase?
#### --distractors--
Globalni objekat klase.

---

Sama funkcija.

---

Nedefinisana referenca.
#### --answer--
Instanca klase.
### --question--

#### --text--
Koja od sledećih opisa kako se ``this`` ponaša u strelicastom funkciji unutar metode klase?
#### --distractors--
Nasleđuje vrednost `this` od najbliže funkcije u drugačijem opsegu.

---

Odnosi se na novokreiranu instancu klase.

---

Nasleđuje vrednost `this` iz globalnog objekta u strogom modu.
#### --answer--
Nasleđuje vrednost `this` iz okružujućeg opsega gde je definisano.
### --question--

#### --text--
Koja je primarna svrha ključne reči `extends`?
#### --distractors--
Definisanje statičkih metoda u konstruktoru roditeljske klase.

---

Kreiranje metode u klasi.

---

Inicijalizuje podrazumevana svojstva u roditeljskoj klasi.
#### --answer--
Kreiranje podklase koja nasleđuje od bazne klase.
### --question--

#### --text--
Koji od sledećih je primer kako kreirati klasu ``Car`` koja nasleđuje od klase ``Vehicle``?
#### --distractors--

`class Car uses Vehicle {}`

---

`class Car = Vehicle {}`

---

`class Car inherits Vehicle {}`

#### --answer--

`class Car extends Vehicle {}`

### --question--

#### --text--
Koji je izlaz sledećeg koda?

```javascript
class Animal {
  speak() {
    return "Animal speaks";
  }
}

class Dog extends Animal {
  speak() {
    return "Dog barks";
  }
}

const myDog = new Dog();
console.log(myDog.speak());
```

#### --distractors--

`Animal speaks`

---

Izbačena je greška.

---

`undefined`

#### --answer--

`Dog barks`

### --question--

#### --text--
Koja je funkcija ključne reči ``super`` u podklasi?
#### --distractors--
Da definše novu instancu roditeljske klase.

---

Brisanje metoda i svojstava iz roditeljske klase.

---

Za pristup samo statičkim metodama roditeljske klase.
#### --answer--
Za pozivanje konstruktora ili metoda nadklase.
### --question--

#### --text--
Koja od sledećih opisa statičnu metodu?
#### --distractors--
Metoda koja je nedostupna unutar same klase.

---

Metoda koja se koristi samo u arrow funkcijama.

---

Metoda koja je jedinstvena za svaku instancu klase.
#### --answer--
Metoda koja pripada samoj klasi, a ne bilo kom instanci.
### --question--

#### --text--
Šta će sledeći kod izbaciti?

```js
class Calculator {
  static add(a, b) {
    return a + b;
  }
}

console.log(Calculator.add(3, 4));
```

#### --distractors--
Došlo je do greške.

---

`undefined`

---

`NaN`

#### --answer--

`7`

### --question--

#### --text--
Kako se pristupa statičkom svojstvu u klasi JavaScript-a?
#### --distractors--
Korišćenjem ``this.propertyName`` unutar metode.

---

Korišćenjem `instance.propertyName` nakon kreiranja instance.

---

Korišćenjem `super.propertyName` unutar podklase.
#### --answer--
Korišćenjem `ClassName.propertyName`.
### --question--

#### --text--
U kom scenariju biste koristili statičku metodu umesto metode instance?
#### --distractors--
Kada želite da pristupite i promenite svojstva instance.

---

Kada je potrebno pozvati metodu na instanci objekta.

---

Kada metoda ne zavisi od podataka specifičnih za instancu i operiše na podatke nivoa klase.
#### --answer--
Kada metoda operiše na podacima koje dele sve instance klase.
### --question--

#### --text--
Šta će sledeći kod izbaciti?

```js
class MyClass {
  sayHello() {
    return "Hello!";
  }
}

console.log(MyClass.sayHello());
```

#### --distractors--

`Hello!`

---

`null`

---

`undefined`

#### --answer--
Baca grešku.
### --question--

#### --text--
Koja ključna reč se koristi za nasleđivanje svojstava i metoda iz druge klase?
#### --distractors--

`import`

---

`inherit`

---

`static`

#### --answer--

`extends`

### --question--

#### --text--
Šta je ispisano sledećim kodom?

```js
class Person {
  static species = "Human";
}

console.log(Person.species);
```

#### --distractors--

`undefined`

---

`null`

---

Izbačena je greška.
#### --answer--

`Human`

### --question--

#### --text--
Koji od sledećih najbolje opisuje nasleđivanje?
#### --distractors--
Omogućava metodi vraćanje novog objekta.

---

Omogućava instanci da definiše sopstvena svojstva.

---

Omogućava konverziju statičkog svojstva u nestatičko svojstvo.
#### --answer--
Omogućava klasi da nasledi svojstva i metode od druge klase.
### --question--

#### --text--
Šta se dešava ako konstruktor nije definisan u podklasi?
#### --distractors--
Biće izbačena greška.

---

Podklasa će nasleđivati konstruktor od bilo koje klase u kodu.

---

Podklasa ne može kreirati nove instance.
#### --answer--
Konstruktor superklase će biti korišćen podrazumevano.
### --question--

#### --text--
Šta `this` predstavlja u sledećem kodu?

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
}
```

#### --distractors--
Globalni objekat.

---

Sama klasa `Animal`.

---

Neredefinisana promenljiva.
#### --answer--
Instanca od `Animal`.
### --question--

#### --text--
Koja je primarna upotreba statičkih svojstava?
#### --distractors--
Za definisanje podrazumevanih svojstava za svaku instancu klase.

---

Nadjačavanje metoda u podklasi.

---

Za definisanje podataka specifičnih za instancu.
#### --answer--
Za skladištenje podataka koji su dostupni celoj klasi i koje dele sve instance.