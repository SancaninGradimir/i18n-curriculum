---
id: 66edd43cded6bff30944b676
title: Kviz o rekurziji
challengeType: 8
dashedName: quiz-recursion
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 9 od 10 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Šta je rekurzija u programiranju?
#### --distractors--
Metoda sortiranja nizova.

---

Beskonačna petlja.

---

Funkcija koja vraća `undefined`.
#### --answer--
Proces u kojem funkcija poziva samu sebe.
### --question--

#### --text--
Koji od sledećih je primer rekurzije?
#### --distractors--

```js
function factorial(n) {
  let result = 1;

  while (n > 0) {
    result = result * n;
    n--;
  }

  return result;
}
```

---

```js
function factorial(n) {
  const arr = Array(n).fill().map((_, i) => i + 1);
  return arr.reduce((acc, curr) => acc * curr, 1);
}
```

---

```js
function factorial(n) {
  let result = 1;

  for (let i = n; i > 0; i--) {
    result *= i;
  }

  return result;
}
```

#### --answer--

```js
function factorial(n) {
  if (n === 0 || n === 1) {
    return 1;
  }
  return n * factorial(n - 1);
}
```

### --question--

#### --text--
Šta će sledeća funkcija vratiti?

```js
function sum(n) {
  if (n === 0) return 0;
  return n + sum(n - 1);
}
sum(3);
```

#### --distractors--
3

---

0

---

1
#### --answer--
Please provide the text you would like me to translate. I am ready to follow all instructions: translating all user-facing text into Serbian (sr-RS), while leaving code blocks, URLs, technical terms, and programming keywords untouched, and ensuring natural, grammatically correct Serbian terminology.
### --question--

#### --text--
Koliko puta će se funkcija ``mystery`` pozvati?

```javascript
function mystery(n) {
  if (n <= 1) return 1;
  return mystery(n - 2);
}
mystery(5);
```

#### --distractors--
2

---

5

---

4
#### --answer--
3
### --question--

#### --text--
Koja od sledećih tvrdnji je tačna u vezi sa rekurzijom?
#### --distractors--
Ne bi trebalo da ima bazni slučaj.

---

Koristi se isključivo za Fibonaccijev niz.

---

Koristi se samo za faktorijale.
#### --answer--
Trebalo bi uvek da ima bazni slučaj.
### --question--

#### --text--
Koji od sledećih opcija bi bio odgovarajući bazni slučaj za dati primer?

```js
function countDownToZero(number) {
  // Base case goes here.

  console.log(number);
  countDownToZero(number - 1);
}
```

#### --distractors--

```js
if (number > 0) {
  return;
}
```

---

```js
if (number !== 0) {
  return;
}
```

---

```js
if (number === 0) {
  return;
}
```

#### --answer--

```js
if (number < 0) {
  return;
}
```

### --question--

#### --text--
Zašto mora rekurzija da ima bazni slučaj?
#### --distractors--
Da bi se osiguralo da funkcija uvek ima vrednost povratka.

---

Smanjenje broja poziva funkcija.

---

Da bi se osiguralo da svi rekurzivni pozivi budu izvršeni u ispravnom redosledu i da se ne baci ``RecursionError``.
#### --answer--
Omogućiti funkciji da prekine svoje rekurzivne pozive i spreči beskonačne petlje.
### --question--

#### --text--
Šta će ova rekurzivna funkcija da uradi?

```js
function repeatString(str) {
  return str + repeatString(str);
}
```

#### --distractors--
Vrati string dvaput.

---

Return `undefined`.

---

Kreiraj prazan string.
#### --answer--
Uzrokovanje preljeva steka.
### --question--

#### --text--
Šta je stack poziva?
#### --distractors--
Lista izvršenih poziva funkcija.

---

Lista vrednosti koje je funkcija vratila.

---

Specijalna funkcija koja se koristi za pozivanje funkcije tačno tri puta.
#### --answer--
Struktura podataka koja prati pozive funkcija i redosled njihovog izvršavanja.
### --question--

#### --text--
Šta ova rekurzivna funkcija radi?

```js
function changeString(str) {
  if (str === "") return "";
  return changeString(str.slice(1)) + str[0];
}
```

#### --distractors--
Duplira string.

---

Uklanjanje samoglasnika.

---

Uklanja razmake.
#### --answer--
Obrnuti niz znakova.

