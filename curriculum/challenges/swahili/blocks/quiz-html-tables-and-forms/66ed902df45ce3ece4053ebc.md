---
id: 66ed902df45ce3ece4053ebc
title: HTML Tabele i Forme Kviz
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koji od sledećih atributa se koristi za određivanje URL-a gde bi podaci forme trebalo da budu poslati?
#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--
Koji dva elementa se koriste za određivanje reda i zaglavlja reda u tabeli?
#### --distractors--
Element ``r`` se koristi za redove, a element ``thead`` se koristi za zaglavlje.

---

Element ``row`` se koristi za redove, a element ``th`` se koristi za zaglavlje.

---

Element ``tr`` se koristi za redove, a element ``head`` se koristi za zaglavlje.
#### --answer--
Element ``tr`` se koristi za redove, a element ``th`` se koristi za zaglavlje.
### --question--

#### --text--
Za šta se koristi element `td`?
#### --distractors--
Koristi se za definisanje tipa podataka tabele.

---

Koristi se za spajanje dve kolone.

---

Koristi se za spajanje dve redke.
#### --answer--
Koristi se za definisanje ćelije tabele.
### --question--

#### --text--
Koja je funkcija atributa `colspan`?
#### --distractors--
Određuje dužinu kolone u tabeli.

---

Uklanja prekid reda između teksta u koloni.

---

Koristi se za definisanje broja kolona.
#### --answer--
Definiše broj kolona koje ćelija tabele treba da pokrije.
### --question--

#### --text--
Koja je uloga atributa `placeholder`?
#### --distractors--
Koristi se za slanje podataka na server kada korisnik pošalje formular.

---

Koristi se za povezivanje elementa ``label`` sa elementom ``input``.

---

Služi za određivanje da polje za unos nije modifikovano od strane korisnika (ili: da je samo za čitanje).
#### --answer--
Koristi se za pružanje podsetnika (placeholder-a) za polje unosa.
### --question--

#### --text--
Koja je svrha elementa `thead`?
#### --distractors--
Koristi se za grupisovanje sadržaja tela u HTML tabeli.

---

Koristi se za grupisanje opisa HTML tabele.

---

Služi za grupisanje sadržaja podnožja u HTML tabeli.
#### --answer--
Koristi se za grupisanje sadržaja zaglavlja unutar HTML tabele.
### --question--

#### --text--
Koji od sledećih predstavlja primer eksplicitnog povezivanja forme između etiketama i ulaznih polja?
#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--
Za šta se koristi HTML validator?
#### --distractors--
Validator je alat koji automatski formatira vaš HTML kod.

---

Validator je alat koji primenjuje stilove na vaš HTML.

---

Validator je alat koji čini da vaš HTML kod radi brže.
#### --answer--
Validator je alat koji proverava sintaksu HTML koda kako bi osigurao da je validan.
### --question--

#### --text--
Koji element se koristi za definisanje ćelije u tabeli?
#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--
Koji od sledećih je alat koji vam omogućava da inspekcipirate i modifikujete HTML strukturu veb stranice?
#### --distractors--
Uklanjanje iz DOM-a.

---

DOM tester.

---

DOM validacija.
#### --answer--
DOM inspektor.
### --question--

#### --text--
Koji od sledećih načina je ispravan za grupisovanje povezanih polja za unos?
#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--
Koji se od sledećih koristi za određivanje da li je polje za unos samo za čitanje (read-only)?
#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--
Koji atribut određuje da je potrebno popuniti polje pre slanja forme?
#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--
Za šta se koriste DevTools?
#### --distractors--
Ovi alati su ugrađeni direktno u pretraživač i koriste se za otkrivanje bilo kakvih problema lintinga u vašem kodu.

---

Ovi alati su ugrađeni direktno u pretraživač i koriste se za automatsko formatiranje vašeg koda.

---

Ovi alati su ugrađeni direktno u pretraživač i koriste se da vam pomognu da osigurate 100% pokrivenost testovima za vaš kod.
#### --answer--
Ovi alati su ugrađeni direktno u pretraživač i koriste se za pomoć pri debagovanju, profilisanju i analizi veb stranica.
### --question--

#### --text--
Koji od sledećih načina je ispravan način za onemogućavanje unosa?
#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--
Koja od sledećih je validna vrednost za atribut `type`?
#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--
Koji je ispravan način korišćenja atributa `size`?
#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--
Koji od sledećih atributa se koriste za određivanje minimalnog i maksimalnog broja karaktera potrebnih u polju za unos?
#### --distractors--
`minlen` and `maxlen`

---

`min` and `max`

---

`minimumLen` and `maximumLen`
#### --answer--
`minlength` and `maxlength`
### --question--

#### --text--
Koji od sledećih primera dugmeta **NE** koristi ispravnu vrednost za atribut ``type``?
#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--
Koji od sledećih atributa se koristi za određivanje vrednosti za dugme?
#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--
Koja je uloga atributa `action` unutar otvarajućeg taga `form`?
#### --distractors--
Koristi se da bi se specificiralo da je polje za unos samo za čitanje (read-only).

---

Koristi se za definisanje broja karaktera koji bi trebalo da bude vidljiv dok korisnik unosi tekst u polje (ili: u unos).

---

Koristi se za prikazivanje podsetnika korisniku, kako bi mu pokazao šta treba da unese u polje za unos.
#### --answer--
Koristi se za navođenje URL adrese na koju se podaci forme šalju.
### --question--

#### --text--
Koji od sledećih atributa se koristi za određivanje HTTP metode koju treba koristiti prilikom slanja podataka forme?
#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--
Koji od sledećih je uobičajena metoda korišćenja kada radite sa formularima?
#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--
Koji od sledećih nije važeća vrednost za atribut ``type``?
#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--
Koja je ispravna metoda za kreiranje dugmeta u formularu?
#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--
Šta je implicitna asocijacija forme?
#### --distractors--
Ovde se ulazi mogu povezati sa labelama korišćenjem atributa `action` i `for` na elementu `label`.

---

Ovo je mesto gde se ulazi mogu povezati sa labelama korišćenjem atributa ``required`` i ``for`` na elementu ``label``.

---

Ovde se ulazi mogu povezati sa labelama korišćenjem atributa ``for`` na elementu ``label``.
#### --answer--
Ovde se polja unosa mogu povezati sa labelama tako što će se polje za unos obaviti elementom `label`.
### --question--

#### --text--
Koja je uloga elementa `fieldset`?
#### --distractors--
Koristi se za grupisanje sadržaja zaglavlja u HTML tabeli.

---

Koristi se za kreiranje etikete za polje unosa.

---

Koristi se za dodavanje opisa koji opisuje grupu ulaznih polja.
#### --answer--
Služi za grupisanje srodnih polja unosa.
### --question--

#### --text--
Koji od sledećih se koristi za kreiranje čekboksa u obrascu?
#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--
Koji od sledećih elemenata se koristi za dodavanje naslova za HTML tabelu?
#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--
Koja je uloga elementa `legend`?
#### --distractors--
Koristi se za grupisanje sadržaja tela u HTML tabeli.

---

Koristi se za dodavanje natpisa koji opisuje ćelije u tabeli.

---

Služi za grupisanje povezanih ulaza.
#### --answer--
Koristi se za dodavanje podpisa koji opisuje grupu ulaznih polja.
### --question--

#### --text--
Koji od sledećih elemenata se koristi za grupisovanje sadržaja zaglavlja u HTML tabeli?
#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--
Koji od sledećih načina je ispravan za dodavanje redova i ćelija podataka u tabelu?
#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--
Šta je DOM inspektor?
#### --distractors--
Alat koji se koristi za uklanjanje svih elemenata iz DOM-a.

---

Set alata ugrađen direktno u pregledač koji vam pomaže da debagujete, profilišete i analizirate veb stranice.

---

Alat koji proverava sintaksu HTML koda kako bi osigurao njegovu validnost.
#### --answer--
Alat koji vam omogućava pregled i modifikaciju HTML strukture veb stranice.
### --question--

#### --text--
Koji od sledećih alata proverava sintaksu HTML koda kako bi osigurao njegovu valjanost?
#### --distractors--
Validacija HTML-a

---

HTML isValid

---

Važeći HTML
#### --answer--
Validator HTML-a
### --question--

#### --text--
Koji od sledećih je ispravan način za određivanje broja kolona za ćeliju podataka u tabeli?
#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--
Šta je stanje fokusa za polje za unos?
#### --distractors--
Ovo je stanje polja za unos kada korisnik ukloni fokus.

---

Ovo je stanje polja za unos kada ga korisnik postavi na neizmenjivo (read-only).

---

Ovo je stanje polja za unos kada ga korisnik onemogući.
#### --answer--
Ovo je stanje polja za unos kada ga korisnik selektuje.
### --question--

#### --text--
Koji od sledećih načina je ispravan za kreiranje radijusnog dugmeta?
#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--
Koja je uloga atributa `for`?
#### --distractors--
Koristi se za prikazivanje uputstva korisniku.

---

Koristi se za određivanje vrednosti unosa

---

Koristi se za kreiranje oznake za polje unosa.
#### --answer--
Koristi se za određivanje polja za unos kojem pripada ova oznaka.
### --question--

#### --text--
Koji od sledećih atributa se koristi za definisanje broja karaktera koji bi trebalo da budu vidljivi dok korisnik unosi tekst u polje za unos?
#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--
Koji od sledećih atributa se koristi za prikazivanje podsetnika korisniku, pokazujući mu šta treba da unese u polje za unos?
#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
