---
id: 67f39b91583dec1e1eddbb9e
title: Pregled petlji i sekvenci
challengeType: 31
dashedName: review-loops-and-sequences
---

# --description--
```markdown
## Liste u Pythonu
```

- **Uvod**: U Pythonu, tip podataka lista je uređena sekvenca elemenata koja može sastojati se od stringova, brojeva ili čak drugih lista. Liste su mutabilne i indeksirane od nule.

__CODE_BLOCK_0__

- **Pristup elementima u listi**: Da biste pristupili elementu iz `cities` liste, možete se osvrnuti na njegov indeksni broj u sekvenci:

__CODE_BLOCK_1__

- **Pristup elementima korišćenjem negativnog indeksiranja**: Da biste pristupili poslednjem elementu bilo koje liste, možete koristiti ``-1`` kao broj indeksa:

__CODE_BLOCK_2__

- Negativno indeksiranje se koristi za pristup elementima počevši od kraja liste, umesto od početka na indeksu `0`.

- **Kreiranje lista korišćenjem `list()` konstruktora**: Liste se mogu kreirati i pomoću `list()` konstruktora. Konstruktor `list()` se koristi za konverziju iterabilnog objekta u listu:

__CODE_BLOCK_3__

- **Određivanje dužine liste**: Možete koristiti funkciju `len()` da dobijete dužinu liste:

__CODE_BLOCK_4__

- **Mutabilnost lista**: Liste su mutabilne, što znači da možete ažurirati bilo koji element u listi, pod uslovom da navedete validan indeksni broj. Da biste ažurirali listu na određenom indeksu, možete joj dodeliti novu vrednost za taj indeks:

__CODE_BLOCK_5__

- **Greška izvan opsega indeksa**: Ako prosledite indeks (bilo pozitivan ili negativan) koji je van granica za listu, onda ćete dobiti `IndexError`:

__CODE_BLOCK_6__

- **Uklanjanje elemenata iz liste**: Elementi se mogu ukloniti iz liste koristeći ključnu reč ``del``:

__CODE_BLOCK_7__

- **Provera da li Elemenat Postoji u Listi**: Ključna reč `in` se može koristiti za proveru da li element postoji u listi:

__CODE_BLOCK_8__

- **Ugnježđivanje lista**: Liste se mogu ugnježđivati unutar drugih lista:

__CODE_BLOCK_9__

- Da biste pristupili ugnježdenoj listi, moraćete da ga pristupite koristeći indeks ``2`` jer su liste indeksoovane od nule (zero-based indexed).

__CODE_BLOCK_10__

- Da biste dalje pristupili drugom jeziku iz te ugnježdene liste, moraćete da ga pristupite koristeći indeks `1`:

__CODE_BLOCK_11__

- **Razpakovanje vrednosti iz liste:** Razpakovanje vrednosti iz liste je tehnika koja se koristi za dodelu vrednosti iz liste novim varijablama. Evo primera kako razpakovati listu `developer` u nove varijable nazvane `name`, `age` i `job` na ovaj način:

__CODE_BLOCK_12__

- Ako broj promenljivih na levoj strani operatera dodeljivanja ne odgovara ukupnom broju stavki u listi, dobićete ``ValueError``.

- **Sakupljanje preostalih elemenata iz liste**: Da biste sakupli bilo koje preostale elemente iz liste, možete koristiti operater asteriska (`*`) na ovaj način:

__CODE_BLOCK_13__

- **Sekcionisanje lista**: Sekcionisanje je koncept pristupa delu liste korišćenjem operatera za sekcionisanje ``:``. Da biste sekcionisali listu koja počinje na indeksu ``1`` i završava se pre indeksa ``3``, možete koristiti sledeću sintaksu:

__CODE_BLOCK_14__

- **Intervali koraka**: Takođe je moguće specificirati interval koraka koji određuje koliko treba povećati između indeksa. Evo primera ako želite da izvučete listu samo parnih brojeva korišćenjem *slicing*-a:

__CODE_BLOCK_15__

## Metode liste

- **append()**: Koristi se za dodavanje elementa na kraj liste. Evo primera korišćenja metode `append()` za dodavanje broja `6` u ovu listu `numbers`:

__CODE_BLOCK_16__

- **Dodavanje lista**: Metoda `append()` se takođe može koristiti za dodavanje jedne liste na kraj druge:

__CODE_BLOCK_17__

- **extend()**: Koristi se za dodavanje više elemenata na kraj liste. Evo primera dodavanja brojeva `6`, `8`, i `10` na kraj liste:

__CODE_BLOCK_18__

- **insert()**: Koristi se za umetanje stavke na određeni indeks u listu. Evo primera korišćenja metode `insert()`:

__CODE_BLOCK_19__

- **remove():** Koristi se za uklanjanje elementa iz liste. Metoda ``remove()`` će ukloniti samo prvi pojavljivanje elementa u listi:

__CODE_BLOCK_20__

- **pop()**: Koristi se za uklanjanje specifičnog elementa sa liste i vraćanje tog elementa:

__CODE_BLOCK_21__

- Ako ne navedete element za ``pop`` metod, onda se uklanja poslednji element.

__CODE_BLOCK_22__

- **clear()**: Koristi se za uklanjanje svih stavki sa liste:

__CODE_BLOCK_23__

- **sort()**: Metoda ``sort()`` se koristi za sortiranje elemenata na mestu. Evo primera sortiranja nasumične liste ``numbers`` na mestu:

__CODE_BLOCK_24__

- **sorted()**: Ugrađena funkcija koja vraća novu sortiranu listu umesto modifikovanja originalne liste:

__CODE_BLOCK_25__

- **reverse()**: Koristi se za obrnuto poređanje elemenata u listi:

__CODE_BLOCK_26__

- **index()**: Služi za pronalaženje prvog indeksa gde se element može pronaći u listi:

__CODE_BLOCK_27__

- Ako se element ne može pronaći korišćenjem ``index()`` metode, rezultat će biti ``ValueError``.

## Tuple-i u Pythonu

- **Definicija**: Tuple je Python tip podataka koji se koristi za kreiranje usmerene sekvence vrednosti. Tuple mogu sadržati mešoviti skup tipova podataka:

__CODE_BLOCK_28__

- Tupli su imutabilni, što znači da elementi u tuplu ne mogu biti promenjeni nakon kreiranja. Ako pokušate da ažurirate jedan od stavki u tuplu, dobićete:

__CODE_BLOCK_29__

- **Pristup elementima iz tuple**: Da biste pristupili elementu iz tuple, koristite notaciju sa uglastim zagradama i broj indeksa:

__CODE_BLOCK_30__

- Negativno indeksiranje se može koristiti za pristup elementima počevši od kraja tupola:

__CODE_BLOCK_31__

- Ako pokušate da prosledite indeksni broj koji premašuje ili je jednak dužini tupola, onda ćete dobiti `IndexError`:

__CODE_BLOCK_32__

- Tuple se takođe može kreirati korišćenjem konstruktora ``tuple()``. Unutar konstruktora, možete proslediti različite iterabilne strukture kao što su stringovi, liste i čak druge tuple.

__CODE_BLOCK_33__

- **Provera elemenata u tuple-u**: Da biste proverili da li je element u tuple-u, možete koristiti ključnu reč `in` na ovaj način:

__CODE_BLOCK_34__

- **Razmotavanje Tuple-a**: Elementi se mogu razmotati iz tuple-a na ovaj način:

__CODE_BLOCK_35__

- Ako trebate prikupiti sve preostale elemente iz tupaola, možete koristiti operater asterisk (`*`) na ovaj način:

__CODE_BLOCK_36__

- **Sekcionisanje Tuple-a**: Sekcionisanje se može koristiti za izvlačenje dela tuple-a. Na primer, stavke `pie` i `cookies` mogu se sekcionisati u zaseban tuple:

__CODE_BLOCK_37__

- **Uklanjanje elemenata iz tupola**: Uklanjanje elementa iz tupola će izazvati ``TypeError`` jer su tupi nepromenljivi (immutable):

__CODE_BLOCK_38__

- **Kada koristiti Tuple naspram Liste?**: Ako vam je potrebna dinamička kolekcija elemenata gde možete dodavati, uklanjati i ažurirati elemente, onda biste trebali koristiti listu. Ako znate da radite sa fiksnom i nemenjom (immutable) kolekcijom podataka, onda bi trebalo koristiti tuple.

## Uobičajene metode tupela

- **`count()`**: Koristi se za određivanje koliko puta se element pojavljuje u tupelu. Na primer, možete proveriti koliko puta je jezik `'Rust'` prisutan u tupelu:

__CODE_BLOCK_39__

- Ako navedeni element u metodi ``count()`` apsolutno nije prisutan u tuplu, onda će vrednost povratka biti ``0``:

__CODE_BLOCK_40__

- Ako se ne proslede argumenti metodi `count()`, onda će Python vratiti `TypeError`.

- **index()**: Koristi se za pronalaženje indeksa gde je određeni element prisutan u tuplu. Evo primera korišćenja metode ``index()`` za pronalaženje indeksa za jezik ``'Java'``:

__CODE_BLOCK_41__

- Ako navedeni stav ne može biti pronađen, Python će vratiti ``ValueError``.

- Možete proslediti opcioni početni indeks metodi ``index()`` kako biste specificirali gde početi pretraživanje stavke u tupelu:

__CODE_BLOCK_42__

- Takođe možete proslediti opciona krajnjeg indeksa metodi ``index()`` kako biste specificirali do kog mesta treba da se pretražuje stavka u tuplu:

__CODE_BLOCK_43__

- **`sorted()`**: Koristi se za sortiranje elemenata u bilo kom iterabilnom objektu i vraća novu sortiranu listu. Evo primera kreiranja nove liste brojeva korišćenjem funkcije `sorted()`:

__CODE_BLOCK_44__

- **Modifikovanje ponašanja sortiranja**: Možete prilagoditi ponašanje sortiranja za iterabilni objekat koristeći opcione argumente `reverse` i `key`. Evo primera korišćenja argumenta `key` za sortiranje elemenata u tuplu po dužini:

__CODE_BLOCK_45__

- Možete kreirati novu listu vrednosti u obrnutom redosledu, koristeći argument ``reverse``, kao što je ovde:

__CODE_BLOCK_46__

## Petlje u Python-u

- **Definicija**: Petlje se koriste za ponavljanje bloka koda određeni broj puta.

- **`for` loop**: Koristi se za iteriranje kroz sekvencu (poput liste, tupaola ili niza) i izvršavanje bloka koda za svaki element u toj sekvenci. Evo primera korišćenja `for` petlje za iteraciju kroz listu i štampanje svakog jezika na konzolu:

__CODE_BLOCK_47__

- Evo primera korišćenja `for` petlje za prolazak kroz string `code` i ispisivanje svakog znaka:

__CODE_BLOCK_48__

- `for` petlje mogu biti ugnježđene. Evo primera korišćenja ugnježdene `for` petlje:

__CODE_BLOCK_49__

- **`while` petlja**: Ponavlja blok koda dok uslov nije `False`. Evo primera korišćenja `while` petlje za igru pogađanja:

__CODE_BLOCK_50__

- **`break` i `continue` izjave**: Koriste se u petljama za modifikovanje njihovog izvršavanja.

Izjava ``break`` se koristi za trenutni izlazak iz petlje kada je zadovoljen određeni uslov. Evo primera korišćenja izraza ``break`` za listu ``developer_names``:

__CODE_BLOCK_51__

- Izjava `continue` se koristi za preskakanje trenutne iteracije i prelazak na sledeću iteraciju petlje. Evo primera kako koristiti izjavu `continue` umesto izjave `break`:

__CODE_BLOCK_52__

- Ite svi petlje ``for`` i ``while`` mogu se kombinovati sa klauzulom ``else``, koja se izvršava samo ako petlja nije prekinuta pomoću ``break``:

__CODE_BLOCK_53__

## Opsezi i njihova upotreba u petljama

- **Funkcija `range()`**: Koristi se za generisanje sekvence celih brojeva.

__CODE_BLOCK_54__

- Zahtevani argument ``stop`` je ceo broj (neuključivo) koji predstavlja krajnju tačku za niz brojeva koji se generiše. Evo primera korišćenja funkcije ``range()``:

__CODE_BLOCK_55__

- Ako nije specificiran argument ``start``, podrazumevano će biti ``0``. Podrazumevano, sekvenca celih brojeva će se inkrementirati sa ``1``. Možete koristiti opcioni argument ``step`` da promenite podrazumevan inkrementalni deo. Evo primera generisanja sekvence parnih celih brojeva od 2 do, ali ne uključujući 11 (tj. uključuje 10).

__CODE_BLOCK_56__

- Ako ne pružite nikakve argumente funkciji ``range()``, dobićete ``TypeError``.

- Funkcija `range()` prihvata samo celobrojne brojeve kao argumente, a ne floatove. Korišćenje floatova će takođe rezultirati `TypeError`:

__CODE_BLOCK_57__

- Možete koristiti negativan ceo broj za argument ``step`` da generišete sekvencu celih brojeva u opadajućem redosledu:

__CODE_BLOCK_58__

Funkcija `range()` se takođe može koristiti za kreiranje liste celih brojeva korišćenjem je sa konstruktorom `list`. Konstruktor `list` koristi se za pretvaranje iterabilnog objekta u listu. Evo primera generisanja liste parnih celih brojeva između 2 i 10, uključujući te krajeve:

__CODE_BLOCK_59__

## `enumerate()` i `zip()` funkcije u Python-u

- **`enumerate()`**: koristi se za iteraciju kroz sekvencu i praćenje indeksa za svaki element u toj sekvenci. Funkcija `enumerate()` uzima iterabilni objekat kao argument i vraća objekat `enumerate`, koji se sastoji od indeksa i vrednosti svakog elementa u iterabilnom objektu.

__CODE_BLOCK_60__

- Funkciju ``enumerate()`` se takođe može koristiti izvan petlje ``for``:

__CODE_BLOCK_61__

- Funkcija ``enumerate()`` takođe prima opcioni parametar ``start`` koji određuje početnu vrednost za brojanje. Ako je ovaj parametar izostavljen, brojanje će početi na ``0``.

- **`zip()`** : Koristi se za iteraciju preko više iterabilnih objekata paralelno. Evo primera korišćenjem funkcije `zip()` za iteraciju preko `developers` i `ids`:

__CODE_BLOCK_62__

## Komprehenzije lista u Python-u

- **Definicija**: List comprehension vam omogućava da kreirate novu listu na jednoj liniji kombinujući petlju i uslov direktno unutar uglastih zagrada. To čini kod kraćim i često lakšim za čitanje.

__CODE_BLOCK_63__

## Iterabilne metode

- **`filter()`**: Koristi se za filtriranje elemenata iz iterabilnog objekta na osnovu određenog uslova. Vraća iterator koji sadrži samo elemente koji zadovoljavaju navedeni uslov. Evo primera kreiranja nove liste koja sadrži samo reči dužih od četiri karaktera:

__CODE_BLOCK_64__

- **`map()`**: Koristi se za primenu funkcije na svaki element u iterabilnom objektu i vraćanje novog iterabilnog objekta sa rezultatima. Evo primera korišćenja funkcije `map()` za konverziju liste celzijus temperatura u Fahrenheit:

__CODE_BLOCK_65__

- **`sum()`**: Koristi se za dobijanje zbirne vrednosti iz iterabilnog objekta, poput liste ili tupela. Evo primera korišćenja funkcije `sum()`:

__CODE_BLOCK_66__

- Takođe možete proslediti opcioni argument ``start`` koji postavlja početnu vrednost za zbir. Evo ažuriranog primera koji koristi argument ``start`` kao pozicionalni argument:

__CODE_BLOCK_67__

- Takođe možete izabrati da koristite argument ``start`` kao ključni argument umesto toga, na ovaj način:

__CODE_BLOCK_68__

## Lambda funkcije

- **Definicija**: Lambda funkcija u Pythonu je koncizan način za kreiranje funkcije bez imena (anonimna funkcija). 
- Lambda funkcije se često koriste kao argument za drugu funkciju. Evo primera lambda funkcije:

__CODE_BLOCK_69__

- Najbolje prakse za korišćenje lambda funkcija uključuju da ih ne dodeljujete promenljivoj, održavate ih jednostavnim i čitljivim, te koristite za kratke, jednokratne funkcije.
# --assignment--
Pregledajte teme i koncepte Petlji i Sekvenci.