---
id: 66f1ad049d7a6ac0886cc2ba
title: React Forms, Data Fetching and Routing Quiz
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koji od sledećih primera predstavlja kontrolisani unos?
#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--
Koji od sledećih predstavlja primer nekontrolisanog input-a?
#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--
Koji od sledećih načina NIJE uobičajen za dohvaćanje podataka u React-u?
#### --distractors--
SWR

---

Axios

---

Fetch API
#### --answer--
Redux
### --question--

#### --text--
Koji od sledećih hook-ova je najčešće povezan sa dohvaćanjem podataka u React-u?
#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--
Šta je memoizacija?
#### --distractors--
Uobičajeni algoritam koji se koristi za pronalaženje najkraćeg puta između dva vrha u grafu.

---

Specijalna tehnika koja se koristi za optimizaciju performansi svih kontrolisanih i nekontrolisanih ulaza.

---

Algoritam razdeliti i osvajati koji se koristi za rekursivno sortiranje podnizova i spajanje tih podnizova.
#### --answer--
Optimizaciona tehnika u kojoj se rezultati skupih poziva funkcija keširaju (pamte) na osnovu specifičnih argumenata.
### --question--

#### --text--
Koji od sledećih hook-ova se koristi za memorizovanje izračunatih vrednosti?
#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--
Koji od sledećih koristi ispravan hook za memorizovanje referenci funkcija?
#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
``` 

---

```js
useState(() => {
 // code goes here
}, [dependency]);
``` 

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
``` 

### --question--

#### --text--
Šta je zavisnost?
#### --distractors--
A dependency je uobičajena biblioteka za testiranje koja se koristi u React aplikacijama.

---

Zavisnost je tip React komponente koja radi samo na serveru.

---

Dependency je specijalni kompajler koji se koristi u React aplikacijama za kompajliranje React koda u JavaScript.
#### --answer--
Zavisnost je situacija u kojoj jedna komponenta ili modul u aplikaciji zavisi od druge kako bi pravilno funkcionisala.
### --question--

#### --text--
Šta je paketni menadžer?
#### --distractors--
Alat koji se koristi za ažuriranje paketa trećih strana instaliranih u aplikacijama.

---

Alat koji se koristi za upravljanje paketima stvorenima isključivo unutar projekta.

---

Alat koji se koristi za premeštanje zavisnosti iz foldera `node_modules` i u poseban config folder.
#### --answer--
Alat koji se koristi za instalaciju, ažuriranje i uklanjanje zavisnosti.
### --question--

#### --text--
Koji od sledećih nije uobičajeno korišćen menadžer paketa u JavaScript ekosistemu?
#### --distractors--
pnpm

---

Yarn

---

npm
#### --answer--
Jest
### --question--

#### --text--
Koja je ključna konfiguraciona datoteka koja sadrži metapodatke o vašem projektu?
#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--
Koji od sledećih je uobičajeno korišćen React framework?
#### --distractors--
Django

---

Webpack

---

Gulp
#### --answer--
Remix
### --question--

#### --text--
Koji od sledećih načina je ispravan za dodavanje ruta u vašu React aplikaciju koristeći React Router?
#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--
Koji je ispravan način za kreiranje dinamičke rute u React Router-u?
#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--
Koji od sledećih hook-ova se koristi za pristup dinamičkim parametrima iz putanje URL-a?
#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--
Koji od sledećih je ispravan način za kreiranje prilagođenog request handler-a u Next.js?
#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--
Šta je prop drilling?
#### --distractors--
Proces kreiranja reducer-a koji definiše kako bi se stanje trebalo promeniti u zavisnosti od korišćenih specifičnih `props`.

---

Proces prenošenja props-ova iz detetkomponente ka roditeljskoj komponenti.

---

Proces kreiranja context provajdera za React aplikaciju i korišćenjem ga za slanje props-ova od roditeljskih ka detilji komponentama.
#### --answer--
Proces prosleđivanja propova od roditeljske komponente ka duboko ugnježđenim detilnim komponentama, čak i kada neke od tih detilnih komponenti ne zahtevaju te propove.
### --question--

#### --text--
Koje od sledećih NIJE uobičajeno korišćeno rešenje za upravljanje stanjem (state management) za React aplikacije?
#### --distractors--
Context API

---

Stanje

---

Redux
#### --answer--
Spring
### --question--

#### --text--
Za šta se koriste React Developer Tools?
#### --distractors--
Ovi alati se koriste samo za proveru problema pristupačnosti u React aplikacijama.

---

Ovi alati se koriste za dodavanje dodatnih testova za komponente React-a.

---

Ovi alati se koriste za memoizovanje svih React komponenti unutar aplikacije.
#### --answer--
Ovi alati se koriste za inspekciju komponenti React-a i identifikovanje problema sa performansama.
### --question--

#### --text--
Šta su React Server Components?
#### --distractors--
Ovo su komponente koje upravljaju kontekstom isključivo na serveru.

---

Ovo su memoizovane komponente koje se renderuju isključivo na serveru.

---

Ovo su komponente koje upravljaju stanjem isključivo na serveru.
#### --answer--
Ovo su komponente koje se renderuju isključivo na serveru.

