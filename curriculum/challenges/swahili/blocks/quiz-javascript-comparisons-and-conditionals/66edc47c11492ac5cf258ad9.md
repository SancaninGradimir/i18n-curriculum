---
id: 66edc47c11492ac5cf258ad9
title: Kviz o poređenjima i uslovima u JavaScriptu
challengeType: 8
dashedName: quiz-javascript-comparisons-and-conditionals
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 9 od 10 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koji je rezultat izraza `undefined > 0`?
#### --distractors--

`true`

---

`undefined`

---

`null`

#### --answer--

`false`

### --question--

#### --text--
Koja logička operacija predstavlja `||`?
#### --distractors--
XOR

---

I

---

NE
#### --answer--
OR
### --question--

#### --text--
Koji je izlaz sledećeg koda?

```js
console.log(5 === 2 + 3 || 4 == 2);
```

#### --distractors--

`undefined`

---

`false`

---

Izbačena je greška.
#### --answer--

`true`

### --question--

#### --text--
Šta su 'truthy' i 'falsy' vrednosti?
#### --distractors--
Vrednost koja je istovremeno i `true` i `false`.

---

Vrednost koja se menja u zavisnosti od konteksta.

---

Vrednost koja funkcioniše drugačije od `true` ili vrednosti `false`.
#### --answer--
Vrednost koja se tretira kao `true` ili `false` kada se susretne u boolean kontekstu.
### --question--

#### --text--
Šta je logovano u konzolu iz koda ispod?

```js
if (1) {
  console.log("True!");
}
```

#### --distractors--

`false`

---

Ništa se ne ispisuje na konzolu.

---

Došlo je do greške.
#### --answer--

`True!`

### --question--

#### --text--
Koja je razlika između `undefined` i `null`?
#### --distractors--
`null` i `undefined` pokazuju na memorijsku lokaciju izvan raspona i nisu dostupni.

---

``null` predstavlja implicitnu vrednost dodeljenu promenljivama i ne može se promeniti, dok je `undefined` eksplicitna vrednost dodeljena promenljivama.`

---

``null` je globalno svojstvo, `undefined` nije.`
#### --answer--
Varijable bez vrednosti su `undefined`, dok `null` predstavlja namernu odsutnost objektnog vrednosti.
### --question--

#### --text--
Šta se dešava kada ne uključite `break` prilikom implementacije naredbe `switch`?
#### --distractors--
Izjava `switch` će prestati naglo.

---

Izjava ``switch`` će izazvati grešku nakon prethodne izjave.

---

Izjava ``switch`` će se zaustaviti nakon prvog podudarnosti.
#### --answer--
Kod nastavlja da evaluira sledeće `case` izjave, čak i nakon pronalaženja podudarnosti.
### --question--

#### --text--
Šta je ispisano na konzoli iz koda ispod?

```js
const a = 2;
if (1 == "1") {
  let b = 3;
  console.log(a + b);
}
console.log(b);
```

#### --distractors--
Došlo je do greške.

---

```js
5
3
```

---

```js
5
undefined
```

#### --answer--
``5``, nakon čega se baca greška.
### --question--

#### --text--
Šta će biti izlaz sledećeg JavaScript koda?

```js
let vehicle = "car";

switch (vehicle) {
  case "bike":
    console.log("Bikes are two-wheelers.");
    break;
  case "car":
    console.log("Some cars are 4x4.");
  case "truck":
    console.log("Trucks can carry heavy loads.");
    break;
  default:
    console.log("Unknown vehicle.");
}
```

#### --distractors--

```md
Some cars are 4x4.
```

---

```md
Some cars are 4x4.
Trucks can carry heavy loads.
Unknown vehicle.
```

---

```md
Unknown vehicle.
```

#### --answer--

```md
Some cars are 4x4.
Trucks can carry heavy loads.
```

### --question--

#### --text--
Šta se ispisuje u konzolu sa kodom ispod?

```js
let x = 5;
if (x > 1 && x < 10) {
  console.log("x is between 1 and 10");
} else {
  console.log("x is not between 1 and 10");
}
```

#### --distractors--
Došlo je do greške.

---

Ništa se ne štampa.

---

`x is not between 1 and 10`

#### --answer--

`x is between 1 and 10`

