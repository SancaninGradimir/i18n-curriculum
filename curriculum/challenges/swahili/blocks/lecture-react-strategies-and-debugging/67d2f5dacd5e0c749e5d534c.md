---
id: 67d2f5dacd5e0c749e5d534c
title: Kako možete da debagujete svoje React komponente koristeći React DevTools?
challengeType: 19
dashedName: how-can-you-debug-your-react-components-using-react-devtools
---

# --description--

Pregledač ima ugrađene alate za programere koje možete koristiti za debagovanje HTML-a, CSS-a i JavaScript-a.

Međutim, nisu odlični za pronalaženje i popravljanje grešaka u React aplikacijama. Zato je tim React razvio alat nazvan „React Developer Tools“ (poznati i kao React DevTools) kako biste mogli inspekcionisati, debagovati i profilisati React aplikacije.

React DevTools je dostupan kao ekstenzija za pregledač za Chrome, Edge i Firefox. Ako koristite Chrome ili Edge, idite na Chrome Web Store, potražite „React Developer Tools“ i dodajte ga u svoj pregledač.

A ako koristite Firefox, idite na stranicu za dodatke (Add-ons) za Firefox, potražite alat i dodajte ga u svoj pregledač.

Ako koristite Safari, možete instalirati React DevTools iz npm pokretanjem komande `npm install -g react-devtools` ili `yarn global add react-devtools`.

Nakon instaliranja i omogućavanja React DevTools, ako otvorite React aplikaciju u pregledaču, pa zatim otvorite alate za programere vašeg pregledača, trebalo bi da vidite dve dodatne kartice: Components (Komponente) i Profiler (Profilisanje).

Kartica Komponente prikazuje svaku komponentu u formatu hijerarhije (tree view). Njime možete:

- Pregledati hijerarhiju komponenti aplikacije.
- Proveriti i modifikovati prope, stanja (states) i vrednosti konteksta u realnom vremenu.
- Pregledati izvorni kod za svaku odabranu komponentu.
- Logovati podatke komponente u konzolu.
- Inspekcionisati DOM elemente za komponentu.

S druge strane, kartica Profiler pomaže vam da snimite i analizirate performanse komponenti kako biste mogli identifikovati nepotrebne ponovne renderovanja (re-renders), videti trajanje commita i stvari koje možete optimizovati.

Evo jednostavne aplikacije koja vam pokazuje kako možete inspekcionisati komponente i sve prope i stanja koje imaju. Ovo je slično kodu koji smo koristili u prethodnoj lekciji o „prop drilling“-u (dubokošu propova):

```jsx
import { useState } from "react";

export default function App() {
  const greeting = "Hello, Prop Drilling!";
  const response = "I'm not here to play!";

  return <Parent greeting={greeting} response={response} />;
}

const Parent = ({ greeting, response }) => {
  return <Child greeting={greeting} response={response} />;
};

const Child = ({ greeting, response }) => {
  return <Grandchild greeting={greeting} response={response} />;
};

const Grandchild = ({ greeting, response }) => {
  const [count, setCount] = useState(0);

  return (
    <>
      <h1>{greeting}</h1>
      <h2>{response}</h2>

      <button onClick={() => setCount(count + 1)}>Increase Count</button>
      <h2>Count: {count}</h2>
      <button onClick={() => setCount(count - 1)}>Decrease Count</button>
    </>
  );
};
```

Ako pogledate u kartici Komponente u React DevTools, možete videti hijerarhiju komponenti (tree view). Komponenta `App` je na vrhu, praćena komponentama `Parent`, `Child` i `Grandchild`.

Ako izaberete bilo koju od ovih komponenti, možete videti prope i stanja u njima. Ako izaberete komponentu `Parent`, možete videti prope `greeting` i `
# --questions--

## --text--

Kako možete instalirati React DevTools na Chrome-u ili Edge-u?
## --answers--

Preuzmite ga sa zvaničnog React veb-sajta.
### --feedback--

Možete ga pronaći na istom mestu kao i druge ekstenzije za pretraživače.
---

Install it via `npm` in your project.

### --feedback--

Možete ga pronaći na istom mestu kao i druge ekstenzije za pretraživače.
---

Add it as a browser extension.

---

It comes pre-installed with React.

### --feedback--

Možete ga pronaći na istom mestu kao i druge ekstenzije za pretraživače.
## --video-solution--

3

## --text--

Nakon instaliranja React DevTools-a, koje dve dodatne kartice se pojavljuju u alatima za razvoj pretraživača za debagovanje React-a?
## --answers--

Komponente i Profilator.
---

Network and Performance.

### --feedback--

Jedna kartica pomaže u inspekciji stabla komponenti, dok druga analizira performanse.
---

Elements and Console.

### --feedback--

Jedna kartica pomaže u inspekciji stabla komponenti, dok druga analizira performanse.
---

Sources and Memory.

### --feedback--

Jedna kartica pomaže u inspekciji stabla komponenti, dok druga analizira performanse.
## --video-solution--

1

## --text--

Šta kartica Profiler u React DevTools pomaže da analizirate?
## --answers--

Mrežni zahtevi u React aplikaciji.
### --feedback--

Pomaže u praćenju vremena renderovanja i optimizaciji sporih komponenti.
---

JavaScript memory leaks.

### --feedback--

Pomaže u praćenju vremena renderovanja i optimizaciji sporih komponenti.
---

CSS styling issues.

### --feedback--

Pomaže u praćenju vremena renderovanja i optimizaciji sporih komponenti.
---

Component performance and re-renders.
 
## --video-solution--

4
