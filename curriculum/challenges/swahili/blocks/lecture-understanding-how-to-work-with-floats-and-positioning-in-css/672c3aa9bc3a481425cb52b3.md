---
id: 672c3aa9bc3a481425cb52b3
title: Šta je svojstvo Z-Indeks, i kako radi da kontroliše složanje pozicioniranih elemenata?
challengeType: 19
dashedName: what-is-the-z-index-property
---

# --interactive--

Svojstvo ``z-index`` u CSS-u koristi se za kontrolisanje vertikalnog redosleda složene pozicioniranih elemenata koji se preklapaju na stranici. Kada su više elemenata složena jedan preko drugog, vrednost ``z-index`` određuje koji se element pojavljuje na vrhu. Što je vrednost ``z-index`` veća, to je element bliži gledaocu, dok niže vrednosti postavljaju element dalje u sloju.

Međutim, `z-index` radi samo na elementima koji su pozicionirani, što znači da element mora imati vrednost pozicije drugačija od `static`, kao što su `relative`, `absolute`, ili `fixed`. Podrazumevana vrednost `z-index` je `auto`, što postavlja element u podrazumevani red složene strukture. Pogledajmo primer sa tri kutije:

```html
<div class="container">
  <div class="box1">Box 1</div>
  <div class="box2">Box 2</div>
  <div class="box3">Box 3</div>
</div>
```

Sada, možemo primeniti neke stilove da pozicioniramo kutije tako da se preklapaju jedna na drugu ovako:

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<div class="container">
  <div class="box1">Box 1</div>
  <div class="box2">Box 2</div>
  <div class="box3">Box 3</div>
</div>
```

```css
.container {
  position: relative;
  width: 300px;
  height: 300px;
  border: 1px solid black;
}

.box1 {
  position: absolute;
  z-index: 1;
  background: lightcoral;
  top: 20px;
  left: 20px;
  width: 100px;
  height: 100px;
}

.box2 {
  position: absolute;
  z-index: 3;
  background: gold;
  top: 40px;
  left: 40px;
  width: 100px;
  height: 100px;
}

.box3 {
  position: absolute;
  z-index: 2;
  background: lightgreen;
  top: 60px;
  left: 60px;
  width: 100px;
  height: 100px;
}
```

:::

Za kontejner, pozicioniranje će biti postavljeno na `relative` a sve kutije ugnježđene unutra će biti postavljene na `absolute` pozicioniranje. Svaka kutija ima različitu vrednost za `z-index`, što rezultira time da su kutije slojevane jedna preko druge.

Možete smatrati da je `z-index` način za kreiranje slojeva na web stranici, a elementi sa višim `z-index` vrednostima će biti postavljeni iznad elemenata sa nižim vrednostima. Ovo je posebno korisno za kontrolisanje kako se preklapajući elementi ponašaju u kompleksnim rasporedima, kao što su modali, pop-api ili alati (tooltipovi).
# --questions--

## --text--

Šta svojstvo `z-index` kontroliše u CSS-u?
## --answers--

Boja elementa.
### --feedback--

`z-index` je odgovoran za to kako se elementi vizuelno preklapaju.
---

The vertical stacking order of positioned elements.

---

The size of an element.

### --feedback--

`z-index` je odgovoran za to kako se elementi vizuelno preklapaju.
---

The horizontal alignment of text.

### --feedback--

`z-index` je odgovoran za to kako se elementi vizuelno preklapaju.
## --video-solution--

2

## --text--

Da bi svojstvo ``z-index`` radilo, šta mora biti tačno o elementu?
## --answers--

Trebalo bi da ima širinu `fixed`.
### --feedback--

Razmislite šta omogućava elementu da bude postavljen u kontekst stabliranja.
---

It must have a `position` value other than `static`.

---

It must have a `display` value of `block`.

### --feedback--

Razmislite šta omogućava elementu da bude postavljen u kontekst stabliranja.
---

It must have a background color set.

### --feedback--

Razmislite šta omogućava elementu da bude postavljen u kontekst stabliranja.
## --video-solution--

2

## --text--

Koja vrednost `z-index` postavlja element iznad svih ostalih?
## --answers--

`z-index: 0;`

### --feedback--

Razmislite o tome koji je broj najveći i kako `z-index` funkcioniše.
---

`z-index: 5;`

---

`z-index: auto;`

### --feedback--

Razmislite o tome koji je broj najveći i kako `z-index` funkcioniše.
---

`z-index: -1;`

### --feedback--

Razmislite o tome koji je broj najviši i kako z-index funkcioniše.
## --video-solution--

2
