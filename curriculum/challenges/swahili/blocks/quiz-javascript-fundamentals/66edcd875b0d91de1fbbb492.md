---
id: 66edcd875b0d91de1fbbb492
title: Test iz Osnova JavaScripta
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koji od ovih će kreirati objekat tipa String?
#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--
Kako možete konvertovati `String` objekat u string literalu?
#### --distractors--
Sa `String()` konstruktorem.

---

Sa metodom `.stringify()`.

---

Umotajte objekat `String` u dvostruke navodnike.
#### --answer--
Sa `.toString()` metodom.
### --question--

#### --text--
Zašto možete koristiti metode kao što su `.length`, `.repeat()`, `.concat()`, i `.slice()` na primitivni tip niza karaktera u JavaScript-u?
#### --distractors--
Ti metodi postoje nativno na primitivima tipa string.

---

Primitivni stringovi su interno skladišteni kao objekti.

---

JavaScript privremeno konvertuje primitivni string u niz.
#### --answer--
JavaScript privremeno omotava string primitiv u String objekat.
### --question--

#### --text--
Šta će sledeći kod ispisati u konzolu?

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--
Koji od sledećih vraća objekat iz niza ``"123"``?
#### --distractors--

`Object("123")`

---

`"123".toNumber()`

---

`Number("123")`

#### --answer--

`new Number("123")`

### --question--

#### --text--
Šta će `Number(true)` i `Number(false)` vratiti, odnosno?
#### --distractors--
`undefined` and `undefined`

---

`NaN` and `NaN`

---

`true` and `false`
#### --answer--
`1` and `0`
### --question--

#### --text--
Šta će `Number(undefined)` i `Number(null)` vratiti, odnosno?
#### --distractors--
`NaN` and `NaN`

---

`0` and `0`

---

`undefined` and `null`
#### --answer--
`NaN` and `0`
### --question--

#### --text--
Koji od sledećih je najbolji naziv za boolean varijablu koja proverava da li korisnik ima dozvolu da nešto radi na vašem veb-sajtu?
#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--
Koji je najbolji naziv za funkciju koja se izvršava kada korisnik pošalje obrazac (formular)?
#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--
Šta je razređeni niz?
#### --distractors--
Niz gde jedan ili više indeksa imaju vrednost `null`.

---

Niz gde jedan ili više indeksa imaju vrednost od `undefined`.

---

Niz gde jedan ili više indeksa imaju vrednost praznog objektnog literala.
#### --answer--
Niz gde jedan ili više indeksa nije dodeljen vrednost.
### --question--

#### --text--
Koji od sledećih primera će `arr` NE biti razređeni niz?
#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--
Koja je glavna svrha lintera?
#### --distractors--
Automatsko formatiranje koda.

---

Za optimizaciju koda radi boljih performansi.

---

Kreiranje dokumentacije za kod.
#### --answer--
Detekcija potencijalnih grešaka i problema u kodu.
### --question--

#### --text--
Koja je prednost korišćenja formatirača u bazi koda?
#### --distractors--
Omogućavaju programerima da kompajluju kod efikasnije.

---

Oni pretvaraju kod u određeni programski jezik.

---

Otkrivaju logičke greške.
#### --answer--
Osiguravaju konzistentan stil koda.
### --question--

#### --text--
U JavaScript-u, ko ili šta je obično odgovorno za alokaciju memorije varijablama?
#### --distractors--
Razvojnik.

---

Operativni sistem

---

Server.
#### --answer--
Pokretni motor
### --question--

#### --text--
Šta će biti ispisano u konzolu u sledećem kodu?

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--
Šta je *hoisting* u JavaScriptu?
#### --distractors--
Propagovanje grešaka do vrha koda.

---

Proces kreiranja novih varijabli.

---

Automatski se premeštaju deklaracije `var` na vrh bloka.
#### --answer--
Proces premeštanja deklaracija promenljivih na vrh njihovog opsega.
### --question--

#### --text--
Zašto se korišćenje ključne reči `var` smatra lošom praksom u modernom JavaScript-u?
#### --distractors--
Sporije su od `let` i `const`.

---

Uzrokuju sintaksne greške.

---

Ne dozvoljavaju proveru tipova.
#### --answer--
Dostupni su van svog bloka.
### --question--

#### --text--
Koja je svrha modula u JavaScriptu?
#### --distractors--
Za poboljšanje performansi koda.

---

Kako bi se kod učinio teškim za razumevanje.

---

Kreiranje novih programskih jezika.
#### --answer--
Da bi se kod organizovao u ponovo upotrebljive jedinice.
### --question--

#### --text--
Kako dodati `app.js` skriptu kao modul u HTML dokument?
#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--
Koji od sledećih primera izvodi funkciju `calculateSum` iz modula `utils.js` i pravilno ga uvozi i koristi u fajlu `app.js`?
#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
