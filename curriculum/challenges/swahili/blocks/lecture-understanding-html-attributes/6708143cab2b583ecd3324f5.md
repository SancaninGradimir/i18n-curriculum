---
id: 6708143cab2b583ecd3324f5
title: Šta su atributi i kako funkcionišu?
challengeType: 19
dashedName: what-are-attributes
---

# --interactive--

Atribut je vrednost postavljena unutar otvarajućeg taga HTML elementa. Atributi pružaju dodatne informacije o elementu ili određuju kako bi element trebalo da se ponaša. Evo osnovne sintakse za atribut:

```html
<element attribute="value"></element>
```

Ime atributa je praćeno znakom jednakosti (`=`) i vrednošću u navodnicima. Vrednost može biti niz ili broj, zavisno od atributa.

Ovaj prvi primer koristi atribute `href` i `target`. Atribut `href` određuje URL veze, a atribut `target` određuje gde da se veza otvori.

**Napomena:** Element ``a``, poznat i kao anker element, koristi se za kreiranje hiperlinkova. Tekst između otvarajućih i zatvarajućih tagova ``a`` je klikovni deo koji korisnici biraju za navigaciju.

Omogućujte interaktivni uresnik i promenite ``href="https://www.freecodecamp.org/news/"`` u ``href="https://www.freecodecamp.org"``. Sada kada kliknete na link u interaktivnom uresniku, videćete početnu stranicu freeCodeCamp-a u novoj tabovoj sesiji pretraživača.

:::interactive_editor

```html
<a href="https://www.freecodecamp.org/news/" target="_blank">Visit freeCodeCamp</a>
```

:::

Bez atributa `href`, link ne bi radio jer ne bi postojala odredišna URL adresa. Zato morate uključiti ovaj atribut `href` da biste učinili link funkcionalnim. `target="_blank"` omogućava linku da se otvori u novoj kartici pretraživača. Vi ćete saznati više o atributu `target` u budućim lekcijama.

Drugi uobičajeni atributi su `src`, i `alt`, ili alternativni atribut - koji se koristi za specifikovanje izvora slike i pružanje alternativnog deskriptivnog teksta za sliku, respektivno.

Omogućite interaktivni uresnik i promenite ``src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/cats.jpg"`` u ``src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/running-cats.jpg"``. Zatim promenite ``alt="Two tabby kittens sleeping together on a couch."`` u ``alt="Two cats running in the dirt."``.

:::interactive_editor

```html
<img src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/cats.jpg" alt="Two tabby kittens sleeping together on a couch." />
```

:::

Slično atributu `href`, atribut `src` je obavezan jer specificira fajl slike koji treba da se prikaže. Atribut `alt` nije obavezan, ali je preporučljiv radi pristupačnosti. Pristupačnost znači osiguravanje da svi, uključujući osobe sa invaliditetom, mogu koristiti i razumeti stvari kao što su veb-sajtovi, aplikacije i fizičke prostore. Vi ćete naučiti više o pristupačnosti u narednim lekcijama.

Neki atributi su malo jedinstveni sa svojom sintaksom, kao što je atribut `checked`.

Omogući interaktivni uresnik i pokušajte da kliknete na polje za izbor (checkbox) u prozoru pregleda kako biste videli kako se mijenja između stanja sačekirano i nije označeno.

:::interactive_editor

```html
<input type="checkbox" checked />
```

:::

U sledećem primeru, imamo element `input` sa atributom `type` podešenim na `checkbox`. Ulazi se koriste za prikupljanje podataka od korisnika, a atribut `type` određuje tip unosa. U ovom slučaju, ovaj unos je čekboks. Saznaćete više o tome kako ulazi funkcionišu u narednim lekcijama.

Atribut ``checked`` se koristi za određivanje da li će prokupljica biti označena podrazumevano. Atribut ``checked`` ne zahteva vrednost. Ako je prisutan, prokupljica će biti označena podrazumevano. Ako atribut nije prisutan, prokupljica neće biti označena. Ovo se naziva booleanski atribut. Naučićete više o boolijanima uopšteno kada dođete do sekcije za JavaScript.

Omogući interaktivni uresnik i pokušaj uklanjanja atributa ``checked`` iz ``input``. Videćeš da polje za čekiranje više nije označeno podrazumevano.

:::interactive_editor

```html
<input type="checkbox" checked />
```

:::

Postoje nekoliko uobičajenih booleanskih atributa koje ćete naići u HTML-u, kao što su ``disabled``, ``readonly``, i ``required``. Ovi atributi se koriste za određivanje stanja elementa, poput toga da li je onemogućen (disabled), samo za čitanje (read-only) ili obavezan (required).

Ovo je primer teksta `input` elementa koji je onemogućen po podrazumevanju. Omogućite interaktivni uresnik i pokušajte da kliknete na `input` element u prozoru za pregled. Sada uklonite atribut `disabled` sa `input` elementa i videćete da `input` više nije onemogućen po podrazumevanju. Trebalo bi sada da možete na njega kliknuti i da kucate unutar polja.

:::interactive_editor

```html
<input type="text" disabled>
```

:::

HTML ima mnogo atributa koji se mogu koristiti za prilagođavanje ponašanja i izgleda elemenata na veb stranici. Razumevanje kako da se koriste atributi je esencijalno za kreiranje interaktivnog i pristupačnog web sadržaja. Tokom sledećih nekoliko lekcija, naučićete o više HTML atributa i kako ih efikasno koristiti u vašim projektima razvoja veba.
# --questions--

## --text--

Koja od sledećih opcija je primer booleana atributa?
## --answers--

`src`

### --feedback--

Pogledajte sekciju gde su razgovarali o opštim boolean atributima.
---

`href`

### --feedback--

Pogledajte sekciju gde su razgovarali o opštim boolean atributima.
---

`disabled`

---

`alt`

### --feedback--

Pogledajte sekciju gde su razgovarali o opštim boolean atributima.
## --video-solution--

3

## --text--

Koja je uloga atributa u HTML-u?
## --answers--

Atributi pružaju dodatne informacije i pomažu u definisanju ponašanja za HTML elemente.
---

Attributes change the background color of an element.

### --feedback--

Odnesite se na odeljak gde je diskutovana definicija atributa.
---

Attributes change the font size of an element.

### --feedback--

Odnesite se na odeljak gde je diskutovana definicija atributa.
---

Attributes add JavaScript functionality to an element.

### --feedback--

Odnesite se na odeljak gde je diskutovana definicija atributa.
## --video-solution--

1

## --text--

Koja od sledećih je ispravna sintaksa za booleanski atribut?
## --answers--

`<input type="checkbox" checked>`

---

`<input type="checkbox" checked="on">`

### --feedback--

Pogledajte sekciju gde je atribut `checked` bio diskutovan.
---

`<input type="checkbox" checked="off">`

### --feedback--

Pogledajte sekciju gde je atribut `checked` bio diskutovan.
---

`<input type="checkbox" checked="isChecked">`

### --feedback--

Pogledajte sekciju gde je atribut `checked` bio diskutovan.
## --video-solution--

1
