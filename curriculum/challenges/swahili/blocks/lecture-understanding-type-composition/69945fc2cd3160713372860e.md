---
id: 69945fc2cd3160713372860e
title: Šta su tipovi aliasi i kako funkcionišu?
challengeType: 19
dashedName: what-are-type-aliases-and-how-do-they-work
---

# --description--

U prethodnoj lekciji ste naučili kako da radite sa tipovima unije. Jedan uobičajen način za rad sa tipovima unije je korišćenje aliasa tipa.

Tip alias je ime za tip. Evo primera dodeljivanja imena za unioni tip:

```ts
type ID = number | string;
```

Često u svojim projektima, kreiraćete aliase tipova i koristiti ih kroz ceo vaš program na ovaj način:

```ts
type ID = number | string;

type User = {
  id: ID;
  name: string;
};

function getUserId(user: User): ID {
  return user.id;
}
```

Ovaj primer koristi dva tipa pod imenima `ID` i `User`. `ID` bi bio unioni tip za `number` ili `string`, dok je `User` objekti tip sa svojstvima `id` i `name`. `id` je specificiran sa tipom `ID`, dok `name` ima tip `string`.

Funkcija `getUserId` ima jedan parametar nazvan `user` i njegov tip je `User`. Tip povratne vrednosti za funkciju `getUserId` bio bi `ID`.

Alias tipovi su odličan način za opisivanje oblika objekta. Uobičajeno je kreirati tipove objekata i koristiti ih u nizovima ovako:

```ts
type User = {
  id: number | string;
  name: string;
};

const users: User[] = [
  { id: 101, name: "Alice" },
  { id: "A102", name: "Bob" },
  { id: 103, name: "Charlie" }
];
```

U ovom primeru, niz `users` mora biti popunjen samo objektima `User`. Ako zaboravite svojstvo u jednom od objekata, TypeScript će baciti grešku kao što je ovo:

```ts
type User = {
  id: number | string;
  name: string;
};

const users: User[] = [
  // Property 'name' is missing in type '{ id: number; }' but required in type 'User'.
  { id: 101 },
  { id: "A102", name: "Bob" },
  { id: 103, name: "Charlie" }
];
```

Možda ste primetili korišćenje PascalCase-a kada je reč o imenovanju tipova aliasa. To je zato što konvencijom korišćenje PascalCase-a pomaže u razlikovanju tipova od promenljivih. Takođe pomaže i čitljivosti. Kada vidite `User[]`, znaćete da je to niz objekata tipa `User`.
# --questions--

## --text--

Šta je tipski alias u TypeScriptu?
## --answers--

Ugrađena ključna reč u TypeScriptu koja se koristi samo za nizove.
### --feedback--

Vratite se na početak lekcije.
---

A special class that stores multiple values.

### --feedback--

Vratite se na početak lekcije.
---

A name given to a type, such as a union or object type.

---

A function that automatically converts one type to another.

### --feedback--

Vratite se na početak lekcije.
## --video-solution--

3

## --text--

Koji je tip ``id`` unutar tipa ``User`` ovde?

```ts
type ID = number | string;

type User = {
  id: ID;
  name: string;
};
```

## --answers--

`number`

### --feedback--

Vratite se na početak lekcije.
---

`string`

### --feedback--

Vratite se na početak lekcije.
---

`number | string`

---

`null`

### --feedback--

Vratite se na početak lekcije.
## --video-solution--

3

## --text--

Zašto se PascalCase često koristi prilikom imenovanja aliasa tipova u TypeScript-u?
## --answers--

Zato što ga zahteva TypeScript.
### --feedback--

Vratite se na kraj lekcije.
---

To help distinguish types from variables and improve readability.

---

To make the code run faster.

### --feedback--

Vratite se na kraj lekcije.
---

To allow the type to be used in arrays.

### --feedback--

Vratite se na kraj lekcije.
## --video-solution--

2
