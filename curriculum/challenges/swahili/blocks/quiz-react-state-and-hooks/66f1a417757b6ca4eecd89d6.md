---
id: 66f1a417757b6ca4eecd89d6
title: Test iz Stanja i Hooks-ova u Reactu
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koji od sledećih načina je ispravan za dodavanje događaja klika na dugme u React-u?
#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--
Šta je React Synthetic Event?
#### --distractors--
Tip JSX elementa samo za događaje `inputs` i `submit`.

---

Vrsta JSX elementa samo za `submit` događaje.

---

Način izmene faze bubanja propagacije događaja kako bi bio prijatniji za korisnika.
#### --answer--
Omotnica za ugrađene događaje poput `click` i `submit`.
### --question--

#### --text--
Koji od sledećih je uobičajen prefiks koji se koristi za imenovanje funkcija za rukovanje događajima (event handler functions)?
#### --distractors--
isHandling

---

hasHandled

---

obrada
#### --answer--
upravljanje
### --question--

#### --text--
Koje su tri glavne faze u renderovanju Reacta?
#### --distractors--
Handshake, učitavanje i hidratacija.

---

Dohvatanje, dekodiranje i izvršavanje.

---

Montiranje, ažuriranje i demontiranje.
#### --answer--
Pokretanje, renderovanje i commitovanje.
### --question--

#### --text--
Koji je ispravan način za rad sa `useState` hook-om?
#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--
Koji od sledećih nije važeći atribut za obradu React događaja?
#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--
Koja je ispravna metoda za ažuriranje stanja niza?

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Front-End");
```

---

```js
setCertificates.push("Front-End");
```

---

```js
setCertificates().append("Front-End");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Front-End"]);
```

### --question--

#### --text--
Šta je pogrešno sa ovom funkcijom?

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--
Izmenjivanje varijable ``useState`` nije dozvoljeno unutar ugnježđene funkcije.

---

``previousState`` mora biti okružen zagradama.

---

Strelaste funkcije ne bi trebalo koristiti unutar funkcije za postavljanje stanja.
#### --answer--
``previousState`` mora biti prošireno pre ``name`` kako bi se osiguralo da druga svojstva ne budu prepisana.
### --question--

#### --text--
Koja je ispravna metoda za ažuriranje stanja i uklanjanje elemenata iz niza?
#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--
Šta je atribut `ref` obično korišćen u React-u?
#### --distractors--
Koristi se za proveru problema sa performansama u React komponentama.

---

Koristi se za proveru problema pristupačnosti u react komponentama.

---

Koristi se za uklanjanje DOM čvorova.
#### --answer--
Koristi se za pristup DOM čvoru.
### --question--

#### --text--
Koji je ispravan način za pristup trenutnoj vrednosti za `ref`?
#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--
Koliko puta će se ova poruka logovati u konzolu?

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--
Tri puta.

---

Dva puta.

---

None jer je niz zavisnosti prazan.
#### --answer--
Samo jednom prilikom inicijalnog renderovanja.
### --question--

#### --text--
Za šta se obično koriste React custom hooks?
#### --distractors--
Koriste se za izvršavanje najkompleksnije funkcionalnosti aplikacije.

---

Obično se ne koriste; preferiraju su ugrađeni hookovi.

---

Zamenjuju metode životnog ciklusa iz prethodnih verzija React-a.
#### --answer--
Omogućavaju ponovno korišćenje stanja logike među komponentama.
### --question--

#### --text--
Koliko puta će se ova poruka zapisati u konzolu?

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--
Logovaće se u bilo kojem trenutku kada je forma poslata ili dođe do događaja klika.

---

Logovaće se dva puta.

---

Logovaće se samo jednom.
#### --answer--
Beležiće se svaki put kada komponenta izvrši renderovanje.
### --question--

#### --text--
Koja je ispravna metoda za sprečavanje osvežavanja pregledača za događaj ``onSubmit``?
#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--


```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--
Koji od sledećih hookova se obično koristi za dohvaćanje podataka?
#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--
Koji od sledećih NIJE validno svojstvo na ugrađenom Event objektu?
#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--
Koja od sledećih faza renderovanja se dešava kada React detektuje da se nešto promenilo i korisnički interfejs (UI) možda treba da bude ažuriran?
#### --distractors--
setter

---

Commit

---

petlja
#### --answer--
okidač
### --question--

#### --text--
Koji od sledećih faza se odnosi na trenutak kada React uzima pripremljene promene iz virtual DOM-a i primenjuje ih na pravi DOM?
#### --distractors--
okidač

---

render

---

seter
#### --answer--
commit
### --question--

#### --text--
Koja je svrha `dependencies` u `useEffect`?

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--
Argument `dependencies`, koji je opcionalan, nema nikakav uticaj na nešto što se tiče `useEffect`.

---

Opcijski argument ``dependencies`` uzrokuje da se efekat izvršava u beskonačnoj petlji.

---

Opcioni argument `dependencies` koristi se za sprečavanje izvršavanja efekta.
#### --answer--
Opcijski argument `dependencies` određuje kada se efekat izvršava.

