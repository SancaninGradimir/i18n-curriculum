---
id: 66f1ae758b77cfc3e4da6151
title: Quiz o TypeScriptu
challengeType: 8
dashedName: quiz-typescript
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Šta je TypeScript?
#### --distractors--
Okvir koji se koristi za sistemsko programiranje.

---

Alat za administraciju baza podataka.

---

Programski jezik koji se koristi za stilizovanje veb sajtova.
#### --answer--
Superset jezika JavaScript koji dodaje statičko tipiziranje.
### --question--

#### --text--
Zašto bi trebalo koristiti TypeScript u projektu?
#### --distractors--
Uklanja potrebu za testiranjem u aplikacijama.

---

Brži je od svih ostalih programskih jezika.

---

Olakšava stilizovanje web sajtova.
#### --answer--
Poboljšava održivost dodavanjem definicija tipova u JavaScript.
### --question--

#### --text--
Na koji jezik se TypeScript kompajlira?
#### --distractors--
```markdown
Java
```

---

Python

---

C++
#### --answer--
JavaScript
### --question--

#### --text--
Koji bi bio izlaz navedenog kodnog fragmenta?

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--
Koja je glavna svrha interfejsa u TypeScript-u?
#### --distractors--
Kompilovanje TypeScript koda u JavaScript.

---

Kreiranje novog tipa podataka.

---

Kreiranje novog JavaScript objekta.
#### --answer--
Definisanje strukture objekta.
### --question--

#### --text--
Koji od sledećih nije osnovni primitiv u TypeScript-u?
#### --distractors--
```
string
```

---

boolean

---

broj
#### --answer--
niz
### --question--

#### --text--
Koja od sledećih opcija pravilno definiše literal tip u TypeScript-u?
#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--
Koja je primarna svrha ključne reči ``type`` u TypeScript-u?
#### --distractors--
Deklarisanje varijabli.

---

Kreiranje klasa.

---

Implementacija interfejsa.
#### --answer--
Za definisanje prilagođenih tipova.
### --question--

#### --text--
Koja od sledećih tačno definiše union tip?
#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--
Koja od sledećih mogućnosti je jedinstvena za `interface` i NE MOŽE se postići korišćenjem `type` u TypeScriptu?
#### --distractors--
Definisanje oblika objekata.

---

Kreiranje tipova unije.

---

Definisanje tipova koji uključuju samo specifične vrednosti.
#### --answer--
Deklarisanje implementacija klasa.
### --question--

#### --text--
Koja je glavna svrha korišćenja generika u TypeScriptu?
#### --distractors--
Implementiranje nasleđivanja između klasa.

---

Definisanje strukture objekta.

---

Kreiranje novog tipa podataka.
#### --answer--
Za pisanje ponovno upotrebljivog koda koji može da radi sa više tipova.
### --question--

#### --text--
Koji od sledećih je **pravilan primer** generičke funkcije u TypeScript-u?
#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--
Šta je sužavanje tipa u TypeScriptu?
#### --distractors--
Smanjenje veličine kompajliranog JavaScripta.

---

Ograničavanje broja tipova u uniji.

---

Uklanjanje nepokorišćenih tipova iz TypeScript projekta.
#### --answer--
Pisanje koda koji uzmanjuje unioni tip na specifičniji tip.
### --question--

#### --text--
Koji fajl se obično koristi za konfigurisanje opcija TypeScript kompajlera?
#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--
Šta opcija `rootDir` u fajlu `tsconfig.json` određuje?
#### --distractors--
Verzija TypeScripta koju treba koristiti tokom kompajlacije.

---

Ciljno okruženje za izvršavanje (npr. Node.js ili pretraživač) za TypeScript kod.

---

Specifičan modulski sistem koji se koristi za uvoz i izvoz.
#### --answer--
Direktorijum koji sadrži vaše izvorni fajlove.
### --question--

#### --text--
Koji primer pravilno koristi `instanceof` za sužavanje tipa kako bi se bezbedno pristupilo svojstvu `value`?
#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
Šta postavljanje `"strict": true` u `tsconfig.json` radi u TypeScriptu?
#### --distractors--
Omogućava statičku proveru tipova kako biste otkrili greške pre nego što pokrenete kod.

---

Obavezuje korišćenje ES6 sintakse.

---

Nateruje vas da kompajlirate kod pre nego što ga pokrenete.
#### --answer--
Omogućava skup opcija za proveru tipova, kao što je obavezna obrada `null` ili `undefined`, i sprečava implicitno `any`.
### --question--

#### --text--
Koja je svrha tipa ``never`` u TypeScript-u?
#### --distractors--
Da predstavlja null ili undefined.

---

Za predstavljanje bilo koje moguće vrednosti.

---

Da predstavlja tip koji može biti bilo šta osim null-a.
#### --answer--
Predstavlja tip vrednosti koji nikada ne nastaju.
### --question--

#### --text--
Koji od sledećih poziva funkcija će se uspešno kompajlirati u TypeScript-u?

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--
Kako možete popraviti grešku kompajlera TypeScript-a `'email' is possibly 'null'` u sledećem kodu koristeći opcionalno lančovanje (optional chaining)?

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`

