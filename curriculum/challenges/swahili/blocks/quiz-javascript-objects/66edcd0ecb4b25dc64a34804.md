---
id: 66edcd0ecb4b25dc64a34804
title: Kviz o JavaScript objektima
challengeType: 8
dashedName: quiz-javascript-objects
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koja od sledećih NE opisuje objekat u JavaScriptu?
#### --distractors--
Kolekcija parova ključ-vrednost.

---

Kontejner za svojstva i metode.

---

Nenprimitivni tip podataka.
#### --answer--
Primitivni tip podataka.
### --question--

#### --text--
Koja od sledećih objektnih definicija je nevažeća?
#### --distractors--

`const book = { "pages": 255 };`

---

`const car = { year: 2025 };`

---

`const person = { "name": "nora" };`

#### --answer--

`const product = { color: blue };`

### --question--

#### --text--
Kako možete pristupiti svojstvu ``job`` ovog objekta ``person``?

```js
let person = { job: "Software Developer" };
```

#### --distractors--

`person -> job`

---

`person#job`

---

`person[job]`

#### --answer--

`person.job`

### --question--

#### --text--
Kako možete pristupiti svojstvu `street` objekta `person` ispod?

```js
const person = {
  address: {
    street: "sample-street"
  }
};
```

#### --distractors--

`person -> address -> street`

---

`person#address#street`

---

`person[address.street]`

#### --answer--

`person["address"]["street"]`

### --question--

#### --text--
Koji od ovih ključeva svojstava se može pristupiti samo koristeći notaciju sa uglastim zagradama?
#### --distractors--
Ključ svojstva koji je poznat i statičan.

---

Ključ svojstva koji je ugnježdjen na više nivoa unutar objekta.

---

Ključ svojstva koji možda ne postoji u objektu.
#### --answer--
Ključ svojstva koji je dinamičan.
### --question--

#### --text--
Koji od sledećih pravilno koristi destrukturiranje da pristupi svojstvu `name` objekta `person` ispod?

```js
const person = { name: "John" }
```

#### --distractors--

`const name = person.name;`

---

`const name = person["name"];`

---

`const { ...name } = person;`

#### --answer--

`const { name } = person;`

### --question--

#### --text--
Koji od sledećih pravilno koristi destrukturiranje da pristupi svojstvu `flour` objekta `recipe` ispod?

```js
const recipe = {
  ingredients: {
    flour: "2 cups",
    sugar: "1 cup"
  }
};
```

#### --distractors--

`const flour = recipe.ingredients.flour;`

---

`const flour = recipe["ingredients"]["flour"];`

---

`const { ingredients: flour } = recipe;`

#### --answer--

`const { ingredients: { flour } } = recipe;`

### --question--

#### --text--
Kako možete dodeliti podrazumevanu vrednost ključu svojstva kada koristite destrukturiranje u JavaScript-u?
#### --distractors--

`const key = object.key && "defaultValue";`

---

`const key = object.key || "defaultValue";`

---

`const { key: "defaultValue" } = object;`

#### --answer--

`const { key = "defaultValue" } = object;`

### --question--

#### --text--
Kako možete proveriti da li objekat `car` ima svojstvo `year`?
#### --distractors--

`year.hasProperty("car");`

---

`car.hasProperty("year");`

---

`car.hasOwnProperty(year);`

#### --answer--

`car.hasOwnProperty("year");`

### --question--

#### --text--
Šta je objektn metoda?
#### --distractors--
String povezan sa objektom.

---

Niz pridružen objektu.

---

Broj asociran sa objektom.
#### --answer--
Funkcija povezana sa objektom.
### --question--

#### --text--
Kako možete pozvati metod `add` ovog objekta `calculator` da sabere brojeve `10` i `7`?

```js
const calculator = {
  add: function(a, b) {
    return a + b;
  }
};
```

#### --distractors--

`calculator#add(10, 7);`

---

`calculator.call(add(10, 7));`

---

`add.calculator(10, 7);`

#### --answer--

`calculator.add(10, 7);`

### --question--

#### --text--
Koji operator uklanja svojstvo sa objekta?
#### --distractors--

`remove`

---

`erase`

---

`discard`

#### --answer--

`delete`

### --question--

#### --text--
Kako možete proveriti da li svojstvo `score` postoji u objektu `player`?
#### --distractors--

`player in "score"`

---

`player in score`

---

`score in player`

#### --answer--

`"score" in player`

### --question--

#### --text--
Kako možete kreirati novi prazan objekat u JavaScriptu?
#### --distractors--

`object() new`

---

`new object()`

---

`Object() new`

#### --answer--

`new Object()`

### --question--

#### --text--
Koja je ispravna sintaksa za korišćenje optional chaining-a u JavaScriptu?
#### --distractors--

`object?key`

---

`object.["key"]`

---

`object.?key`

#### --answer--

`object?.key`

### --question--

#### --text--
Za šta se koristi optional chaining u JavaScriptu?
#### --distractors--
Koristi se za definisanje promenljivih sa podrazumevanim vrednostima.

---

Služi za definisanje opcionih metoda unutar objekta koje se mogu ili ne mogu pozvati.

---

Koristi se za pisanje konciznijeg koda tako što se više poziva metoda povezuje u jednoj izjavi.
#### --answer--
Služi za siguran pristup svojstvima objekta koji bi mogli biti ``null`` ili ``undefined``.
### --question--

#### --text--
Koji metod se koristi za konverziju JavaScript objekta u JSON string?
#### --distractors--

`Stringify.toJSON()`

---

`JSON.toObject()`

---

`Object.toJSON()`

#### --answer--

`JSON.stringify()`

### --question--

#### --text--
Koji metod konvertuje JSON string nazad u JavaScript objekat?
#### --distractors--

`JSON.toObject()`

---

`JSON.convert()`

---

`JSON.object()`

#### --answer--

`JSON.parse()`

### --question--

#### --text--
Koja od ovih opcija pristupa svojstvu ``color`` objekta ``toy`` koristeći notaciju uglastih zagrada?
#### --distractors--

`toy.color`

---

`toy[color]`

---

`color[toy]`

#### --answer--

`toy["color"]`

### --question--

#### --text--
Kako možete pristupiti telefonu `home` ovog objekta `learner` u JavaScript-u koristeći notaciju tačke?

```js
const learner = {
  name: "Nora",
  age: 45,
  contact: {
    email: "nora@email.com",
    phone: {
      home: "123-456-7890",
      work: "098-765-4321"
    }
  }
};
```

#### --distractors--

`learner.name.phone.home`

---

`learner.email.work`

---

`learner.contact.home`

#### --answer--

`learner.contact.phone.home`

