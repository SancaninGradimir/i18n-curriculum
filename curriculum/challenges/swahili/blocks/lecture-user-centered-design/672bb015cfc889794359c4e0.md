---
id: 672bb015cfc889794359c4e0
title: Koje su najbolje prakse za dizajniranje modalnih dijaloga?
challengeType: 19
dashedName: what-are-best-practices-for-designing-modal-dialogs
---

# --interactive--

**NAPOMENA**: Neki interaktivni primeri mogu koristiti CSS i JavaScript koji još niste naučili. Ne brinite zbog pokušaja razumevanja celog koda. Cilj primera je da vam pokažu predgleđaje ovih dizajnerskih koncepata kako biste bolje razumeli kako stvari funkcionišu. Da biste videli predgleđaje, moraćete da omogućite interaktivni uresnik.

Šta je modal? To je vrsta pop-apa koji vam sajt može prikazati preko svog sadržaja. HTML ima element ``dialog`` koji možete koristiti za kreiranje modala.

Sadržaj iza modala je obično ugasen. Ovo pomaže korisniku da vizuelno fokusira na oblast sa kojom želite da interaguju – u ovom slučaju, modal.

Uvek je dobra ideja dozvoliti korisniku da klikne izvan modala kako bi ga zatvorio.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button>Subscribe</button>
  <button>Close</button>
</dialog>

<script src="index.js"></script>
```

```css
dialog {
  border: none;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

dialog::backdrop {
  background: rgba(0, 0, 0, 0.5);
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('button:last-of-type');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
}); 

```

:::

Često ćete videti vrlo istaknuta dugmad na modalima. Oni se nazivaju CTA-ji, ili poziv na akciju. Želite da budu lako prepoznatljivi jer je svrha prekidanja korisnikovog protoka modalom podstaknuti ih na preduzimanje određene akcije.

Modali bi takođe trebalo da imaju dugme za zatvaranje. Iako možda veoma želite da korisnik klikne na vaše CTA-ove, važno je dati im opciju da izađu iz modala i nastave sa onim što su prethodno radili.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button class="cta">Subscribe</button>
  <button class="close">Close</button>
</dialog>
<script src="index.js"></script>
```

```css
.cta {
  background-color: #007BFF;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
}

.close {
  background-color: transparent;
  color: #007BFF;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('.close');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
});

```

:::

Naravno, postoje brige za pristupačnost kod modala, kao što je pravilno upravljanje fokusom na elementima. Međutim, ako koristite ove opšte prakse kao svoju polaznu tačku, imaćete čvrst temelj na kojem možete graditi.
# --questions--

## --text--

Koja je svrha zamagljivanja sadržaja iza modalnog dijaloga?
## --answers--

Da bi se sačuvala baterijska snaga na mobilnim uređajima.
### --feedback--

Lekcija pominje specifičan razlog za ovaj vizuelni efekat.
---

To help users visually identify the area they should interact with.

---

To make the website load faster.

### --feedback--

Lekcija pominje specifičan razlog za ovaj vizuelni efekat.
---

To prevent users from seeing sensitive information.

### --feedback--

Lekcija pominje specifičan razlog za ovaj vizuelni efekat.
## --video-solution--

2

## --text--

Šta snabiva skraćenica CTA u kontekstu dizajna modala?
## --answers--

Kliknite da aktivirate.
### --feedback--

Lekcija pruža pun naziv za ovaj akronim kada govori o istaknutim dugmićima u modalima.
---

Complete The Action.

### --feedback--

Lekcija pruža pun naziv za ovaj akronim kada govori o istaknutim dugmićima u modalima.
---

Call-to-Action.

---

Close The Application.

### --feedback--

Lekcija pruža pun naziv za ovaj akronim kada govori o istaknutim dugmićima u modalima.
## --video-solution--

3

## --text--

Zašto je važno uključiti dugme za zatvaranje u modal dijalog?
## --answers--

To je zakonom propisano u nekim zemljama.
### --feedback--

Ova lekcija naglašava važnost omogućavanja korisnicima da se povuku iz modala.
---

To prevent the modal from crashing the browser.

### --feedback--

Ova lekcija naglašava važnost omogućavanja korisnicima da se povuku iz modala.
---

To give users an option to exit the modal and resume their previous activity.

---

To make the modal look more professional.

### --feedback--

Ova lekcija naglašava važnost omogućavanja korisnicima da se povuku iz modala.
## --video-solution--

3
