---
id: 6626b4c58c027d86478ff5eb
title: Korak 3
challengeType: 1
dashedName: step-3
---

# --description--
Nastavnik je zaista zadovoljan programom koji ste do sada kreirali. Ali sada žele da imaju jednostavan način da provere da li učenik ima polognu ocenu. Pologna ocena je bilo šta što nije `"F"`.

Dopunite funkciju ``hasPassingGrade`` koja prima ocenu učenika kao parametar. Vaša funkcija bi trebalo da vrati ``true`` ako je ocena položna i ``false`` ako nije.

**Saveti**

- Koristite funkciju ``getGrade`` da dobijete ocenu učenika. Zatim proverite da li je ocena prolazna ili ne.
# --hints--
Vaša funkcija ``hasPassingGrade`` bi trebalo da vrati booleanski vrednost.

__CODE_BLOCK_0__

Vaša `hasPassingGrade` funkcija bi trebalo da vrati `true` ako je ocena `"A"`.

__CODE_BLOCK_1__

Vaša `hasPassingGrade` funkcija bi trebalo da vrati `false` ako je ocena `"F"`.

__CODE_BLOCK_2__

Vaša funkcija ``hasPassingGrade`` bi trebalo da vrati ``true`` za sve prolazne ocene.

__CODE_BLOCK_3__

# --seed--

## --seed-contents--

```js
function getAverage(scores) {
  let sum = 0;

  for (const score of scores) {
    sum += score;
  }

  return sum / scores.length;
}

function getGrade(score) {
  if (score === 100) {
    return "A++";
  } else if (score >= 90) {
    return "A";
  } else if (score >= 80) {
    return "B";
  } else if (score >= 70) {
    return "C";
  } else if (score >= 60) {
    return "D";
  } else {
    return "F";
  }
}

--fcc-editable-region--
function hasPassingGrade(score) {
  
}


console.log(hasPassingGrade(100));
console.log(hasPassingGrade(53));
console.log(hasPassingGrade(87));
--fcc-editable-region--
```
