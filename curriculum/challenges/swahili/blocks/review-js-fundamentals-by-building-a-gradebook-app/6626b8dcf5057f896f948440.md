---
id: 6626b8dcf5057f896f948440
title: Korak 4
challengeType: 1
dashedName: step-4
---

# --description--
Sada kada nastavnik ima sve informacije koje mu treba, želi da bude u mogućnosti da pošalje poruku studentu sa rezultatima.

Popunite funkciju ``studentMsg`` sa ``totalScores`` i ``studentScore`` za parametre. Funkcija treba da vrati string koji predstavlja poruku za studenta.

Ako je student položio kurs, `string` bi trebalo da prati ovaj format:

__CODE_BLOCK_0__

Ako učenik ne položi predmet, string bi trebalo da prati ovaj format:

__CODE_BLOCK_1__

Zamenite `average-goes-here` prosečnim rezultatom ukupnih bodova. 
Zamenite `grade-goes-here` sa ocenom učenika.

**Saveti**

- Koristite funkciju `getAverage` da biste dobili prosečnu ocenu razreda.
- Koristite funkciju `getGrade` da biste dobili ocenu učenika.
- Koristite konkatenaciju stringa (`+`) za izradu poruke. 
- Obratite pažnju na interpunkciju i razmake u poruci.
# --hints--
```
`studentMsg([92, 88, 12, 77, 57, 100, 67, 38, 97, 89], 37)` treba da vrati sledeću poruku: `"Class average: 71.7. Your grade: F. You failed the course."`.
```

__CODE_BLOCK_0__

``studentMsg([56, 23, 89, 42, 75, 11, 68, 34, 91, 19], 100)`` bi trebalo da vrati sledeću poruku: ``"Class average: 50.8. Your grade: A++. You passed the course."``.

__CODE_BLOCK_1__

Vaša funkcija ``studentMsg`` treba da vrati tačnu poruku na osnovu ocene učenika i prosečne ocene razreda.

__CODE_BLOCK_2__

# --seed--

## --seed-contents--

```js
function getAverage(scores) {
  let sum = 0;

  for (const score of scores) {
    sum += score;
  }

  return sum / scores.length;
}

function getGrade(score) {
  if (score === 100) {
    return "A++";
  } else if (score >= 90) {
    return "A";
  } else if (score >= 80) {
    return "B";
  } else if (score >= 70) {
    return "C";
  } else if (score >= 60) {
    return "D";
  } else {
    return "F";
  }
}

function hasPassingGrade(score) {
  return getGrade(score) !== "F";
}

--fcc-editable-region--
function studentMsg(totalScores, studentScore) {

}
console.log(studentMsg([92, 88, 12, 77, 57, 100, 67, 38, 97, 89], 37));
--fcc-editable-region--

```


# --solutions--

```js
function getAverage(scores) {
  let sum = 0;

  for (const score of scores) {
    sum += score;
  }

  return sum / scores.length;
}

function getGrade(score) {
  if (score === 100) {
    return "A++";
  } else if (score >= 90) {
    return "A";
  } else if (score >= 80) {
    return "B";
  } else if (score >= 70) {
    return "C";
  } else if (score >= 60) {
    return "D";
  } else {
    return "F";
  }
}

function hasPassingGrade(score) {
  return getGrade(score) !== "F";
}

function studentMsg(totalScores, studentScore) {
  let average = getAverage(totalScores);
  let grade = getGrade(studentScore);

  return `Class average: ${average}. Your grade: ${grade}. You ${
    hasPassingGrade(studentScore) ? "passed" : "failed"
  } the course.`;
}

```
