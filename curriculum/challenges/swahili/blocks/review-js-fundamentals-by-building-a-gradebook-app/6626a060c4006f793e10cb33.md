---
id: 6626a060c4006f793e10cb33
title: Korak 2
challengeType: 1
dashedName: step-2
---

# --description--
Sada je nastavniku potrebna vaša pomoć za pretvaranje očne učenika u slovnu ocenu.

Popunite funkciju ``getGrade`` koja prima broj ``score`` kao parametar. Vaša funkcija bi trebalo da vrati niz znakova koji predstavlja slovnu ocenu na osnovu rezultata.

Ovo su ocene i odgovarajući slovi ocena:

| Opseg bodova | Ocena |
| ----------- | ------- |
| `100`       | `"A++"` |
| `90 - 99`   | `"A"`   |
| `80 - 89`   | `"B"`   |
| `70 - 79`   | `"C"`   |
| `60 - 69`   | `"D"`   |
| `0 - 59`    | `"F"`   |

**Saveti**

- Setakajte se što ste naučili o uslovnim izjavama (`if`, `else if`, i `else`).
- Setakajte se što ste naučili o operatorima poređenja (`>`, `<`, `>=`, `<=`, `===`).
- Setakajte se što ste naučili o logičkom AND operateru (`&&`).
# --hints--
Vaša funkcija ``getGrade`` bi trebalo da vrati ``"A++"`` ako je rezultat ``100``.

__CODE_BLOCK_0__

Funkcija `getGrade` bi trebalo da vrati `"A"` ako je rezultat `94`.

__CODE_BLOCK_1__

Funkcija ``getGrade`` bi trebalo da vrati ``"B"`` ako je rezultat između ``80`` i ``89``.

__CODE_BLOCK_2__

Funkcija `getGrade` bi trebalo da vrati `"C"` ako je rezultat `78`.

__CODE_BLOCK_3__

Funkcija ``getGrade`` bi trebalo da vrati ``"D"`` ako je rezultat između ``60`` i ``69``.

__CODE_BLOCK_4__

Funkcija `getGrade` bi trebalo da vrati `"F"` ako je rezultat `57`.

__CODE_BLOCK_5__

Funkcija ``getGrade`` bi trebalo da vrati ``"F"`` ako je rezultat između ``0`` i ``59``.

__CODE_BLOCK_6__

# --seed--

## --seed-contents--

```js
function getAverage(scores) {
  let sum = 0;

  for (const score of scores) {
    sum += score;
  }

  return sum / scores.length;
}
--fcc-editable-region--
function getGrade(score) {

}

console.log(getGrade(96));
console.log(getGrade(82));
console.log(getGrade(56));
--fcc-editable-region--
```
