---
id: 68c3bc80f67363a31791fe1c
title: Šta je apstrakcija i kako pomaže u održavanju organizovanih kompleksnih sistema?
challengeType: 19
dashedName: what-is-abstraction-and-how-does-it-help-keep-complex-systems-organized
---

# --description--

Sada kada smo pogledali enkapsulaciju, nasleđivanje i polimorfizam, hajde da razgovaramo o sledećem ključnom konceptu objektno orijentisanog programiranja – apstrakciji.

Apstrakcija je proces skrivanja složenih detalja implementacije i prikazivanja samo esencijalnih karakteristika objekta ili sistema. Razmislite o tome kao o fokusiranju na ono što nešto radi, a ne na to kako to radi.

Apstrakcija nije ograničena samo na Python. To je programski koncept koji se može implementirati u mnogim jezicima koji podržavaju objektno orijentisano programiranje.

Da ilustrujemo apstrakciju, zamislite da voziš automobil. Kada si za volanom, interaguješ sa esencijalnim delovima kao što su volan, menjač i pedale gasa i kočnice. Ne moraš da znaš intrikate detalje o tome kako radi motor, kako menjača prebacuje brzine ili fizika iza sistema kočenja, jer su sve to složeni detalji implementacije.

To je apstrakcija u akciji! Pruža ti pojednostavljeni interfejs za interakciju sa kompleksnim sistemom.

U slučaju automobila ponovo, pojednostavljeni interfejs su volan, kočnice i gas, dok je složeni sistem sam automobil.

Što se tiče načina na koji Python implementira apstrakciju, radi to putem `abc` modula.

Ovaj modul pruža klasu `ABC` (što znači „abstract base class“ ili apstraktna bazna klasa) i dekorator `@abstractmethod`.

`ABC` je klasa koja je namenjena da se nasleđuje, ali ne možeš kreirati direktne objekte iz nje. Ona definiše zajednički interfejs metoda i svojstava koje moraju implementirati njene podklase.

S druge strane, apstraktna metoda je metoda deklarisana u Apstraktnoj Baznoj Klasici (ABC) korišćenjem dekoratora `@abstractmethod`. Može nemati nikakvu implementaciju ili osnovnu podrazumevanu. Međutim, svaka podklasa mora da je prepiski (override) kako bi se smatrala konkretnom i instancijalizabilnom, čak i ako je dostavljena podrazumevana implementacija.

Evo osnovne sintakse apstraktne klase u Pythonu:

```py
from abc import ABC, abstractmethod

# Define an abstract base class
class AbstractClass(ABC):
    @abstractmethod
    def abstract_method(self):
        pass

# Concrete subclass that implements the abstract method
class ConcreteClassOne(AbstractClass):
    def abstract_method(self):
        print('Implementation in ConcreteClassOne')

# Another concrete subclass
class ConcreteClassTwo(AbstractClass):
    def abstract_method(self):
        print('Implementation in ConcreteClassTwo')
```

Evo osnovnog primera:

```py
from abc import ABC, abstractmethod

class Animal(ABC): # Inherits from abstract base class
   @abstractmethod # Abstract method decorator
   def make_sound(self):  # The method subclasses must override
       pass

# Concrete class that will override the abstract method
class Dog(Animal):
   def make_sound(self):
       print('Woof!')

# Another concrete class that will override the abstract method
class Cat(Animal):
   def make_sound(self):
       print('Meow!')

# Another concrete class that will override the abstract method
class Monkey(Animal):
   def make_sound(self):
       print('Ooh ooh aah aah!')

# Create instances of each concrete class
animals = [Dog(), Cat(), Monkey()]

# Loop through the instances to call the make_sound method
for animal in animals:
   animal.make_sound()

# Output:
# Woof!
# Meow!
# Ooh ooh aah aah!
```

U ovom primeru:

- Uvozimo klasu `ABC` i `abstractmethod` iz modula `abc`.
- Zatim kreiramo klasu `Animal` koja nasleđuje od `ABC`, i kreiramo apstraktnu metodu `make_sound` u njoj koju svaka podklasa `Animal`-a mora da prepiski.
- Kreiramo konkretne klase `Dog`, `Cat` i `Monkey`, koje moraju da prepiski apstraktnu metodu `make_sound`.
- Instancijalizujemo konkretne klase i pozivamo njihovu metodu `make_sound` kako bismo pokazali kako svaka od njih implementira apstraktnu metodu `make_sound` na svoj način.

Zapamti da ne možeš kreirati instancu klase `Animal`. Evo šta se dešava ako to pokušaš:

```py
dog = Animal() 
# TypeError: Can't instantiate abstract class Animal 
# without an implementation for abstract method 'make_sound'
```

Ista pravila važe za podklase koje ne pružaju implementaciju apstraktne metode. Čak i ako definišeš podklasu od `Animal`-a, ne možeš je instancijalizovati dok ne prepiski `make_sound`. Evo primera koji to pokazuje:

```py
class Bird(Animal):
    pass

bird = Bird()
# TypeError: Can't instantiate abstract class Bird 
# without an implementation for abstract method 'make_sound'
```

Evo još jednog primera, ovog puta sa atributom instance koji možeš proslediti instancama konkretnih metoda:

```py
from abc import ABC, abstractmethod

# The blueprint for any toy that can speak
class TalkingToy(ABC):
   def __init__(self, name):
       self.name = name
   @abstractmethod
   def speak(self):
       pass

class RobotToy(TalkingToy):
   def speak(self):
       print(f'{self.name} says beep boop! I am a robot!')

class TeddyBearToy(TalkingToy):
   def speak(self):
       print(f"{self.name} says hug me! I'm cuddly!")

class DinosaurToy(TalkingToy):
   def speak(self):
       print(f'{self.name} says ROOOOAR!')

# Create toys
rusty = RobotToy('Rusty')
fluffy = TeddyBearToy('Fluffy')
rex = DinosaurToy('Rex')

toys = [rusty, fluffy, rex]
for toy in toys:
   toy.speak()

# Output:
# Rusty says beep boop! I am a robot!
# Fluffy says hug me! I'm cuddly!
# Rex says ROOOOAR!
```

U ovom primeru: 

- Imamo apstraktnu baznu klasu `TalkingToy` koja definiše nacrt za svaku igračku koja može da govori.
- Podklase `RobotToy`, `TeddyBearToy` i `DinosaurToy` implementiraju metodu `speak` na svoj način.
- Kada kreiramo instance ovih podklasa i pozovemo metodu `speak`, svaka igračka govori na svoj jedinstven način.

Zaključno, apstrakcija u Pythonu pojednostavljuje složene sisteme povećanjem ponovne upotrebljivosti.

Videli ste kako možete ponovo koristiti jednu metodu iz apstraktne klase preko više podklasa, istovremeno prisiljavajući svaku podklasu da pruži svoje specifično ponašanje.

Ovaj pristup održava vaš kod organizovanim, fleksibilnim i lakšim za održavanje, posebno kako vaša aplikacija raste.
# --questions--

## --text--

Šta je primarni cilj apstrakcije u objektno orijentisanom programiranju?
## --answers--

Izložiti sve interne detalje implementacije korisnicima.
### --feedback--

Razmislite kako apstrakcija pojednostavljuje interakciju sa kompleksnim sistemima.
---

To merge multiple classes into one.

### --feedback--

Razmislite kako apstrakcija pojednostavljuje interakciju sa kompleksnim sistemima.
---

To prevent methods from being overridden in child classes.

### --feedback--

Razmislite kako apstrakcija pojednostavljuje interakciju sa kompleksnim sistemima.
---

To hide complex logic and only show essential features

## --video-solution--

4

## --text--

Kako Python implementira apstrakciju kroz svoj modul `ABC`?
## --answers--

Omogućavanjem direktne instancijacije apstraktnih klasa.
### --feedback--

ABC modul nameće implementaciju metoda u podklasama.
---

By using decorators and inheritance to define abstract methods that subclasses must implement.

---

By automatically hiding all methods with double underscores.

### --feedback--

ABC modul nameće implementaciju metoda u podklasama.
---

By converting classes to functions.

### --feedback--

ABC modul nameće implementaciju metoda u podklasama.
## --video-solution--

2

## --text--

U analogiji automobila za apstrakciju, šta predstavlja uprošćeni interfejs i složeni sistem?
## --answers--

Interfejs su motorni klipovi, a složeni sistem je volan.
### --feedback--

Razmislite o tome da je interfejs ono sa čim direktno interagujete, dok je složeni sistem način na koji funkcioniše.
---

The interface is the steering wheel, brakes, and accelerator, and the complex system is the engine, transmission, and braking physics.

---

The interface is the car manual, and the complex system is the dashboard controls.

### --feedback--

Razmislite o tome da je interfejs ono sa čim direktno interagujete, dok je složeni sistem način na koji funkcioniše.
---

The interface is the fuel type, and the complex system is the tyres.

### --feedback--

Razmislite o tome da je interfejs ono sa čim direktno interagujete, dok je složeni sistem način na koji funkcioniše.
## --video-solution--

2
