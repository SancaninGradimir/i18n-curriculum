---
id: 692487767b6b728454f53f29
title: NodeJS Core Modules Quiz
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koji metod ``fs`` dodaje sadržaj u postojeći fajl bez prepisivanja?
#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--
Šta se dešava kada koristite sinhrone metode iz `fs` modula?
#### --distractors--
Automatski brišu postojeće datoteke.

---

U svim slučajevima, izvršavaju se brže od asinhronih.

---

Vraćaju Promises podrazumevano.
#### --answer--
Oni blokiraju program dok operacija ne završi.
### --question--

#### --text--
Šta radi parametar `utf8` u `fs.readFile()`?
#### --distractors--
Sprečava bacanje grešaka.

---

Menja ekstenziju fajla.

---

Komprimuje velike fajlove.
#### --answer--
To govori Node-u da vrati čitljivi string umesto buffera.
### --question--

#### --text--
Koja `Buffer` metoda kreira novi buffer ispunjen nulama?
#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--
Šta radi `Buffer.toString()`?
#### --distractors--
Pretvara buffer u binarni kod.

---

Briše sadržaj buffera.

---

Prikazuje dužinu bajtova buffera.
#### --answer--
Pretvara sadržaj buffera u čitljiv string.
### --question--

#### --text--
Koja `crypto` funkcija zahteva privatni ključ za generisanje potpisa?
#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--
Zašto biste trebali izbegavati direktno pisanje sopstvenih kripto algoritama?
#### --distractors--
Ovo je ilegalno u Node.js aplikacijama.

---

Node.js ne podržava šifrovanje.

---

JavaScript ne može da izračuna hash-ove.
#### --answer--
Može biti nesigurno bez dobro testiranih biblioteka.
### --question--

#### --text--
Koji `crypto` metod se preporučuje za generisanje sigurnosnih nasumičnih tokena umesto `Math.random()`?
#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--
Koja metoda OS modula vraća arhitekturu CPU-a?
#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--
Šta vraća `os.userInfo()`?
#### --distractors--
Samo korisničko ime.

---

Samo adresu IP uređaja.

---

Samo ID prijavljenog korisnika.
#### --answer--
Informacije o trenutnom korisniku sistema.
### --question--

#### --text--
Koja `path` metoda spaja segmente i normalizuje višak kosih crta?
#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--
Koja metoda `path` proizvodi apsolutnu putanju zasnovanu na trenutnom radnom direktorijumu?
#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--
Šta globalna promenljiva `__dirname` predstavlja?
#### --distractors--
Ime korisnika koji pokreće Node.

---

Ekstenzija trenutne datoteke.

---

Relativna putanja do foldera koja počinje sa `./`.
#### --answer--
Apsolutna staza direktorijuma koji sadrži trenutnu datoteku.
### --question--

#### --text--
Koje svojstvo procesa sadrži varijable okruženja poput `NODE_ENV`?
#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--
Šta radi događaj `process.on("exit")`?
#### --distractors--
Restartuje Node aplikaciju.

---

Šalje signal `exit` podprocesima.

---

Čisti red događaja.
#### --answer--
Izvršava kod neposredno pre nego što proces Node-a završi.
### --question--

#### --text--
Koji tip streama može da čita i piše podatke, ali ih ne transformiše?
#### --distractors--
Čitljiv tok

---

Transformaciona struja

---

Upisivni tok
#### --answer--
Dupleksni tok
### --question--

#### --text--
Šta je potrebno za efikasnu implementaciju čitanja i pisanja velikih fajlova?
#### --distractors--
Korišćenjem samo `Buffer.alloc()`

---

Uvoz modula `crypto`

---

Čuvanje celog fajla u memoriji prvo
#### --answer--
Korišćenje streamova za obradu podataka u delovima
### --question--

#### --text--
Koji metod povezuje čitljiv stream sa upisivim stream-om?
#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--
Koji događaj strema ukazuje da nema više podataka za pisanje?
#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--
Šta se loguje ako odštampate blok sa čitljivog streama bez `toString()`?
#### --distractors--
Sadržaj teksta

---

PID procesa

---

Ime operativnog sistema
#### --answer--
Objekat bafera sa binarnim podacima