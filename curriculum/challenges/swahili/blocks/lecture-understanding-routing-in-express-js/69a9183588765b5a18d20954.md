---
id: 69a9183588765b5a18d20954
title: Koje su različite metode odgovora u Expressu?
challengeType: 19
dashedName: what-are-the-different-response-methods-in-express
---

# --description--

U Express, element `res` (odgovor) koristi se za slanje odgovora nazad klijentu nakon obrade dolaznih zahteva. Express nudi nekoliko ugrađenih načina za slanje različitih tipova odgovora. Na primer, možete poslati običan tekst, podatke JSON, uputiti korisnika na drugi URL, ili prikazati šablon.

Najčešći načini odgovora su `res.send()`:

```javascript
app.get("/text", (req, res) => {
  res.send("Hello, World!");
});
```

Njia ya `res.send()` hutuma jibu kurudi kwa mteja. Inaweza kutuma maandishi ya kawaida, HTML, JSON, au hata data ya binary. Katika mfano huu, mteja anapotembelea `/text`, seva inajibu kwa maandishi `Hello, World!`.

Drugi često korišćeni način rešenja je `res.json()`.

Njia hii hutuma jibu la JSON kwa mteja. Ni muhimu hasa wakati wa kujenga API, ambapo mara nyingi unahitaji kurudisha data iliyopangwa kama vitu au safu za data. Express hubadilisha moja kwa moja kitu cha JavaScript kuwa mfuatano wa herufi wa JSON:

```javascript
app.get("/user", (req, res) => {
  res.json({ name: "John Doe", age: 30 });
});
```

Ikiwa mteja atatuma ombi kwa `/user`, seva itajibu kama ifuatavyo:

```json
{
  "name": "John Doe",
  "age": 30
}
```

Unapohitaji kudhibiti nambari ya hali ya HTTP ya jibu, unaweza kutumia `res.status()`. Njia hii huweka nambari ya hali na kawaida hutumika pamoja na njia nyingine ya jibu kama `res.send()` au `res.json()`:

```javascript
app.get("/notfound", (req, res) => {
  res.status(404).send("Not Found");
});
```

Hapa, seva inaweka nambari ya hali kuwa `404` na kutuma ujumbe `Not Found` kurudi kwa mteja.

Ikiwa unataka kumpeleka mtumiaji kwenye URL tofauti, unaweza kutumia `res.redirect()`:

```javascript
app.get("/oldpage", (req, res) => {
  res.redirect("/newpage");
});
```

Mteja anapotembelea `/oldpage`, anaelekezwa upya moja kwa moja kwenda `/newpage`.

Mwishowe, `res.render()` hutumika wakati wa kufanya kazi na injini za kiolezo kama Pug, EJS, au Handlebars. Inaonyesha kiolezo cha mtazamo na kutuma HTML inayotokana kwa mteja:

```javascript
app.get("/profile", (req, res) => {
  res.render("profile", { username: "john_doe" });
});
```

U ovom primeru, server prikazuje pogled na `profile` i prenosi razliku od `username` koja se može prikazati u HTML.

Ove metode odgovora vam omogućavaju da vratite različite tipove podataka u zavisnosti od prirode zahteva. Bilo da koristite tekst, podatke od JSON ili usmeravate klijenta, Express ima jednostavan set alata za rukovanje tim.

# --questions--

## --text--

Šta radi put `res.json()`?

## --answers--

Pošalji odgovor za JSON kupcu.

---

Pošalji standardni tekst kupcu.

### --feedback--

Razmisli o strukturi podataka koje šalješ nazad.

---

Unesite broj statusa odgovora.

### --feedback--

Razmislite o strukturi podataka koju šaljete nazad.

---

Upućujte klijenta na drugi URL.

### --feedback--

Razmisli o strukturi podataka koje šalješ nazad.

## --video-solution--

1

## --text--

Da li biste mogli da pošaljete statusni kod 404 i posebnu poruku?

## --answers--

`res.send(404, 'Not Found')`

### --feedback--

Koji put koristiš da postaviš statusni broj?

---

`res.status(404).send('Not Found')`

---

`res.redirect('/404')`

### --feedback--

Koju metodu koristiš za postavljanje statusnog koda?

---

`res.status(500).send('Error')`

### --feedback--

Koju metodu koristite za postavljanje statusnog broja/koda?

## --video-solution--

2

## --text--

Putanja za `res.redirect()` šta radi?

## --answers--

Pošalji fajl klijentu.

### --feedback--

Zamislite šta se dešava kada URL preusmerava na drugi URL.

---

Uputite klijenta na drugi URL.

---

Pošalji podatke za JSON klijentu.

### --feedback--

Zamislite šta se dešava kada URL preusmerava na drugi URL.

---

Unesite broj statusa odgovora.

### --feedback--

Zamislite šta se dešava kada URL preusmerava na drugi URL.

## --video-solution--

2