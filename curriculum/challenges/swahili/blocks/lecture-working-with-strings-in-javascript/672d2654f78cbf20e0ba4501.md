---
id: 672d2654f78cbf20e0ba4501
title: Šta je notacija sa uglastim zagradama i kako pristupiti karakterima iz stringa?
challengeType: 19
dashedName: what-is-bracket-notation-and-how-do-you-access-characters-from-a-string
---

# --interactive--

U JavaScriptu, nizovi znakova se tretiraju kao sekvence karaktera, a svaki karakter u nizu može se pristupiti korišćenjem notacije uglastih zagrada. To vam omogućava da dohvatite određeni karakter iz niza na osnovu njegove pozicije, što se naziva njegov indeks.

Indeks je pozicija karaktera unutar niza, i on je baziran na nuli. To znači da prvi karakter niza ima indeks `0`, drugi karakter ima indeks `1`, i tako dalje.

Na primer, u stringu `hello`, karakter `h` je na indeksu `0`, `e` je na indeksu `1`, `l` je na indeksu `2`, i tako dalje.

Notacija zagrada koristi kvadratne zagrade (`[]`) i indeks karaktera na koji želite pristupiti. Pogledajmo primer:

:::interactive_editor

```js
let greeting = "hello";
console.log(greeting[1]); // "e"
```

:::

U ovom primeru, možemo pristupiti karakteru na indeksu `1`, koji je `e`.

Da biste dobili poslednji karakter niza, možete koristiti dužinu niza umanjenu za jedan.
Svojstvo ``length`` niza govori vam koliko karaktera sadrži, pa da biste pristupili poslednjem karakteru, oduzeli biste jedan od dužine:

:::interactive_editor

```js
let greeting = "hello";
console.log(greeting[greeting.length - 1]); // "o"
```

:::

U ovom slučaju, `length` od `hello` je `5`, a poslednji karakter (`o`) se nalazi na indeksu `4` što je `5 - 1`.

Ako želite da dobijete više znakova, možete koristiti notaciju uglastih zagrada ovako:

:::interactive_editor

```js
let greeting = "hello";
let firstTwo = greeting[0] + greeting[1]; // "he"
console.log(firstTwo);
```

:::

U ovom primeru, konkatenišemo prvi i drugi karakter koristeći notaciju uglastih zagrada da bismo formirali string ``he``.

Notacija uglastih zagrada korisna je kada vam treba pristup specifičnim karakterima u nizu karaktera, kao što je izvlačenje inicijala iz imena ili proveravanje određenog slova radi validacije.
# --questions--

## --text--

Koji je indeks karaktera `"r"` u nizu `"JavaScript"`?
## --answers--

`2`

### --feedback--

Zapamtite da brojevi indeksa počinju od `0`.
---

`4`

### --feedback--

Zapamtite da brojevi indeksa počinju od `0`.
---

`6`

---

`8`

### --feedback--

Zapamtite da brojevi indeksa počinju od `0`.
## --video-solution--

3

## --text--

Kako biste pristupili poslednjem karakteru stringa koristeći notaciju uglastih zagrada?
## --answers--

`string[length]`

### --feedback--

Razmislite o tome kako pronaći indeks poslednjeg karaktera.
---

`string[string.length]`

### --feedback--

Razmislite o tome kako pronaći indeks poslednjeg karaktera.
---

`string[string.length - 1]`

---

`string[string - 1]`

### --feedback--

Razmislite o tome kako pronaći indeks poslednjeg karaktera.
## --video-solution--

3

## --text--

Šta vam bracket notacija omogućava da uradite sa nizovima (stringovima) u JavaScriptu?
## --answers--

Dodajte nove znakove u niz.
### --feedback--

Fokusirajte se na to kako oznaka uglastih zagrada interakcioniše sa pojedinačnim karakterima.
---

Change the data type of the string.

### --feedback--

Fokusirajte se na to kako oznaka uglastih zagrada interakcioniše sa pojedinačnim karakterima.
---

Access specific characters in the string using their index.

---

Convert the string into an array of characters.

### --feedback--

Fokusirajte se na to kako oznaka uglastih zagrada interakcioniše sa pojedinačnim karakterima.
## --video-solution--

3
