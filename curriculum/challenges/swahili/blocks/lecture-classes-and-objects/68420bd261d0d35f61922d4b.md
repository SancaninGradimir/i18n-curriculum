---
id: 68420bd261d0d35f61922d4b
title: Kako klase rade i kako se razlikuju od objekata?
challengeType: 19
dashedName: how-do-classes-work-and-how-do-they-differ-from-objects
---

# --description--

U Pythonu, klase i objekti rade ruku pod ruku kako bi organizovali i upravljali podacima. Izgradite klasu da definišete zajedničko ponašanje, a zatim kreirajte objekte koji koriste to ponašanje.

Drugim rečima, klasa je kao nacrt ili šablon koji koristite za kreiranje objekata.

Pogledaćemo šta su klase i kako ih koristiti za kreiranje objekata.

Da biste kreirali klasu, koristite ključnu reč ``class`` praćenu imenom klase i dvotačkom. Zatim unutar klase, možete dodati inicijalizator, zajedno sa bilo kojim atributima i metodama.

Atributi su kao varijable unutar klase i koriste se za skladištenje podataka. Metode su funkcije definisane unutar klase, i predstavljaju akcije koje objekti kreirani sa klasom mogu da izvrše.

Evo osnovne sintakse klase:

```python
class ClassName:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def sample_method(self):               
        print(self.name.upper())
```

* `class ClassName` se sastoji od ključne reči `class` za kreiranje klase, praćene imenom klase, ovde nazvanim `ClassName`. U Pythonu je uobičajeno koristiti konvenciju **PascalCase** prilikom imenovanja klasa.
    
* `def __init__(self, name, age)` je specijalna metoda koja se automatski poziva kada se kreira novi objekat. Inicijalizuje atribute objekata koji će biti kreirani sa klasom.
    
    Pored toga, prvi parametar `__init__` je uvek referenca na specifični objekat koji se kreira ili koristi. Po konvenciji, ovaj parametar se zove `self`, ali tehnički, možete koristiti bilo koje ime. `self` vam omogućava pristup sopstvenim atributima i metodama objekta.
    
* `self.name = name` i `self.age = age` su atributi koje će objekti imati.
    
* `def sample_method(self):` je metoda koju svaki kreirani objekat može pozvati.
    
* `print(self.name.upper())` je ono što će metoda `sample_method` uraditi, u ovom slučaju, ispisuje ime velikim slovima.

Ako sve to zvuči kao mnogo, ne brinite. Pogledaćemo sličan primer klase `Dog`, i kako možete kreirati objekte iz nje:

```python
class Dog:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def bark(self):
        print(f"{self.name.upper()} says woof woof!")
```

Sa ovom `Dog` klasom, možete kreirati objekat. Evo osnovne sintakse za kreiranje objekata iz klase:

```python
object_1 = ClassName(attribute_1, attribute_2)
object_2 = ClassName(attribute_1, attribute_2)
```

Takođe možete pozvati bilo koju od metoda definisanih u klasi sa svakog objekta:

```python
object_1.method_name()
object_2.method_name()
```

Sada ćemo kreirati dva psa koristeći klasu `Dog` kao šablon:

```python
class Dog:
    def __init__(self, name, age):
        self.name = name
        self.age = age

    def bark(self):
        print(f"{self.name.upper()} says woof woof! I'm {self.age} years old!")

dog_1 = Dog("Jack", 3)
dog_2 = Dog("Thatcher", 5)

# Call the bark method
dog_1.bark()  # JACK says woof woof! I'm 3 years old!
dog_2.bark()  # THATCHER says woof woof! I'm 5 years old!
```

Kao što možete videti, kreiramo dva objekta psa koristeći klasu `Dog`. Kada inicijalizujemo `dog_1`, prosleđuju se string `Jack` i broj `3`, što postavlja atribute `name` i `age` za tu instancu. A `dog_2` se inicijalizuje sa stringom `Thatcher` i brojem `5` kao njegovim `name` i `age`, respektivno.

Zatim kada pozovete metodu `.bark()` na `dog_1` i `dog_2`, možete videti kako se oba izlaza razlikuju, i koristiti jedinstvene `name` i `age` atribute koje ste prosledili prilikom kreiranja svakog objekta.

Zaključno, razlika između klase i objekta je u tome što klasa predstavlja šablon ili nacrt, a objekat je ono što se kreira korišćenjem tog šablona.

Takođe, klasa definiše koje podatke i ponašanje objekat treba da ima, a objekat drži stvarne podatke i koristi to ponašanje. Vi napišete klasu jednom, i možete napraviti mnogo objekata od nje, svaki sa različitim podacima.


# --questions--

## --text--

Koji je izlaz ovog koda?

```python
class Dog:  
    def __init__(self, name):  
        self.name = name

    def bark(self):  
        print(f"{self.name} says Woof!")  

my_dog = Dog("Rex")
print(my_dog.name)
```

## --answers--

`Rex says Woof!`

### --feedback--

Pogledajte šta se štampa: da li poziva metodu ili pristupa atributu?
---

`name`

### --feedback--

Pogledajte šta se štampa: da li poziva metodu ili pristupa atributu?
---

`Rex`

---

Error

### --feedback--

Pogledajte šta se štampa: da li poziva metodu ili pristupa atributu?
## --video-solution--

3

## --text--

Šta je specijalna metoda koja se automatski poziva kada se kreira novi objekat?
## --answers--

`__create_object__`

### --feedback--

Razmislite o tome šta inicijalizuje atribute koje će objekat imati.
---

`__init__`

---

`__new__`

### --feedback--

Razmislite o tome šta inicijalizuje atribute koje će objekat imati.
---

`__setup__`

### --feedback--

Razmislite o tome šta inicijalizuje atribute koje će objekat imati.
## --video-solution--

2

## --text--

Šta je nacrt ili šablon za kreiranje objekata?
## --answers--

Varijabla
### --feedback--

Razmislite o tome šta definiše strukturu i ponašanje objekata kreiranih od toga.
---

A function

### --feedback--

Razmislite o tome šta definiše strukturu i ponašanje objekata kreiranih od toga.
---

A class

---

A loop

### --feedback--

Razmislite o tome šta definiše strukturu i ponašanje objekata kreiranih od toga.
## --video-solution--

3

