---
id: 66edcc779993c0da6906dbb9
title: Kviz o funkcijama u JavaScriptu
challengeType: 8
dashedName: quiz-javascript-functions
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koji od sledećih načina je tačan za deklarisanje funkcije u JavaScriptu?
#### --distractors--

```js
func greet() {
  console.log("Hello there!");
}
```

---

```js
defineFunction greet() {
  console.log("Hello there!");
}
```

---

```js
def greet() {
  console.log("Hello there!");
}
```

#### --answer--

```js
function greet() {
  console.log("Hello there!");
}
```

### --question--

#### --text--
Šta se dešava kada pozovete (ili izvršite) funkciju?
#### --distractors--
Kod unutar funkcije će se izvršiti i `null` će uvek biti vraćen.

---

Ništa se neće desiti kada pozovete funkciju.

---

A `TypeError` će biti izbačen i program će se srušiti.
#### --answer--
Kod unutar funkcije će se izvršiti i vraćena će vrednost.
### --question--

#### --text--
Koja od sledećih je ispravan način za pozivanje funkcije?
#### --distractors--

```js
exampleFunction;
```

---

```js
exampleFunction>>;
```

---

```js
()exampleFunction();
```

#### --answer--

```js
exampleFunction();
```

### --question--

#### --text--
Koja je uloga ključne reči `return`?
#### --distractors--
Ključna reč ``return`` će izvršiti funkciju.

---

Ključna reč ``return`` će izazvati grešku tipa.

---

Ključna reč ``return`` nema svrhe u funkcijama.
#### --answer--
Ključna reč ``return`` vraća vrednost iz funkcije.
### --question--

#### --text--
Koji od sledećih je validan izraz funkcije?
#### --distractors--

```js
expression function getSum(x, y) {
    return x + y;
};
```

---

```js
getSum: function(x, y) {
    return x + y;
};
```

---

```js
function = getSum(x, y) {
    return x + y;
};
```

#### --answer--

```js
const getSum = function(x, y) {
    return x + y;
};
```

### --question--

#### --text--
Šta su argumenti funkcija?
#### --distractors--
Vrednosti koje predstavljaju odsustvo vrednosti.

---

Posebne vrednosti koje deluju slično anonimnim funkcijama.

---

Zamenske vrednosti korišćene unutar funkcije.
#### --answer--
Vrednosti koje se prosleđuju funkciji kada je pozvana.
### --question--

#### --text--
Šta su parametri funkcije?
#### --distractors--
Decimalne vrednosti korišćene unutar funkcije.

---

Vrednosti koje se prosleđuju funkciji kada je pozvana.

---

Vrednosti koje predstavljaju namerno odsustvo vrednosti.
#### --answer--
Vrednosti navedene unutar definicije funkcije.
### --question--

#### --text--
Šta će biti logovano u konzolu?

```js
const calculateTotal = (amount, taxRate = 0.05) => {
  return amount + (amount * taxRate);
};

console.log(calculateTotal(100));
```

#### --distractors--

`10`

---

`undefined`

---

`1`

#### --answer--

`105`

### --question--

#### --text--
Koja od sledećih je ispravna sintaksa za strelicu funkciju?
#### --distractors--

```js
const sum <<>> (num1, num2) => num1 + num2
```

---

```js
const sum === (num1, num2) === num1 + num2
```

---

```js
const sum >=> (num1, num2) => num1 + num2
```

#### --answer--

```js
const sum = (num1, num2) => num1 + num2
```

### --question--

#### --text--
Kakav će biti rezultat za sledeći kod?

```js
function greet() {
  const developer = "Jessica";
  console.log("Hello there!");
}

console.log(developer);
```

#### --distractors--
Neće se ništa dogoditi i kod će raditi normalno.

---

String ``"Hello there!"`` će biti logovan u konzolu.

---

String ``"Hello!"`` će biti logovan u konzolu.
#### --answer--
Dobićete grešku referenciranja jer ``developer`` nije definisan globalno.
### --question--

#### --text--
Kada možete izostaviti zagrade oko liste parametara za `arrow function`?
#### --distractors--
Uvek biste trebali da izostavite zagrade oko liste parametara.

---

Nikada ne bi trebalo da izostavljate zagrade za strelicastu funkciju (*arrow function*).

---

Trebalo bi da uklonite zagrade samo kada radite sa više parametara.
#### --answer--
Zagrade treba da izostavite samo kada imate jedan parametar.
### --question--

#### --text--
Kada možete izostaviti vitičaste zagrade i ključnu reč ``return`` za streličastu funkciju?
#### --distractors--
Trebalo bi uvek da izostavite vitičaste zagrade i ključnu reč `return`.

---

Nikada ne smete izostaviti kvasterije i ključnu reč `return`.

---

Kada telo funkcije ima više linija koda.
#### --answer--
Kada telo funkcije sastoji se samo od jednog izraza.
### --question--

#### --text--
Šta će ova funkcija vratiti?

```js
function exampleFunction() {
  return "Hello";
  return "World!";
};
```

#### --distractors--

`"World!"`

---

`"Hello World!"`

---

`undefined`

#### --answer--

`"Hello"`

### --question--

#### --text--
Koji od sledećih načina je ispravan za korišćenje podrazumevanih parametara?
#### --distractors--

```js
function sum(num1, num2 <<>> 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 === 12) {
  return num1 + num2
}
```

---

```js
function sum(num1, num2 >> 12) {
  return num1 + num2
}
```

#### --answer--

```js
function sum(num1, num2 = 12) {
  return num1 + num2
}
```

### --question--

#### --text--
Koji će biti rezultat za sledeći kod?

```js
const developer = "Jessica";

function greet() {
  console.log("Hello, " + developer)
}

greet();
```

#### --distractors--
String `"Hello, developer"` će biti logovan na konzolu.

---

Poruka o grešci će se prikazati u konzoli.

---

Ništa se neće prikazati u konzoli.
#### --answer--
String `"Hello, Jessica"` će biti zapisano u konzolu.
### --question--

#### --text--
Šta se dešava ako pokušate da uklonite vitičaste zagrade za običnu funkciju?
#### --distractors--
Na stranici će se pojaviti prozor sa obaveštenjem o grešci.

---

Program će se srušiti i neće moći da se pokrene ponovo.

---

Ništa neće se desiti.
#### --answer--
Dobićete grešku sintakse.
### --question--

#### --text--
Šta će biti rezultat za sledeći kod?

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + 10);
```

#### --distractors--
Prikazaće se poruka o grešci u konzoli.

---

Broj `0` će biti logovan u konzolu.

---

Ništa će biti zapisano na konzolu.
#### --answer--
Broj `10` će biti logovan u konzolu.
### --question--

#### --text--
Koji će biti izlaz za sledeći kod?

```js
const exampleFunction = (param1, param2) => param1 + param2;
console.log(exampleFunction(3, "Something"));
```

#### --distractors--

`null`

---

`undefined`

---

`3 + "Something"`

#### --answer--

`"3Something"`

### --question--

#### --text--
Šta će biti rezultat za sledeći kod?

```js
const sum = (num1, num2) => num1 + num2
console.log(sum(0, 0) + num2);
```

#### --distractors--
Poruka o grešci koja navodi da `num1` nije definisana.

---

Broj `0` će biti prikazan u konzoli.

---

``undefined`` će biti prikazano u console-u.
#### --answer--
Poruka o grešci koja navodi da `num2` nije definisana.
### --question--

#### --text--
Koji će biti izlaz za sledeći kod?

```js
const divideTwoNumbers = (num1, num2) => num1 / num2;
console.log(divideTwoNumbers(3, 0));
```

#### --distractors--

`Error`

---

`0`

---

`null`

#### --answer--

`Infinity`

