---
id: 68376949899fcbad3c9cac23
title: JavaScript i Kviz o pristupačnosti
challengeType: 8
dashedName: quiz-js-a11y
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 9 od 10 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koji atribut se koristi u svrhe pristupačnosti da ukazuje da li je kontrolni element proširen ili skupljen?
#### --distractors--

`aria-controlled`

---

`aria-controls`

---

`aria-expands`

#### --answer--

`aria-expanded`

### --question--

#### --text--
Koje dve atributa mogu se koristiti u kombinaciji sa ``aria-expanded`` da uspostave programsko povezivanje između kontrolujućeg elementa i elementa koji on kontroliše?
#### --distractors--
`aria-labelledBy` and `aria-owns`

---

`aria-controls` and `aria-label`

---

`aria-live` and `aria-owns`
#### --answer--
`aria-controls` and `aria-owns`
### --question--

#### --text--
Koji od sledećih atributa kreira živo područje na vašoj stranici koje se može koristiti za obaveštavanje korisnika čitača ekrana o dinamičkim promenama sadržaja u tom području kako se dešavaju?
#### --distractors--

`aria-live-regions`

---

`aria-lives`

---

`aria-live-region`

#### --answer--

`aria-live`

### --question--

#### --text--
Koji od sledećih `div` elemenata će biti onemogućen samo za osobe koje koriste asistivne tehnologije, kao što su čitači ekrana?
#### --distractors--

```html
<div role="button" tabindex="-1" aria-disables="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disabling="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disable="true">Edit</div>
```

#### --answer--

```html
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

### --question--

#### --text--
Koji je validan način korišćenja atributa `aria-expanded`?
#### --distractors--

```html
<button aria-expanded="expand">Menu</button>
```

---

```html
<button aria-expanded="allow">Menu</button>
```

---

```html
<button aria-expanded="set">Menu</button>
```

#### --answer--

```html
<button aria-expanded="true">Menu</button>
```

### --question--

#### --text--
Koji atribut se koristi za kreiranje programskog odnosa između kontrolnog elementa i elementa koji utiče na stranici?
#### --distractors--

`aria-set`

---

`aria-region`

---

`aria-expanded`

#### --answer--

`aria-controls`

### --question--

#### --text--
Koji od sledećih NIJE uobičajena upotreba za live regions?
#### --distractors--
Poruke koje se prikazuju nakon obavljanja neke akcije (kao što je poruka o grešci ili potvrda).

---

Sadržaj koji se periodično ažurira (kao što su traka sa tekstom, marquee ili tajmer za brojanje).

---

Opšte poruke o statusu (poput ažuriranja procesa).
#### --answer--
Poruke koje se prikazuju pre nego što je izvršena neka akcija (kao što je dijalog za potvrdu).
### --question--

#### --text--
Koji od sledećih primera predstavlja ispravan način za označavanje kartice koja je trenutno izabrana/aktivna?
#### --distractors--

```html
<div role="tablist">
  <button role="tab" aria-selecting="true">Tab 1</button>
  <button role="tab" aria-selecting="false">Tab 2</button>
  <button role="tab" aria-selecting="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-select="true">Tab 1</button>
  <button role="tab" aria-select="false">Tab 2</button>
  <button role="tab" aria-select="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-selects="true">Tab 1</button>
  <button role="tab" aria-selects="false">Tab 2</button>
  <button role="tab" aria-selects="false">Tab 3</button>
</div>
```

#### --answer--

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
```

### --question--

#### --text--
Koji od sledećih nije važeća vrednost za atribut ``aria-live``?
#### --distractors--

`off`

---

`polite`

---

`assertive`

#### --answer--

`on`

### --question--

#### --text--
Koji od sledećih `aria-live` atributnih vrednosti znači da je ažuriranje veoma važno i ima najviši prioritet?
#### --distractors--

`now`

---

`immediate`

---

`urgent`

#### --answer--

`assertive`

