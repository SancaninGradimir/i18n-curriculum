---
id: 66edd630f7666cfa54b404d0
title: Asinhroni JavaScript kviz
challengeType: 8
dashedName: quiz-asynchronous-javascript
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Šta je asinhrono programiranje?
#### --distractors--
Metoda za sekvencijalno izvršavanje koda.

---

Tehnika za rukovanje greškama u vašem kodu.

---

Način za slanje mrežnih zahteva, obično radi preuzimanja ili slanja podataka na server.
#### --answer--
Metoda za izvršavanje koda konkurentno bez blokiranja glavnog niti.
### --question--

#### --text--
Šta je thread?
#### --distractors--
Funkcija koja se koristi za dohvaćanje podataka sa servera.

---

Program koji izvršava JavaScript kod u veb pretraživaču.

---

Poseban tip funkcije koja se prosleđuje kao argument drugoj funkciji.
#### --answer--
Niz instrukcija koji se može izvršiti nezavisno od glavnog toka programa.
### --question--

#### --text--
Koja je svrha Fetch API-ja u JavaScriptu?
#### --distractors--
Upravljanje asinhronim callbackovima.

---

Kreiranje i upravljanje kolačićima.

---

Prikazivanje dinamičkog HTML sadržaja.
#### --answer--
Za slanje mrežnih zahteva na servere.
### --question--

#### --text--
Koji HTTP metod se koristi za slanje podataka na server radi obrade?
#### --distractors--

`GET`

---

`DELETE`

---

`HEAD`

#### --answer--

`POST`

### --question--

#### --text--
Koji od sledećih načina je ispravan za povlačenje podataka sa API-ja?
#### --distractors--

```js
fetch('https://api.example.com/data')
  .accept(response => response.json())
  .accept(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .allow(response => response.json())
  .allow(data => console.log(data))
```

---

```js
fetch('https://api.example.com/data')
  .send(response => response.json())
  .send(data => console.log(data))
```

#### --answer--

```js
fetch('https://api.example.com/data')
  .then(response => response.json())
  .then(data => console.log(data))
```

### --question--

#### --text--
Šta je Promise u JavaScriptu?
#### --distractors--
Način za sinhronizovanje više operacija prilikom rada sa Fetch API-jem.

---

Poseban tip funkcije povratnog poziva koju morate koristiti u svim funkcijama višeg reda.

---

Tehnika koja se koristi za rukovanje greškama kada radite sa Fetch API-jem.
#### --answer--
Objekat koji predstavlja eventualno zaokruživanje (ili neuspeh) asinhrone operacije.
### --question--

#### --text--
Šta vam omogućava Promise chaining?
#### --distractors--
Omogućava vam da izvršite više sinhronih operacija paralelno.

---

Omogućava vam da obradite sinhrone operacije efikasnije.

---

Omogućava vam automatsko ponavljanje neuspešnih operacija.
#### --answer--
Omogućava vam da izvršite sekvencu asinhronih operacija jednu za drugom.
### --question--

#### --text--
Koja metoda se koristi za obradu uspešno završenog promise-a?
#### --distractors--

`.finally()`

---

`.catch()`

---

`.error()`

#### --answer--

`.then()`

### --question--

#### --text--
Koja metoda se koristi za rešavanje grešaka u lancu promisesa?
#### --distractors--

`.finally()`

---

`.then()`

---

`.error()`

#### --answer--

`.catch()`

### --question--

#### --text--
Koji od sledećih nije validan HTTP metod?
#### --distractors--

`PUT`

---

`GET`

---

`POST`

#### --answer--

`SEND`

### --question--

#### --text--
Koja od sledećih je ispravan sintaksa za kreiranje `async` funkcije?
#### --distractors--

`function myFunction() {}`

---

`function async myFunction() {}`

---

`async {} => ()`

#### --answer--

`async function myFunction() {}`

### --question--

#### --text--
Šta radi ključna reč ``await`` u asinhronoj funkciji?
#### --distractors--
Kreira novi Promise.

---

Izvršava kod odmah bez čekanja i vraća objekat.

---

Loguje rezultat Promise-a i vraća `null`.
#### --answer--
Zaustavlja izvršavanje funkcije dok se Promise ne razreši.
### --question--

#### --text--
Koji od sledećih API-ja se koristi za omogućavanje veb sajtovima da traže lokaciju korisnika?
#### --distractors--
Lokator API

---

Geografski API

---

Pronađi API
#### --answer--
Geolocation API
### --question--

#### --text--
Koji od sledećih atributa preuzima skriptu asinhrono, ali čeka da se HTML dokument potpuno parsira pre nego što pokrene izvršavanje skripte?
#### --distractors--

`asyncDefer`

---

`deferred`

---

`deferring`

#### --answer--

`defer`

### --question--

#### --text--
Šta će sledeći kod vratiti?

```js
fetch('https://api.example.com/data')
```

#### --distractors--
Greška koja ukazuje da su podaci korumpirani.

---

Greška koja ukazuje na to da se podaci ne mogu dohvatiti.

---

Neće se vratiti ništa i JavaScript će preći na sledeću funkciju u skripti.
#### --answer--
`Promise` koji se razrešava u `Response` objekat.
### --question--

#### --text--
Koji je podrazumevani HTTP metod za Fetch API?
#### --distractors--

`POST`

---

`PUT`

---

`DELETE`

#### --answer--

`GET`

### --question--

#### --text--
Koja od sledećih metoda se koristi za brisanje resursa sa servera?
#### --distractors--

`REMOVING`

---

`REMOVE`

---

`DELETING`

#### --answer--

`DELETE`

### --question--

#### --text--
Koja od sledećih metoda je ispravan način za kreiranje Promise-a?
#### --distractors--

```js
const promise = get Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = set Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

---

```js
const promise = put Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

#### --answer--

```js
const promise = new Promise((resolve, reject) => {
  setTimeout(() => {
    resolve('Data received successfully');
  }, 2000);
});
```

### --question--

#### --text--
Koji od sledećih atributa instruira pretraživač da asinhrono preuzme skript fajl, a pritom nastavi sa parsiranjem HTML dokumenta?
#### --distractors--

`resolve`

---

`catch`

---

`put`

#### --answer--

`async`

### --question--

#### --text--
Koji metod Geolocation API-a se koristi za dobijanje trenutne pozicije uređaja?
#### --distractors--

`getLocation()`

---

`getPosition()`

---

`getLatLong()`

#### --answer--

`getCurrentPosition()`

