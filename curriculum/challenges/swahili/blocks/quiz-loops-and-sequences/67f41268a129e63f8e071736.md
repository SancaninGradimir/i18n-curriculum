---
id: 67f41268a129e63f8e071736
title: Kviz: Petlje i Sekvence
challengeType: 8
dashedName: quiz-loops-and-sequences
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Koji od sledećih predstavlja tačnu sintaksu za listu?
#### --distractors--

```py
cities = //'Los Angeles', 'London', 'Tokyo'\\
```

---

```py
cities = {'Los Angeles', 'London', 'Tokyo'}
```

---

```py
cities = <'Los Angeles', 'London', 'Tokyo'>
```

#### --answer--

```py
cities = ['Los Angeles', 'London', 'Tokyo']
```

### --question--

#### --text--
Šta će se ispisati u konzolu?

```py
cities = ['Los Angeles', 'London', 'Tokyo']
print(cities[-1])
```

#### --distractors--

`'None'`

---

`'Los Angeles'`

---

`'London'`

#### --answer--

`'Tokyo'`

### --question--

#### --text--
Šta je iterabilni?
#### --distractors--
Ovo je poseban tip stringa koji može da primi veliki broj znakova.

---

To je poseban tip broja koji se koristi za predstavljanje veoma velikih vrednosti.

---

To je specijalni tip podataka koji se uglavnom koristi u klasama i posebnim funkcijama.
#### --answer--
To je poseban tip objekta preko kojeg se može iterirati po elementima, po jedan u jednom trenutku.
### --question--

#### --text--
Šta će sledeće da ispisa u konzolu?

```py
developer = 'Jessica'

print(list(developer))
```

#### --distractors--

`'J', 'e', 's', 's', 'i', 'c', 'a'`

---

`{'J', 'e', 's', 's', 'i', 'c', 'a'}`

---

`<'J', 'e', 's', 's', 'i', 'c', 'a'>`

#### --answer--

`['J', 'e', 's', 's', 'i', 'c', 'a']`

### --question--

#### --text--
Koji od sledećih se koristi za dobijanje broja elemenata u listi?
#### --distractors--

`list()`

---

`long()`

---

`length()`

#### --answer--

`len()`

### --question--

#### --text--
Šta se dešava ako pokušate da prosledite indeks (bilo pozitivan ili negativan) koji je van granica za listu?
#### --distractors--
Dobićete `SyntaxError`.

---

Dobićete `RangeError`.

---

Dobićete `TypeError`.
#### --answer--
Dobićete `IndexError`.
### --question--

#### --text--
Šta će se ispisati u konzolu?

```py
desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']
print(desserts[1:3])
```

#### --distractors--

`['Cake', 'Cookies', 'Ice Cream', 'Pie']`

---

`['Cake', 'Cookies']`

---

`['Cake', 'Cookies', 'Cookies', 'Ice Cream']`

#### --answer--

`['Cookies', 'Ice Cream']`

### --question--

#### --text--
Koja od sledećih metoda se koristi za dodavanje elementa na kraj liste?
#### --distractors--

`add()`

---

`pop()`

---

`push()`

#### --answer--

`append()`

### --question--

#### --text--
Šta će se ispisati u konzolu?

```py
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.extend(even_numbers)
print(numbers)
```

#### --distractors--

`[2, 3, 4, 5, 6, 8, 10]`

---

`[5, 6, 8, 10]`

---

`[1, 2, 3, 4]`

#### --answer--

`[1, 2, 3, 4, 5, 6, 8, 10]`

### --question--

#### --text--
Koji od sledećih metoda se koristi za umetanje elementa na određeni indeks u listu?
#### --distractors--

`push()`

---

`add()`

---

`place()`

#### --answer--

`insert()`

### --question--

#### --text--
Šta će se ispisati u konzolu?

```py
numbers = [1, 2, 3, 4, 5, 5, 5]
numbers.remove(5)

print(numbers)
```

#### --distractors--

`[1, 2, 3, 4]`

---

`[1, 2, 3, 4, 5, 5, 5]`

---

`[1, 2, 3, 4, 5]`

#### --answer--

`[1, 2, 3, 4, 5, 5]`

### --question--

#### --text--
Koji od sledećih metoda se koristi za pražnjenje liste?
#### --distractors--

`pop()`

---

`empty()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--
Šta je tupel?
#### --distractors--
To je izmenljivi tip podataka koji predstavlja nasumičnu sekvencu vrednosti.

---

To je specijalni tip podataka koji se koristi za predstavljanje velikih brojeva.

---

Radi se o metodi koja služi za dodavanje elemenata u listu.
#### --answer--
To je nepromenjiva, uređena sekvenca vrednosti.
### --question--

#### --text--
Koji je ispravan način za raspakivanje elemenata iz tupaola?
#### --distractors--

```py
developer = ('Alice', 34, 'Rust Developer')
(name, age, job) <= developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
<<name, age, job>> = developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job == developer
```

#### --answer--

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job = developer
```

### --question--

#### --text--
Šta će biti rezultat sledećeg koda?

```py
developer = ('Jane Doe', 23, 'Python Developer')
del developer[1]
```

#### --distractors--
To će proizvesti `RangeError`.

---

Ništa neće se desiti.

---

Kreiraće beskonačnu petlju.
#### --answer--
To će proizvesti `TypeError`.
### --question--

#### --text--
Koji će biti rezultat za sledeći kod?

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')
programming_languages.index('Python', 3)
```

#### --distractors--

`2`

---

`6`

---

`0`

#### --answer--

`5`

### --question--

#### --text--
Koju od sledećih funkcija koristi se za kreiranje nove liste sortiranih vrednosti?
#### --distractors--

`has_sort()`

---

`sort()`

---

`is_sorted()`

#### --answer--

`sorted()`

### --question--

#### --text--
Koja je ispravna metoda za korišćenje petlje `for`?
#### --distractors--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages [
    print(language)
]
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language of programming_languages:
    print(language)
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages {
    print(language)
}
```

#### --answer--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages:
    print(language)
```

### --question--

#### --text--
Koja od sledećih funkcija se koristi za generisanje niza celobrojnih brojeva?
#### --distractors--

`for()`

---

`int()`

---

`sequence()`

#### --answer--

`range()`

### --question--

#### --text--
Koja od sledećih funkcija se koristi za iteraciju preko više iterabilnih objekata paralelno?
#### --distractors--

`map()`

---

`filter()`

---

`reduce()`

#### --answer--

`zip()`

