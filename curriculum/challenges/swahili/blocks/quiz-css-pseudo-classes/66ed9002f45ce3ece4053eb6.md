---
id: 66ed9002f45ce3ece4053eb6
title: Kviz o CSS pseudoklasama
challengeType: 8
dashedName: quiz-css-pseudo-classes
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Šta su pseudo-klase?
#### --distractors--
Oni su CSS pravila koja kreiraju dodatni sadržaj unutar elementa kada je to potrebno.

---

One su svojstva koja prilagođavaju veličinu ili raspored elementa na stranici.

---

To su selektori koji se koriste za dodavanje efekata kretanja elementu tokom interakcija.
#### --answer--
To su ključne reči dodate selektoru koje stilizuju element na osnovu njegovog stanja.
### --question--

#### --text--
Koja pseudo-klasa se primenjuje kada je uređaj za pokazivanje pozicioniran iznad elementa?
#### --distractors--

`:focus`

---

`:active`

---

`:checked`

#### --answer--

`:hover`

### --question--

#### --text--
Koji pseudo-element vam omogućava da stilizujete prvo slovo pasusa?
#### --distractors--

`:first-letter`

---

`:first-child`

---

`:last-of-type`

#### --answer--

`::first-letter`

### --question--

#### --text--
Koji pseudo-class menja stil elementa dok se klikta na njega?
#### --distractors--

`:focus`

---

`:hover`

---

`:checked`

#### --answer--

`:active`

### --question--

#### --text--
Koji pseudo-klasa se koristi za stilizovanje elementa kada je spreman da primi unos korisnika, kao što je polje za tekst koje je kliknuto ili u koje je pređeno pomoću taba?
#### --distractors--

`::focus`

---

`:active`

---

`:visited`

#### --answer--

`:focus`

### --question--

#### --text--
Koji od sledećih CSS pravila ispravno dodaje tekst `Note:` ispred svakog paragraf elementa koji ima klasu `note`?
#### --distractors--

```css
.note::before {
  content: "Note:";
}
```

---

```css
p.note::after {
  content: "Note:";
}
```

---

```css
p::before {
  content: "Note:";
}
```

#### --answer--

```css
p.note::before {
  content: "Note:";
}
```

### --question--

#### --text--
Koja pseudo-klasa važi za polje za unos kada je izabrano ili uključeno?
#### --distractors--

`:required`

---

`:disabled`

---

`:optional`

#### --answer--

`:checked`

### --question--

#### --text--
Koji od sledećih je ispravna sintaksa za stilizovanje poslednjeg elementa u listi?
#### --distractors--

```css
li:nth-child(last) {
  color: blue;
}
```

---

```css
#li:last-child {
  color: blue;
}
```

---

```css
.li:last-child() {
  color: blue;
}
```

#### --answer--

```css
li:last-child {
  color: blue;
}
```

### --question--

#### --text--
Koja pseudo-klasa cilja polja za unos koja nisu obavezna za popunjavanje?
#### --distractors--

`:required`

---

`:enabled`

---

`::optional`

#### --answer--

`:optional`

### --question--

#### --text--
Šta radi pseudo-klasa `:disabled`?
#### --distractors--
Stilizuje označena input polja.

---

Stilizuje elemente preko kojih se prelazi mišem.

---

Izabira elemente koji ne odgovaraju datoj selekciji/selektoru.
#### --answer--
Stilizuje elemente koji nisu dostupni za interakciju korisnika.
### --question--

#### --text--
Koja pseudo-klasa se primenjuje kada polje forme ispuni svoje kriterijume validacije?
#### --distractors--

`:checked`

---

`:required`

---

`:disabled`

#### --answer--

`:valid`

### --question--

#### --text--
Koji od ovih nije pseudoklas lokacije?
#### --distractors--

`:visited`

---

`:any-link`

---

`:link`

#### --answer--

`:current`

### --question--

#### --text--
# --description--
Koji od sledećih selektuje treću stavku liste?
#### --distractors--

```css
li:child(3) {
  color: red;
}
```

---

```css
li:last-child(3) {
  color: red;
}
```

---

```css
li:nth-child(three) {
  color: red;
}
```

#### --answer--

```css
li:nth-child(3) {
  color: red;
}
```

### --question--

#### --text--
Koji elementi će imati `color` od `blue` sa sledećim CSS-om?

```css
p:is(.blue, .highlight) {
  color: blue;
}
```

#### --distractors--

```html
<p class="class">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

---

```html
<div class="blue">Paragraph 1</div>
<div class="highlight">Paragraph 2</div>
```

---

```html
<p>Paragraph 1</p>
<span class="highlight">Paragraph 2</span>
```

#### --answer--

```html
<p class="blue">Paragraph 1</p>
<p class="highlight">Paragraph 2</p>
```

### --question--

#### --text--
Šta radi pseudo-klasa `:not()`?
#### --distractors--
Dodaje stilove svim elementima.

---

Selektuje sve elemente koji su deca roditeljskog elementa.

---

Izabira elemente koji odgovaraju datoj selektoru.
#### --answer--
Izdvaja elemente koji ne odgovaraju datoj selekciji/selektoru.
### --question--

#### --text--
Šta radi sledeće CSS pravilo?

```css
p:first-of-type {
  font-style: italic;
}
```

#### --distractors--
Izabira prvi element `p` u dokumentu.

---

Izabira sve elemente ``p`` u dokumentu.

---

Izabira prvi potomak svakog `p` elementa.
#### --answer--
Izdvaja prvi `p` element unutar roditeljskog kontejnera.
### --question--

#### --text--
Šta radi pseudo klasa ``:last-of-type``?
#### --distractors--
Selektuje prvi element deteta određenog tipa unutar svog roditeljskog elementa.

---

Selektuje srednji element deteta specifičnog tipa unutar svog roditeljskog elementa.

---

Izabira svaki potomak element određenog tipa unutar svog roditeljskog elementa.
#### --answer--
Selektuje poslednji element određenog tipa među svojim potomcima unutar roditeljskog elementa.
### --question--

#### --text--
Koja pseudo-klasa se koristi za selekciju drugog elementa u listi?
#### --distractors--

`:first-child`

---

`:required`

---

`:is()`

#### --answer--

`:nth-child(2)`

### --question--

#### --text--
Koji od ovih je funkcionalna pseudo-klasa?
#### --distractors--

`:first-child`

---

`:match()`

---

`:checked`

#### --answer--

`:is()`

### --question--

#### --text--
Koji od ovih nije funkcionalna pseudo-klasa?
#### --distractors--

`:has()`

---

`:not()`

---

`:where()`

#### --answer--

`:contains()`

## --quiz--

### --question--

#### --text--
Koja pseudo-klasa se koristi za ciljanje elemenata forme koji su omogućeni?
#### --distractors--

`:disabled`

---

`:active`

---

`:focus`

#### --answer--

`:enabled`

### --question--

#### --text--
Koja pseudo-klasa vam omogućava da selektujete elemente brojeći od kraja?
#### --distractors--

`:nth-child(n)`

---

`:last-child`

---

`:last-of-type`

#### --answer--

`:nth-last-child(n)`

### --question--

#### --text--
Koja od sledećih opcija vam omogućava da izaberete elemente koji sadrže određene podelemente?
#### --distractors--

`:is()`

---

`:where()`

---

`:in-range`

#### --answer--

`:has()`

### --question--

#### --text--
Koji od sledećih selektuje elemente koji ne sadrže nikakav sadržaj ili potomke elemente?
#### --distractors--

`:only-child`

---

`:last-child`

---

`:not()`

#### --answer--

`:empty`

### --question--

#### --text--
Šta cilja ovaj CSS selektor?

```css
input:invalid {
  background-color: red;
}
```

#### --distractors--
Svi elementi za unos.

---

Svi elementi unosa sa vrednostima u dozvoljenom opsegu.

---

Svi elementi unosa koji prolaze validaciju.
#### --answer--
Svi elementi unosa koji ne prođu validaciju.
### --question--

#### --text--
Koja pseudo-klasa selektuje polja za unos čija je vrednost automatski popunjena od strane pregledača?
#### --distractors--

`:visited`

---

`:valid`

---

`:where()`

#### --answer--

`:autofill`

### --question--

#### --text--
Koji pseudo-class selektuje element ako je on ili bilo koji od njegovih potomaka fokusiran?
#### --distractors--

`:focus`

---

`:in-range`

---

`:only-child`

#### --answer--

`:focus-within`

### --question--

#### --text--
Koji pseudo klas predstavlja linkove koji ukazuju na isti dokument?
#### --distractors--

`:target`

---

`:visited`

---

`:link`

#### --answer--

`:local-link`

### --question--

#### --text--
Koja od sledećih opcija stilizuje element `p` kada je odredište fragmenta URL-a?
#### --distractors--

```css
p:empty {
  background-color: gold;
}
```

---

```css
p:not(.targeted) {
  background-color: gold;
}
```

---

```css
p:is(.target) {
  background-color: gold;
}
```

#### --answer--

```css
p:target {
  background-color: gold;
}
```

### --question--

#### --text--
Koja pseudo-klasa se koristi kada je element cilj (target) URL fragmenata?
#### --distractors--

`:focus-within`

---

`:hover`

---

`:visited`

#### --answer--

`:target`

### --question--

#### --text--
Šta selektuje pseudo-klasa `:only-child`?
#### --distractors--
Selektuje roditeljski element koji ima samo jedno dete.

---

Selektuje sve potomke elemente unutar roditeljskog elementa.

---

Selektuje roditeljski element koji sadrži samo jedan tip deteta.
#### --answer--
Selektuje element koji nema braće i sestre unutar svog roditeljskog elementa.
### --question--

#### --text--
Koja pseudo-klasa selektuje element ako je jedini svog tipa unutar svog roditeljskog elementa?
#### --distractors--

`:only-child`

---

`:nth-of-type(n)`

---

`:first-of-type`

#### --answer--

`:only-of-type`

### --question--

#### --text--
Koje CSS pravilo će primeniti `color` od `yellow` na drugi `p` element u sledećem HTML-u?

```html
<div>
  <h1>Courses</h1>
  <p>HTML</p>
  <p>CSS</p>
  <p>JavaScript</p>
</div>
```

#### --distractors--

```css
p:nth-child(2) {
  color: yellow;
}
```

---

```css
p:first-of-type {
  color: yellow;
}
```

---

```css
p:last-of-type {
  color: yellow;
}
```

#### --answer--

```css
p:nth-of-type(2) {
  color: yellow;
}
```

### --question--

#### --text--
Koji pseudo-element vam omogućava da selektujete markere stavki liste radi stilizovanja?
#### --distractors--

`::before`

---

`::after`

---

`:root`

#### --answer--

`::marker`

### --question--

#### --text--
Koji pseudo-klasa vam omogućava da ciljate najviši nivo element u dokumentu, obično `html` element?
#### --distractors--

`:first-child`

---

`:in-range`

---

`:target`

#### --answer--

`:root`

### --question--

#### --text--
Koja CSS pseudo-klasa ima specifičnost od nule, što garantuje da neće interferisati sa drugim stilskim pravilima?
#### --distractors--

`:is()`

---

`:not()`

---

`:focus`

#### --answer--

`:where()`

### --question--

#### --text--
Koji od sledećih je pseudo-klasa strukturalna po stablu?
#### --distractors--

`:where()`

---

`:valid`

---

`:link`

#### --answer--

`:root`

### --question--

#### --text--
Koji CSS pravilnik će postaviti ``background-color`` navedenog elementa na ``red`` ako je njegova vrednost izvan definisanog opsega?

```html
<input type="number" min="10" max="25"/>
```

#### --distractors--

```css
input:in-range {
  background-color: red;
}
```

---

```css
input {
  background-color: red;
}
```

---

```css
input:valid {
  background-color: red;
}
```

#### --answer--

```css
input:out-of-range {
  background-color: red;
}
```

### --question--

#### --text--
Koji pseudo-element koristi property `content` da ubaci sadržaj nakon elementa?
#### --distractors--

`::before`

---

`::marker`

---

`::first-letter`

#### --answer--

`::after`

### --question--

#### --text--
Koja pseudoklasa primjenjuje stilove na element ako je njegova vrijednost unutar navedenog raspona?
#### --distractors--

`:out-of-range`

---

`:enabled`

---

`:checked`

#### --answer--

`:in-range`
