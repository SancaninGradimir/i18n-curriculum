---
id: 66f1b06a5a5d10cc100af620
title: Git kviz
challengeType: 8
dashedName: quiz-git
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Šta je `git`?
#### --distractors--
Popularan alat za pristupačnost.

---

Linter koji se često koristi u softverskim projektima.

---

Poseban tip JavaScript biblioteke.
#### --answer--
Sistem za kontrolu verzija omogućava vam da pratite i upravljate promenama u vašem projektu.
### --question--

#### --text--
Koji od sledećih je popularni cloud-bazirani provajder za kontrolu verzija?
#### --distractors--
Spring Boot

---

Laravel

---

Remiks
#### --answer--
GitHub
### --question--

#### --text--
Šta radi komanda ``git config``?
#### --distractors--
Postavlja varijable konfiguracije za rebaseovanje vaših promena.

---

Postavlja konfiguracije za upravljanje samo udaljenim repozitorijumima na GitLab-u.

---

Definiše konfiguracije za rešavanje konflikata spajanja (merge conflicts).
#### --answer--
Postavlja varijable konfiguracije koje određuju kako Git funkcioniše na vašoj mašini.
### --question--

#### --text--
Koji komanda prikazuje trenutno instaliranu verziju Gita na vašoj mašini?
#### --distractors--

`git --versions`

---

`git --setVersion`

---

`git --set`

#### --answer--

`git --version`

### --question--

#### --text--
Koja od sledećih je alat koji se koristi za obavljanje zadataka specifičnih za GitHub bez napuštanja komandne linije?
#### --distractors--
GitHub CI/CD

---

GitHub Actions

---

GitHub Pages
#### --answer--
GitHub CLI
### --question--

#### --text--
Koja od sledećih je funkcija koja vam omogućava da automatizujete radne tokove direktno u vašem GitHub repozitorijumu, uključujući izgradnju (building), testiranje i implementaciju (deploying) vašeg koda?
#### --distractors--
GitHub Pipelines

---

GitHub Cloud

---

GitHub API
#### --answer--
GitHub Actions
### --question--

#### --text--
Koju od sledećih komandi treba koristiti za inicijalizovanje praznog `git` repozitorijuma?
#### --distractors--

`git status`

---

`git set`

---

`git log`

#### --answer--

`git init`

### --question--

#### --text--
Šta radi komanda ``git push``?
#### --distractors--
Ova komanda se koristi za pushovanje cele istorije Git-a na GitHub.

---

Ova komanda se koristi za guranje promena sa udaljenog repozitorijuma na vašu lokalnu mašinu.

---

Ova komanda se koristi za slanje vašeg koda na GitHub i automatsko spajanje istog u granu `main`.
#### --answer--
Ova komanda se koristi za slanje vaših lokalnih commit-ova na udaljeni repozitorijum.
### --question--

#### --text--
Koji od sledećih komandi se koristi za kloniranje repozitorijuma?
#### --distractors--

`git cloning`

---

`git cloned`

---

`git clones`

#### --answer--

`git clone`

### --question--

#### --text--
Koji od sledećih komandi se koristi za listanje svih prethodnih commit-ova sa korisnim informacijama kao što su autor, datum commita, poruka commita i commit hash?
#### --distractors--

`git rebase`

---

`git status`

---

`git commit`

#### --answer--

`git log`

### --question--

#### --text--
Koji od sledećih komandi će listati sve udaljene repozitorijume povezane sa vašim lokalnim Git repozitorijumom?
#### --distractors--

`git rebase -v`

---

`git pull -v`

---

`git checkout -v`

#### --answer--

`git remote -v`

### --question--

#### --text--
Šta radi komanda ``git reset``?
#### --distractors--
Ova komanda vam omogućava da resetujete sve pokušaje rešavanja konflikata spajanja (merge conflicts).

---

Ova komanda vam omogućava da resetujete sve pokušaje za `git rebase`.

---

Ova komanda vam omogućava da resetujete sve commit-ove za repozitorijum.
#### --answer--
Ova komanda vam omogućava da resetujete trenutno stanje grane.
### --question--

#### --text--
Šta je pull request?
#### --distractors--
Zahtev za kombinovanje niza commit-ova sa jedne grane na drugu.

---

Zahtev za rešavanje svih konflikata spajanja.

---

Zahtev za uklanjanje svih commit-ova iz repozitorijuma.
#### --answer--
Zahtev za povlačenje promena sa vaše grane u ciljnu granu.
### --question--

#### --text--
Šta grana `main` predstavlja u projektu?
#### --distractors--
To predstavlja glavnu granu dokumentacije za aplikaciju.

---

Predstavlja hotfix granu za aplikaciju.

---

Predstavlja glavnu granu funkcionalnosti za aplikaciju.
#### --answer--
Predstavlja glavnu ili produkcionu granu za aplikaciju.
### --question--

#### --text--
Za šta se najbolje koriste GitHub Pages?
#### --distractors--
Najbolje je razvijati igre sa C# i Unity-jem.

---

Ovo je najbolje za razlaganje aplikacija koje koriste samo Python.

---

Najbolje je za *deployment*-ovanje web sajtova koji su intenzivni po backendu.
#### --answer--
Najbolje je za deployovanje statičnih sajtova.
### --question--

#### --text--
Koju od sledećih komandi se koristi za prikazivanje trenutnog stanja vašeg radnog direktorijuma?
#### --distractors--

`git show`

---

`git check`

---

`git update`

#### --answer--

`git status`

### --question--

#### --text--
Koju komandu koristite za povlačenje promena sa udaljenog repozitorijuma?
#### --distractors--

`git get`

---

`git sync`

---

`git update`

#### --answer--

`git fetch`

### --question--

#### --text--
Šta je fajl `.gitignore`?
#### --distractors--
Poseban tip fajla koji se koristi za prikaz vašu celokupnu istoriju `git`.

---

Specijalni tip fajla koji se koristi za prikaz razlika (diffs) u vašim promenama.

---

Poseban tip datoteke koji se koristi za razrešavanje konflikata u projektu.
#### --answer--
Poseban tip fajla koji služi za reći `git` da ne prati određene fajlove i foldere u projektu.
### --question--

#### --text--
Koja od sledećih komandi se koristi za listanje svih vaših lokalnih grana?
#### --distractors--

`git show`

---

`git log`

---

`git list`

#### --answer--

`git branch`

### --question--

#### --text--
Koja od sledećih komandi se koristi za prebacivanje ili kombinovanje niza commit-ova sa jedne grane na drugu?
#### --distractors--

`git remote`

---

`git merge`

---

`git switch`

#### --answer--

`git rebase`

