---
id: 6977a8bbcbdd557886bd0718
title: Korak 7
challengeType: 20
dashedName: step-7
---

# --description--

Sa uključenim naplati, imate konačan iznos za celu grupu. Morate utvrditi koliko svaki pojedinac duguje tako što ćete ukupan račun podeliti sa brojem prijatelja.

U Pythonu, koristite napredni kosac `/` za deljenje. Na primer:

```py
half = 10 / 2
```

Kreirajte varijablu pod nazivom `final_bill` i dodelite joj rezultat deljenja `running_total` sa `num_of_friends`.

Na kraju, koristite funkciju `print()` da prikažete niz (string) `Bill per person:` praćen razmakom i vrednošću `final_bill`.

# --hints--

You should define a variable named `final_bill`.

```js
({
    test: () => assert(runPython(`
    _Node(_code).has_variable('final_bill')
    `))
})
```

You should use the `/` operator to divide `running_total` by `num_of_friends` and assign the result to your `final_bill` variable.

```js
({
    test: () => assert(runPython(`
    _Node(_code).find_variable('final_bill').is_equivalent('final_bill = running_total / num_of_friends')
    `))
})
```

You should print the string `Bill per person:` followed by a space and the `final_bill` variable.

```js
({
    test: () => runPython(`
import io, contextlib, re

buffer = io.StringIO()
with contextlib.redirect_stdout(buffer):
    exec(_code)

match = re.search(r"Bill per person: ([0-9]+(?:\\.[0-9]+)?)", buffer.getvalue())

assert match
assert abs(float(match.group(1)) - (((37.89 + 57.34 + 39.39 + 64.21) * 1.25) / 4)) < 1e-6
`)
})
```

# --seed--

## --seed-contents--

```py
running_total = 0

num_of_friends = 4

appetizers = 37.89
main_courses = 57.34
desserts = 39.39
drinks = 64.21

running_total += appetizers + main_courses + desserts + drinks
print('Total bill so far:', running_total)

tip = running_total * 0.25
print('Tip amount:', tip)

running_total += tip
print('Total with tip:', running_total)

--fcc-editable-region--

--fcc-editable-region--
```
