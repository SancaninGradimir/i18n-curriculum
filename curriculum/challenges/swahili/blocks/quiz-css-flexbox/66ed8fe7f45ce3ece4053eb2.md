---
id: 66ed8fe7f45ce3ece4053eb2
title: CSS Flexbox Kviz
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Šta je flexbox?
#### --distractors--
Dvodimenzionalni model za raspored.

---

Trodimenzionalni model za raspored.

---

Višedimenzionalni model za raspored.
#### --answer--
Jednodimenzionalni model za raspoređivanje.
### --question--

#### --text--
Koje je CSS svojstvo postavljeno za omogućavanje flexbox rasporeda za element `div`?
#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--
Koje CSS svojstvo omogućava flex elementima da se prebace na novi red kada je trenutni red pun?
#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--
Šta svojstvo flexbox `justify-content` određuje?
#### --distractors--
Kontroliše smer elemenata unutar flexbox kontejnera.

---

Upravlja stilom teksta detetkih elemenata fleks-kontejnera.

---

Kontroliše poravnanje dečijih elemenata unutar fleks kontejnera.
#### --answer--
Upravlja raspodelom elemenata potomaka na glavnoj osi.
### --question--

#### --text--
Koje svojstvo određuje pravac elemenata unutar flexbox kontejnera?
#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--
Koja CSS svojstva mogu se koristiti da bi flex stavke tečale od leve ka desnoj, i da nova flex stavka pojavi iznad prethodne ako je širina trenutnog reda prešla?
#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--
Koje svojstvo kontroliše pozicioniranje elemenata na poprečnoj osi?
#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--
Koja nije vrednost za `justify-content`?
#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--
Koja CSS svojstva biste koristili da centrirate element unutar flex kontejnera horizontalno i vertikalno?
#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--
Koja vrednost `flex-direction` čini da elementi fleks-kontejnera idu iz desna u levu?
#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--
Koja vrednost nije validna za svojstvo `flex-wrap`?
#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--
Šta radi vrednost `stretch` u `align-items`?
#### --distractors--
Predmeti su smešteni direktno jedan uz drugoga, ka početnoj ivici kontejnera.

---

Elementi su poravnati tako da linije bazne njihovog flex container-a budu poravnate.

---

Predmeti su upakovani direktno jedan uz drugoga ka krajnjoj ivici kontejnera.
#### --answer--
Automatsko podešavanje veličine stavki tako da budu jednake i popune kontejner.
### --question--

#### --text--
Koja `justify-content` vrednost postavlja prvi element na početak, poslednji element na kraj i ravnomerno raspoređuje ostale elemente između njih duž glavne ose?
#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--
Koja `justify-content` vrednost razmakljuje elemente tako da su praznine između svake pare susednih elemenata i ivice kontejnera jednake?
#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--
Koji `justify-content` vrednost ravnomerno raspoređuje elemente duž glavne ose, ostavljajući pola tog prostora na ivicama kontejnera?
#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--
Koja od sledećih opcija rezultira poravnanjem stavki na početak poprečne ose?
#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--
Koji od sledećih rezultata poravnava elemente na kraj poprečne ose?
#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--
Koju vrednost za ``flex-direction`` treba koristiti kako bi se elementi poravnati duž glavne ose, od leve ka desnoj?
#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--
Koja vrednost za `flex-direction` će učiniti da su elementi poravnati duž poprečne ose od vrha do dna?
#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--
Koji od sledećih scenarija nije pogodan za flexbox?
#### --distractors--
Centriranje elementa unutar kontejnera.

---

Vertikalno poravnanje stavki u redu.

---

Dinamičko prilagođavanje rasporeda i poravnanja elemenata za različite viewport-ove.
#### --answer--
Popravljena veličina i razmak između stavki, slično mrežnom rasporedu.

## --quiz--

### --question--

#### --text--
Koja je svrha jednodimenzionalnog modela raspoređivanja poput Flexbox-a?
#### --distractors--
Za obradu rasporeda u dve dimenzije, istovremeno obrađujući redove i kolone.

---

Za kontrolu redosleda složene pozicioniranih elemenata korišćenjem `z-index`.

---

Za kreiranje 3D transformacija i kompleksnih animacija na elementima stranice.
#### --answer--
Za raspoređivanje elemenata duž jedne linije ili ose, bilo kao red ili kao kolonu.
### --question--

#### --text--
Koji CSS svojstvo poravnava sve fleksibilne elemente na desnu stranu elementa `.container`?

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--
Koje su dve ose koje određuju pravac raspoređivanja elemenata u Flex modelu?
#### --distractors--
Horizontalna osa i vertikalna osa.

---

X osa i Y osa.

---

Primarna osa i sekundarna osa.
#### --answer--
Glavna osa i poprečna osa.
### --question--

#### --text--
Koji od sledećih CSS pravila će rasporediti elemente u vertikalnu kolonu i tako ih centrirati horizontalno unutar svog kontejnera?
#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--
Koja je glavna svrha svojstva `flex-wrap`?
#### --distractors--
Definiše smer u kojem se flex elementi postavljaju unutar flex kontejnera.

---

Poravnava flex elemente duž glavne ose kontejnera.

---

Poravnava flex elemente duž poprečne ose kontejnera.
#### --answer--
Određuje da li fleks elementi treba da pređu na nove linije kako bi odgovarali dostupnom prostoru.
### --question--

#### --text--
Koje je podrazumevano ponašanje deklaracije `flex-wrap: nowrap;`?
#### --distractors--
Prisiljava flex elemente da se prebace na novu liniju ako su preširoki za kontejner.

---

Prebacuje stavke na novi red, ali u opškom poretku.

---

To sprečava fleks kontejner da bude veći od svog roditeljskog elementa.
#### --answer--
Ovo prisiljava flex elemente da ostanu na jednoj liniji, čak i ako prevrlaju kontejner.
### --question--

#### --text--
Koje CSS svojstvo je skraćeni zapis za postavljanje oba svojstva `flex-direction` i `flex-wrap` odjednom?
#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--
Koji je efekat postavljanja `flex-direction: column-reverse;` na fleks kontejneru?
#### --distractors--
Raspoređuje stavke horizontalno, od desne ka levej.

---

Obrnjava stavke u redu, ali ih zadržava na jednoj liniji.

---

Raspoređuje stavke vertikalno, odozgo nadole.
#### --answer--
Raspoređuje stavke vertikalno u obrnutom redosledu, od dna ka vrhu.
### --question--

#### --text--
Koji CSS kodni deo pravilno postavlja flex elemente u vertikalnu kolonu i omogućava im da se prebace na novu kolonu ako je visina kontejnera premala?
#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--
Koja `justify-content` vrednost poravnava sve flex elemente na kraj glavne ose kontejnera?
#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--
Koji je efekat sledećeg CSS pravila na fleksibilne elemente?

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--
Elementi su poređani u koloni i će se prebacivati na novu kolonu.

---

Elementi su poređeni u red i prebacuju se (ili "savijaju") na novi red.

---

Stavci su poređani u koloni i će se previše prikazati ako ne stanu.
#### --answer--
Elementi su poređani u red i prelivaju će kontejner ukoliko ne stanu.
### --question--

#### --text--
Kako `justify-content: space-evenly;` poravna flekse elemente?

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--
Stavci su ravnomerno razmakovani, bez razmaka na početku ili kraju.

---

Predmeti su upakovani zajedno u sredinu kontejnera.

---

Elementi se poravnavaju na desnu stranu kontejnera, sa jednakim razmakom između njih.
#### --answer--
Predmeti imaju jednak razmak između sebe i na oba kraja kontejnera.
### --question--

#### --text--
Koji atribut biste koristili za vertikalno centriranje elemenata kada je `flex-direction: row;` definisano?
#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--
Šta radi vrednost `flex-start` za svojstvo `justify-content`?
#### --distractors--
Poravnava stavke na početak poprečne ose.

---

Razvlači stavke da popuni glavnu osu kontejnera.

---

Centriše sve elemente duž glavne ose.
#### --answer--
Poravnava elemente na početak glavne ose kontejnera.
### --question--

#### --text--
Kako biste postavili fleks kontejner da rasporedi svoje stavke od desna ka levo na jednoj liniji, i takođe ih pozicionirali na vrhu kontejnera?
#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--
Koji od sledećih predstavlja validnu deklaraciju za svojstvo ``flex-flow``?
#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--
Koje od sledećih CSS pravila će rezultirati da se flex stavke centriraju horizontalno i istegnu vertikalno kako bi popunile visinu kontejnera?
#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--
Koji uticaj ima `align-items: stretch;` kada se `flex-direction` postavi na `column`?
#### --distractors--
Vertikalno proteže elemente kako bi popunili visinu kontejnera.

---

Nema uticaja kada je smer `column`.

---

Vertikalno proteže elemente kako bi odgovaralo najvišem elementu.
#### --answer--
Razteže stavke horizontalno kako bi popunile širinu kontejnera.
### --question--

#### --text--
Ako želite da obrnete redosled stavki u horizontalnom redu, koju deklaraciju bi trebalo koristiti?
#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--
Koja vrednost od `align-items` će pozicionirati elemente na vrhu kontejnera čije je `flex-direction` postavljeno/jednakom `row`?
#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`
