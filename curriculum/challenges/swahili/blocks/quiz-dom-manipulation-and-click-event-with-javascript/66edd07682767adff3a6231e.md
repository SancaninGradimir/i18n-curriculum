---
id: 66edd07682767adff3a6231e
title: Kviz za Manipulaciju DOM-om i Događajima Klika sa JavaScript-om
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Šta je Document Object Model?
#### --distractors--
Jezik programiranja koji se koristi za interakciju sa HTML stranicama.

---

Skup objekata koji sadrže samo brojeve sa plutajućom zarezom.

---

Skup JavaScript klasa koji predstavljaju HTML elemente.
#### --answer--
Programski interfejs koji vam omogućava da interagujete sa HTML dokumentima.
### --question--

#### --text--
Šta znači API?
#### --distractors--
Dodatni programski interfejs

---

Dodavanje Programskog Interfejsa

---

Drugi programski interfejs
#### --answer--
Interfejs za programiranje aplikacija
### --question--

#### --text--
Kako možete koristiti JavaScript da pronađete sve HTML ankre kojima nedostaju hiperlinkovi?
#### --distractors--

```js
document.querySelectorAll("link![href]");
```

---

```js
document.querySelectorAll("link:not([href])");
```

---

```js
document.querySelectorAll("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--
Šta radi svojstvo `textContent`?
#### --distractors--
Uklanja tekstualni sadržaj iz svih HTML elemenata.

---

Uklanja tekstualni sadržaj iz vašeg JavaScript fajla.

---

Dodaje tekst svim nizovima i objektima u vašoj JavaScript datoteci.
#### --answer--
Vraća tekstualni sadržaj elementa u čistom formatu.
### --question--

#### --text--
Šta radi svojstvo `innerHTML`?
#### --distractors--
To je svojstvo ``Element`` koje se koristi za dobijanje celokupnog tekstualnog sadržaja za HTML element.

---

To je svojstvo `Element` koje se koristi za dodavanje anotacija vašem HTML-u.

---

To je svojstvo `Element` koje se koristi za pokretanje događaja u vašem HTML-u.
#### --answer--
Radi se o svojstvu ``Element`` koje se koristi za postavljanje ili ažuriranje delova HTML markupa.
### --question--

#### --text--
Koji od sledećih će dodati validan event listener?
#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--
Koji od sledećih NIJE važeća metoda objekta `document`?
#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()` 

#### --answer--

`addElementToDOM()`

### --question--

#### --text--
Koja od sledećih će postaviti atribut `class` na `my-class`?
#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--
Šta je objekat `Event`?
#### --distractors--
To je poseban objekat koji se koristi za uklanjanje događaja iz vašeg JavaScript projekta.

---

Služi za automatsko dodavanje događaja u vaše JavaScript projekte.

---

Ovo je poseban objekat koji se koristi prilikom rada sa nizovima.
#### --answer--
To je payload koji se pokreće kada korisnik na neki način interaguje sa vašom veb stranicom.
### --question--

#### --text--
Koji od sledećih je metoda korišćena za uklanjanje slušalice događaja koja je prethodno dodata na element koristeći metodu `addEventListener()`?
#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--
Šta su inline hendleri za događaje?
#### --distractors--
Specijalni atributi na HTML elementu koji se koriste za poništavanje svih događaja.

---

Specijalni atributi na HTML elementu koji se koriste samo za pokretanje slanja forme u vašem JavaScript kodu.

---

Specijalni atributi na HTML elementu koji se koriste za uklanjanje događaja.
#### --answer--
Posebni atributi na HTML elementu koji se koriste za izvršavanje JavaScript koda kada dođe do događaja.
### --question--

#### --text--
Koje su dve glavne kategorije za web API-je?
#### --distractors--
API-ji za Javu i API-ji trećih strana.

---

API-ji pregledača i Rust API-ji.

---

Sistemski API-ji i API-ji trećih strana.
#### --answer--
APIs pregledača i API trećih strana.
### --question--

#### --text--
Koji događaj se aktivira kada su svi elementi HTML stranice uspešno parsirani od strane pregledača?
#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--
Koje od sledećih je svojstvo koje predstavlja inline stil elementa?
#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--
Koji od sledećih načina je ispravan za korišćenje inline event handler-a?
#### --distractors--

`<button eventclick="validate()">Validate</button>`

---

`<button click="validate()">Validate</button>`

---

`<button clickevent="validate()">Validate</button>`

#### --answer--

`<button onclick="validate()">Validate</button>`

### --question--

#### --text--
Koja od sledećih metoda se koristi za kašnjenje akcije na određeno vreme?
#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--
Koji od sledećih se može koristiti za kreiranje i kontrolisanje animacija direktno unutar JavaScript-a?
#### --distractors--
Omogućite Animations API

---

Upravljanje animacijama API

---

Kreiraj Animations API
#### --answer--
Web Animations API
### --question--

#### --text--
Šta radi Canvas API?
#### --distractors--
Koristi se za animaciju grafikona u vašem CSS fajlu.

---

Koristi se samo za uklanjanje grafičkih elemenata iz vašeg JavaScript fajla.

---

Koristi se za uklanjanje grafike iz vašeg HTML fajla.
#### --answer--
Koristi se za manipulisanje grafike putem vašeg JavaScript fajla.
### --question--

#### --text--
Šta je delegacija događaja?
#### --distractors--
Proces slušanja događaja kada je događaj otkazan/poništen.

---

Proces slušanja događaja koji se dešavaju prilikom klikovanja.

---

Proces slušanja događaja kada se dogodi promena.
#### --answer--
Proces slušanja događaja koji su se propagirali do roditeljskog elementa.
### --question--

#### --text--
Koja od sledećih opcija NIJE metoda koju možete koristiti prilikom rada sa dijalogima i modalima?
#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`

