---
id: 67358be1c7903489c0a7db78
title: Quiz o JavaScript Map-ovima i Set-ovima
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--
Da biste položili kviz, morate tačno odgovoriti na najmanje 18 od 20 pitanja ispod.
# --quizzes--

## --quiz--

### --question--

#### --text--
Šta je Set u JavaScript-u?
#### --distractors--
Struktura podataka koja dozvoljava duplirane vrednosti.

---

Struktura podataka koja skladišti samo stringove i brojeve.

---

Kolekcija čiji elementi ne mogu biti obrisani.
#### --answer--
Kolekcija u kojoj svaka vrednost može biti prisutna samo jednom.
### --question--

#### --text--
Koji od sledećih metoda je dostupan u Maps-u, ali ne i u Sets-u?
#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--
Koji metod se koristi za dodavanje elemenata u Set?
#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--
Kako proveriti da li Set sadrži određenu vrednost?
#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--
Koji od ovih predstavlja odličan slučaj upotrebe za Setove?
#### --distractors--
Upravljanje parovima ključ-vrednost u bazi podataka.

---

Kreiranje porednih lista stavki.

---

Skladištenje hijerarhijskih struktura podataka u lokalnom skladištu pretraživača.
#### --answer--
Uklanjanje duplikata iz niza.
### --question--

#### --text--
Šta je WeakSet u JavaScript-u?
#### --distractors--
Kolekcija slična Set-u koja dozvoljava samo primitivne tipove podataka.

---

Kolekcija slična Setu koja dozvoljava duplikate vrednosti.

---

Kolekciona struktura slična Setu koja sprečava uklanjanje vrednosti.
#### --answer--
Kolekcija slična Setu koja skladišti samo objekte.
### --question--

#### --text--
Koja je jedna razlika između Set-a i WeakSet-a?
#### --distractors--
WeakSet može da skladišti bilo kakvu vrstu vrednosti, uključujući brojeve.

---

`Set` ne dozvoljava duplikate vrednosti, ali `WeakSet` dozvoljava.

---

Set omogućava prikupljanje smeća, dok WeakSet ne omogućava.
#### --answer--
Set može da sadrži bilo kakvu vrstu vrednosti, dok WeakSet sadrži samo objekte.
### --question--

#### --text--
Šta je Map u JavaScript-u?
#### --distractors--
Kolekcija koja dozvoljava samo jedinstvene vrednosti.

---

Kolekcija koja može da koristi samo stringove kao ključeve.

---

Kolekcija koja ne dozvoljava uklanjanje svojih elemenata.
#### --answer--
Kolekcija parova ključ-vrednost bilo kog tipa.
### --question--

#### --text--
Koji metod se koristi za umetanje parova ključ-vrednost u Map?
#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--
Šta će sledeći kod izbaciti?

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--
Koja je razlika između `Map`-a i `Object`-a u JavaScript-u?
#### --distractors--
Mapa dozvoljava samo brojeve kao ključeve.

---

Objects dozvoljavaju bilo koji tip kao ključeve, dok Maps dozvoljavaju samo stringove.

---

Maps ne podržavaju iteraciju, dok Objekti jesu.
#### --answer--
Maps dozvoljavaju bilo koji tip ključa, ali Objects dozvoljavaju samo stringove i simbole.
### --question--

#### --text--
Kako se može ukloniti par ključ-vrednost iz `Map`-a?
#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--
Šta svojstvo ``size`` objekta `Map` vraća?
#### --distractors--
Broj indeksa u Map-u.

---

Broj jedinstvenih vrednosti u Map-u.

---

Ukupna memorija koju koristi Map.
#### --answer--
Broj parova ključ-vrednost u Map-u.
### --question--

#### --text--
Šta je WeakMap u JavaScript-u?
#### --distractors--
Kolekcija slična Map-u koja dozvoljava primitivne vrednosti kao ključeve.

---

Kolekcija slična Mapi koja blokira brisanje stavki.

---

Kolekcija slična Mapi koja automatski sortira svoje ključeve.
#### --answer--
Kolekcija slična Mapu sa slabim referencama na ključeve.
### --question--

#### --text--
Koja je jedna razlika između Map-a i WeakMap-a?
#### --distractors--
Map dozvoljava samo primitivne tipove kao ključeve, dok WeakMap dozvoljava samo objekte.

---

Map omogućava prikupljanje smeća, dok WeakMap ne omogućava.

---

A WeakMap dozvoljava duplirane ključeve, dok Map to ne dozvoljava.
#### --answer--
`Map` je iterabilan, dok `WeakMap` nije.
### --question--

#### --text--
Koji od sledećih metoda NE postoji na WeakMap?
#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--
Koji će biti izlaz navedenog koda?

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--
Kako možete iterirati kroz vrednosti u Setu?
#### --distractors--
Korišćenjem metode `.map()`.

---

Korišćenjem metode `.reduce()`.

---

Korišćenjem metode `.filter()`.
#### --answer--
Korišćenjem metode `.forEach()`.
### --question--

#### --text--
Koje dve metode za `Sets` vraćaju ``SetIterator`` koji sadrži vrednosti određenog `Set`-a?
#### --distractors--
`entries()` and `values()`

---

`forEach()` and `values()`

---

`has()` and `add()`
#### --answer--
`keys()` and `values()`
### --question--

#### --text--
Koji će biti izlaz navedenog koda?

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
