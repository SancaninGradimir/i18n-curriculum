---
id: 673407be6af21d6766ed4b96
title: Kako API za Fetch funkcioniše sa standardnim HTTP metodama i res.json()?
challengeType: 19
dashedName: how-does-the-fetch-api-work-with-common-http-methods-and-res-json
---

# --description--

U prethodnoj lekciji, pogledali smo šta je API od Fetch i kako ga koristiti. U ovoj lekciji, razgovaraćemo o metodama za HTTP za `GET`, `POST`, `PUT`, i `DELETE` za API od Fetch.

Tuanze na njia ya kawaida zaidi ya HTTP ambayo ni njia ya `GET`. Hii hutumika kupata data kutoka kwa seva. Unapotumia `fetch()` bila kubainisha njia, kwa kawaida hutumia `GET`.

```js
fetch('https://api.example.com/data')
```

U ovom kodu, šaljemo zahtev od `GET` ka `https://api.example.com/data`. Sada, molimo vas da znate da ne možete koristiti ove podatke direktno; morate odgovor pretvoriti u format JSON. Tek nakon toga ga možete koristiti bilo gde u svom projektu. Evo primera kako to učiniti:

```js
fetch('https://api.example.com/data')
  .then(response => response.json())
  .then(data => console.log(data))
```

Katika msimbo huu, jibu linalotoka kutoka API ya Fetch ni ahadi, na tunatumia mshughulikiaji wa `.then` kubadilisha jibu kuwa muundo wa JSON. Katika funzo lililopita, ulijifunza kuwa `Promise` ni kitu kinacho wakilisha kukamilika kwa mwisho (au kushindwa) kwa mchakato isiyo ya wakati mmoja na thamani yake.

Thamani ya ahadi haijulikani wakati ahadi inapotengenezwa. Inajulikana tu wakati mchakato isiyo ya wakati mmoja umekamilika. Tunapounganisha mshughulikiaji wawili wa `.then` kwa wito la fetch, hii ni kitu kinachoitwa uunganishaji wa mfululizo wa ahadi ambacho kitafundishwa katika funzo lijalo.

Hadi sasa tumekuwa tukipata rasilimali kutoka kwa seva. Lakini, je, unajua kuwa tunaweza pia kutuma data kwa seva? Njia ya `POST` hutumika kutuma data kwa seva ili kuunda rasilimali. Hapa kuna mfano wa njia ya `POST` ambayo hutumika kuunda rasilimali kwenye seva:

```js
fetch('https://api.example.com/users', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    name: 'John Doe',
    email: 'john@example.com'
  })
})
```

U ovom primeru, šaljemo zahtev za `POST` kreiranje novog korisnika. Specifikujemo metodu kao `POST`, postavljamo odgovarajuća zaglavlja i uključujemo `body` sa podacima koje želimo poslati. `body` zahteva da bude niz znakova, pa koristimo `JSON.stringify()` da konvertujemo naš objekat u niz znakova tipa JSON.

Njia ya `PUT` hutumika kusahihisha rasilimali zilizopo kwenye seva. Hapa kuna mfano:

```js
fetch('https://api.example.com/users/45', {
  method: 'PUT',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    name: 'John Smith',
    email: 'john@example.com'
  })
})
```

Katika mfano huu, angalia kwa makini URL, unaweza kuona `45` mwishoni. Hii kwa kawaida hutumika kama kitambulisho cha kipekee kutambua data tunayotaka kusahihisha. Tulitumia njia ya `PUT` katika msimbo na pia kubainisha data kama `body` ambayo itatumika kusahihisha data iliyotambuliwa.

Njia ya `DELETE` hutumika kufuta rasilimali kutoka kwa seva. Hapa kuna mfano:

```js
fetch('https://api.example.com/users/45', {
  method: 'DELETE',
})
```

Katika msimbo huu, tunajumuisha njia ya `DELETE` na kitambulisho mwishoni mwa URL kutambua data inayohitaji kufutwa.

# --questions--

## --text--

Koji je standardni način za HTTP koji koristi `fetch()` ako nije definisan?

## --answers--

`POST`

### --feedback--

Razmislite o najčešćoj operaciji kada dohvatate podatke sa servera.

---

`GET`

---

`PUT`

### --feedback--

Razmislite o najčešćoj operaciji kada dohvatate podatke sa servera.

---

`DELETE`

### --feedback--

Razmislite o najčešćoj operaciji kada dohvatate podatke sa servera.

## --video-solution--

2

## --text--

Kada šaljete podatke za zahtev `POST` koristeći `fetch()`, šta morate uraditi sa podacima pre nego što ih uključite u `body`?

## --answers--

Otkri

### --feedback--

Razmislite kako podaci bi trebalo da se pripreme kada se koriste u telu zahteva.

---

Pletenje.

### --feedback--

Razmislite kako bi podaci trebalo da budu pripremljeni kada se koriste u telu zahteva.

---

Postane sekvenca slova JSON.

---

Napravi Base64 kod.

### --feedback--

Razmislite kako bi podaci trebalo da budu pripremljeni kada se koriste u telu zahteva.

## --video-solution--

3

## --text--

Šta radi putnica (ili ruta) `response.json()`?

## --answers--

Šaljem podatke za JSON na server.

### --feedback--

Razmislite kako podaci sa servera treba da se procesiraju radi korišćenja u JavaScript.

---

Kreira nešto novo za JSON.

### --feedback--

Razmislite kako podaci sa servera treba da se obrađuju kako bi bili korišćeni u JavaScript.

---

Analizira telo odgovora kao JSON.

---

Potvrdite da je odgovor u formatu JSON.

### --feedback--

Razmislite kako podaci sa servera treba da se obrađuju kako bi bili korišćeni u JavaScript.

## --video-solution--

3