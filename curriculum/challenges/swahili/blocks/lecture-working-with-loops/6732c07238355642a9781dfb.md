---
id: 6732c07238355642a9781dfb
title: Za šta se koriste izjave `break` i `continue` u petljama?
challengeType: 19
dashedName: what-are-the-break-and-continue-statements-used-for-in-loops
---

# --interactive--

Izjava `break` se koristi za rano izlaska iz petlje, dok se izjava `continue` koristi za preskakanje trenutne iteracije petlje i prelazak na sledeću.

Evo primera korišćenja deklaracije `break` u petlji `for`:

:::interactive_editor

```js
for (let i = 0; i < 10; i++) {
  if (i === 5) {
    break;
  }
  console.log(i);
}
```

:::

U gornjem primeru, petlja počinje da broji od `0` i dok je `i` manje od `10`, petlja će nastaviti da radi.

Unutar petlje, proveravamo da li `i` je jednako `5`. Ako jeste, koristimo izjavu `break` za ranije izlaska iz petlje. U suprotnom, logujemo vrednost `i` na konzolu. Dakle, izlaz koda će ispisati brojeve `0`, `1`, `2`, `3`, i `4`.

Izjava `break` je korisna kada želite rano izaći iz petlje na osnovu određenog uslova. Na primer, ako pretražujete određenu vrednost u nizu, možete koristiti izjavu `break` da napustite petlju čim pronađete tu vrednost.

Ponekad želite da preskočite određenu iteraciju petlje bez potpunog izlaska iz petlje. Ovde nam je potrebna naredba `continue`. Evo primera korišćenja naredbe `continue` u petlji `for`:

:::interactive_editor

```js
for (let i = 0; i < 10; i++) {
  if (i === 5) {
    continue;
  }
  console.log(i);
}
```

:::

Kao i ranije, inicijalizovali smo `i` na `0` i imamo uslov koji će pokrenuti petlju sve dok `i` bude manji od `10`.

Unutar petlje, kada je `i` jednako `5`, koristimo naredbu `continue` da preskočimo trenutnu iteraciju i pređemo na sledeću.

Izlaz ovog koda će ispisati brojeve `0`, `1`, `2`, `3`, `4`, `6`, `7`, `8`, i `9`. Broj `5` je preskočen zbog naredbe `continue`.

Još jedna stvar koju možete uraditi sa oba `break` i `continue` izjava je korišćenje oznaka za određivanje koji petlja želite da prekinete ili nastavite.

Ovo je korisno kada imate ugnježđene petlje i želite da kontrolišete tok spoljne petlje iznutra unutrašnje petlji.

Evo primera korišćenja labela sa izjavom `break`:

:::interactive_editor

```js
outerLoop: for (let i = 0; i < 3; i++) {
  innerLoop: for (let j = 0; j < 3; j++) {
    if (i === 1 && j === 1) {
      break outerLoop;
    }
    console.log(`i: ${i}, j: ${j}`);
  }
}
```

:::

U ovom primeru, imamo spoljašnju petlju `for` sa oznakom `outerLoop` i unutrašnju petlju `for` sa oznakom `innerLoop`.

Kada `i` je jednako `1` i `j` je jednako `1`, koristimo naredbu `break` sa oznakom `outerLoop` da bismo rano izašli iz spoljne petlje. Ovo će izaći iz unutrašnje i spoljne petlje.

Izlaz ovog koda će logovati sledeće u konzolu:

```md
"i: 0, j: 0"
"i: 0, j: 1"
"i: 0, j: 2"
"i: 1, j: 0"
```

Većinom vremena nećete naći potrebu da koristite oznake sa izjavama `break` i `continue`, ali je dobro znati da imate tu opciju ako vam ikada zatreba.
# --questions--

## --text--

Koja je svrha naredbe ``break`` u petlji?
## --answers--

Da preskoči trenutnu iteraciju i nastavi sa sledećom.
### --feedback--

Ime podrazumeva da vam omogućava da izađete iz petlje.
---

To exit the loop immediately.

---

To stop the program execution.

### --feedback--

Ime podrazumeva da vam omogućava da izađete iz petlje.
---

To restart the loop from the beginning.

### --feedback--

Ime podrazumeva da vam omogućava da izađete iz petlje.
## --video-solution--

2

## --text--

Šta se događa kada se izjava `continue` naiđe unutar petlje?
## --answers--

Petlja izlazi odmah.
### --feedback--

Razmislite o tome kada biste možda želeli da preskočite iteraciju petlje.
---

The loop starts over from the beginning.

### --feedback--

Razmislite o tome kada biste možda želeli da preskočite iteraciju petlje.
---

The current iteration is skipped and the next iteration starts.

---

The loop is paused.

### --feedback--

Razmislite o tome kada biste možda želeli da preskočite iteraciju petlje.
## --video-solution--

3

## --text--

Koja je svrha korišćenja oznaka sa izjavama `break` i `continue`?
## --answers--

Za određivanje koju petlju treba napustiti ili preskočiti kada se radi sa ugnježdene petlje.
---

To name the loops for easier debugging.

### --feedback--

Etikete se koriste za kontrolisanje toka izvršavanja u ugnježdenim petljama.
---

To restart the loop from a specific point.

### --feedback--

Etikete se koriste za kontrolisanje toka izvršavanja u ugnježdenim petljama.
---

To allow multiple break and `continue` statements within the same loop.

### --feedback--

Etikete se koriste za kontrolisanje toka izvršavanja u ugnježdenim petljama.
## --video-solution--

1

